{
  "ticket": "RYA-1138",
  "findings": [
    {
      "file": "config/constants.py",
      "line": 52,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_spectra_ext_dir()` \u2014 same shape as the ispec resolver: the historical `ROOT.parent/...` stays the FIRST candidate so a correctly sited checkout resolves unchanged, and the unresolved fallback keeps the old value so `skipif(not _HAS_DATA)` behaves exactly as before on a machine that genuinely lacks the tree. Looked at, never trusted.",
      "source": "candidates = [ROOT.parent.joinpath(*tail),                 # historical, first",
      "family": "spectra data volume"
    },
    {
      "file": "config/constants.py",
      "line": 64,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_spectra_ext_dir()` \u2014 same shape as the ispec resolver: the historical `ROOT.parent/...` stays the FIRST candidate so a correctly sited checkout resolves unchanged, and the unresolved fallback keeps the old value so `skipif(not _HAS_DATA)` behaves exactly as before on a machine that genuinely lacks the tree. Looked at, never trusted.",
      "source": "return found or ROOT.parent.joinpath(*tail)",
      "family": "spectra data volume"
    },
    {
      "file": "config/constants.py",
      "line": 1201,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_ispec_dir()` \u2014 the candidate LIST, the historical fallback and the error message all name `ROOT.parent / 'ispec'` on purpose: it stays the FIRST candidate so a correctly-sited checkout resolves exactly as before, but it is now VERIFIED (the ispec package must actually be there) rather than assumed. Escaping to look is fine; escaping to trust is not.",
      "source": "candidates = [ROOT.parent / 'ispec',            # the historical derivation, first",
      "family": "ispec engine install"
    },
    {
      "file": "config/constants.py",
      "line": 1212,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_ispec_dir()` \u2014 the candidate LIST, the historical fallback and the error message all name `ROOT.parent / 'ispec'` on purpose: it stays the FIRST candidate so a correctly-sited checkout resolves exactly as before, but it is now VERIFIED (the ispec package must actually be there) rather than assumed. Escaping to look is fine; escaping to trust is not.",
      "source": "return found or Path(_os.environ.get('ISPEC_DIR', str(ROOT.parent / 'ispec')))",
      "family": "ispec engine install"
    },
    {
      "file": "config/constants.py",
      "line": 1232,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_ispec_dir()` \u2014 the candidate LIST, the historical fallback and the error message all name `ROOT.parent / 'ispec'` on purpose: it stays the FIRST candidate so a correctly-sited checkout resolves exactly as before, but it is now VERIFIED (the ispec package must actually be there) rather than assumed. Escaping to look is fine; escaping to trust is not.",
      "source": "f\"  Tried: {ROOT.parent / 'ispec'}, {CANONICAL_WORKTREE_PARENT / 'ispec'}, \"",
      "family": "ispec engine install"
    },
    {
      "file": "config/constants.py",
      "line": 1359,
      "anchor": "repo",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_expand_repo_parent()` \u2014 the register's own token, now expanded against candidate parents and VERIFIED (the expansion must point at something that exists) instead of assumed to be `repo.parent`. The remaining escapes are the candidate list and the unresolved fallback, which keeps the old value so a path naming a not-yet-created directory still resolves and nothing begins raising at import.",
      "source": "for cand in (repo.parent, CANONICAL_WORKTREE_PARENT, Path.home() / 'Projects'):",
      "family": "path-register {repo_parent} token"
    },
    {
      "file": "config/constants.py",
      "line": 1369,
      "anchor": "repo",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_expand_repo_parent()` \u2014 the register's own token, now expanded against candidate parents and VERIFIED (the expansion must point at something that exists) instead of assumed to be `repo.parent`. The remaining escapes are the candidate list and the unresolved fallback, which keeps the old value so a path naming a not-yet-created directory still resolves and nothing begins raising at import.",
      "source": "return raw.replace('{repo_parent}', str(repo.parent))",
      "family": "path-register {repo_parent} token"
    },
    {
      "file": "pipeline/artifact_store.py",
      "line": 97,
      "anchor": "__file__",
      "file_depth": 2,
      "climbs": 3,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1090's `_legacy_derived_root()` \u2014 DELIBERATELY still derives the old pre-1090 root so the divergence guard can see a store left behind by the old derivation. It is never used AS the store root; `store_root()` is named.",
      "source": "return Path(__file__).resolve().parents[2]",
      "family": "other"
    },
    {
      "file": "pipeline/canonical_checkout.py",
      "line": 119,
      "anchor": "root",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1138's own `_canonical_location()` \u2014 compares a checkout's parent against the NAMED `CANONICAL_WORKTREE_PARENT`. It reads the parent in order to REPORT on it, and resolves nothing through it.",
      "source": "return root.resolve().parent == CANONICAL_WORKTREE_PARENT.resolve()",
      "family": "other"
    },
    {
      "file": "scripts/generate_sources_page.py",
      "line": 66,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_site_root()` \u2014 the sibling website checkout, searched across the parents site repos have actually lived under rather than assumed to sit beside this worktree. Falls back to the historical value because the target may legitimately not be checked out.",
      "source": "candidates = [ROOT.parent / name, CANONICAL_WORKTREE_PARENT / name,",
      "family": "sibling site repo"
    },
    {
      "file": "scripts/generate_sources_page.py",
      "line": 75,
      "anchor": "ROOT",
      "file_depth": 2,
      "climbs": 1,
      "levels_above_repo_root": 1,
      "known_external": true,
      "why": "RYA-1140's `_resolve_site_root()` \u2014 the sibling website checkout, searched across the parents site repos have actually lived under rather than assumed to sit beside this worktree. Falls back to the historical value because the target may legitimately not be checked out.",
      "source": "return found or ROOT.parent / name",
      "family": "sibling site repo"
    }
  ],
  "n_undocumented": 0
}

# FITS and spectral data files
*.fits
*.fit
*.fts
*.fits.gz

# Pipeline outputs
results/plots/
results/tables/
data/raw/
data/processed/
# RYA-469: per-star NAMESPACED working outputs (regenerable). The FROZEN gold solar
# reference under data/reference/ is committed (NOT ignored) — it is the differential
# denominator and is immutability-guarded.
data/outputs/

# Stewardship check artifacts (regenerated by scripts/check_stewardship.py — RYA-355)
data/results/gf_divergence_*.csv

# Model atmosphere grids (large binary files)
data/model_atmospheres/*.mod
data/model_atmospheres/*.dat

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Environments
.env
.venv
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
ispec.log

# molecfit/esorex transient log
esorex.log

# RYA-396 NLTE-grid scrape checkpoints (intermediate)
data/nlte_grids/raw/

# RYA-402: Amarsi/GALAH PySME departure grids (multi-GB binaries; re-fetchable from the provenance JSON, never committed)
data/nlte_grids/**/*.grd
# RYA-410: the Zenodo download archives are transient (extracted then deleted) — never commit
data/nlte_grids/**/*.tar.gz
# RYA-426: UV conditioning manifests (per-star working artifacts, regenerable)
data/conditioning/

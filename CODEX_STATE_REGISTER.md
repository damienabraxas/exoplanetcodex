# Codex State Register

**The mutable current-truth ledger. Read this FIRST.**

The Linear tickets are an *immutable journal* — every step, dead end, and reversal, recorded and never rewritten. This register is the *mutable ledger* — the single authoritative statement of **what is true right now**, rewritten as things settle, regress, or supersede. When "current state of X" and "what the journal says happened to X" diverge, **this file wins for state**; the journal wins for history.

Built to be read fast by both Ryan/Claude.ai **and** local models on Sirius (Qwen etc.) — keep it clean-table, one fact per row, machine-parseable.

## How to read this — NATIVE vs MIRROR rows
- **NATIVE** — this register *is* the source of truth: verdicts, statuses, gate states, scope decisions, grid/model/instrument selections, `reopen-only-if` triggers. Hand-maintained here.
- **MIRROR** — reflects another single source (stellar params ← `config/constants.py` → `STAR_PARAMS`/`stars.yaml`; abundances ← results tables). Shown for fast access, but **regenerated from source, never hand-trusted**. A mirror value that disagrees with its source is a defect. Mirror tables are **script-generated** (`scripts/gen_state_register_targets.py`), not typed.

**Maintenance discipline:** update a row the moment its component settles, regresses, or is superseded — and always at a gate sign-off (a gate can't sign off until its rows are current). Every value cites its source (no value from memory). `[confirm]` = reconstructed from working memory, not yet source-cited — **not settled until the citation is attached.** Full procedure: `skills/codex-state-register/SKILL.md`, wired into session-close in `DEV_CYCLE.md`.

**Status vocab:** `SETTLED` · `SETTLED-WITH-CAVEAT` · `REGRESSED` · `STALE` · `OPEN` · `NOT-SELF-SUFFICIENT` · `PENDING`

**Version: v31** · _Last updated: 2026-08-08 · By: Mr. Code (**RYA-681 — the RYA-553 Fe 1D→3D idempotency guard is re-keyed on the VALUE, and the FE_GATE blind spot is closed.** RYA-669 measured a fresh phase_c run landing **A(Fe I) 7.416**, not 7.466: the Magic-2013 −0.05 dex correction double-applied, with `tests/test_solar_calibration_gate.py` **9 passed** and the RYA-632 ledger guard **0 undocumented**. Two structural causes, both fixed here. **(1) The guard keyed idempotency on a prose label a DIFFERENT process writes.** `scripts/phase_c_verdict_rya371.py` skipped the offset iff the gold row's `method_scale` contained `3D` — and `scripts/build_solar_reference_v2_rya522.py` **hardcoded** `"1D-NLTE (Fe I)"` for Fe, so the label could never follow the value. The correction therefore sat in a closed loop (gold → verdict → candidate → freeze → gold) with a **ratchet**: RYA-665 froze the post-correction 7.466 under the pre-correction label and the guard re-armed; left alone, every freeze cycle subtracts another 0.05. Idempotency is now keyed on the value, in one new module **`pipeline/solar_scale_provenance.py`** — an explicit machine-readable **`scale_state`** column written FROM the verdict's own `fe_1d3d_correction` record (data copied from data), cross-checked against where the number actually sits between the two published scale centres (Asplund 3D-true 7.46; that + the tabulated granulation term 7.51), with the decision boundary at **|correction|/2 = 0.025** — derived from the correction, not tuned. Declaration-vs-value disagreement, an undeclared scale, or a value on neither scale all **RAISE**. The builder derives Fe's label via `method_scale_label()` and refuses to write the contradiction it now refuses to read. **(2) `FE_GATE` is NOT narrowed — deliberately.** It is a symmetric ±0.05 physical acceptance window on the 3D-true reference and the correction is also 0.05, so a doubled correction lands at `A_correct − 0.05`, **inside the window for any correct anchor ≥ 7.46** — structural, not a coincidence of today's numbers. Narrowing to h < 0.044 would fit the coincidence, leave the correct anchor 0.038 dex of headroom, and still miss a smaller doubled correction. RYA-166 gate **A1 instead gains an ORTHOGONAL scale-identity assertion** (reported anchor must classify onto 3D-NLTE; the value the correction was applied TO must classify onto 1D-NLTE): 7.466 → 3D **PASS with 0.019 dex margin**, 7.416 → **NEITHER, FAIL**, 7.516 → 1D FAIL. `FE_GATE [7.41,7.51]` unchanged; **9 assertions in, 9 out.** **(3) The hole itself:** `tests/test_fe_1d3d_idempotency_rya681.py` (26 tests) demonstrates the shipped A1 gate **failing on a substituted 7.416 verdict and passing on 7.466**, the correction applying once, being skipped on a 3D row, **converging instead of ratcheting** under repeated application, and raising on gold v3's shape / an already-doubled anchor / an undeclared scale / a mis-stamped column. **⚠️ THE VERDICT CHANNEL IS NOW LOUDLY BLOCKED — this is the intended outcome, not a regression.** Gold v3's Fe row is genuinely self-contradictory (3D value 7.466 under the 1D label), so phase_c **refuses to regenerate** rather than emit 7.416. Gold is write-once (RYA-469) and the wrong cell is the LABEL, not the value — **7.466 is correct** — so clearing this needs a **ratified gold v4**, which is **RYA-669's call, Ryan's decision**. Seventeen tests that re-run `build_verdicts` off the live gold carry a **CONDITIONAL** xfail (reason single-sourced in `tests/gold_scale_blocker.py`, read off the live row) that **retires itself** the moment a self-consistent gold is promoted. **NO gold vN, `CURRENT`, `hash_manifest.json` or measured value was modified; gold v3 sha256 47ad869e…83421 verified unchanged.** The RYA-653 corrected candidate is regenerated because the builder changed — **no value moved**, only Fe's label/note plus the new column — and it is now the self-consistent shape a v4 would take. Ledger guard unchanged at 2 documented-owed / **0 undocumented**. Files: `pipeline/solar_scale_provenance.py` (new), `scripts/phase_c_verdict_rya371.py`, `scripts/build_solar_reference_v2_rya522.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`, `tests/test_fe_1d3d_idempotency_rya681.py` (new), `tests/gold_scale_blocker.py` (new), `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv`, `CODEX_STATE_REGISTER.md`, `SEQUENCE.md`. Prior — **RYA-665 — gold v3 is FROZEN: the pre-527 mini-freeze.** `data/reference/solar/CURRENT` moves **v2 → v3**, promoting the RYA-653 corrected candidate through `scripts/promote_solar_reference.py` (RYA-469 write-once + hash manifest; no hand-edit path). **Exactly two frozen VALUES change** — **Fe I 7.516→7.466** (RYA-553 tabulated Magic-2013 1D→3D) and **Mn I 5.47→5.466** (RYA-546 ab-initio, reversing RYA-411's keep-MPIA) — plus two verdicts (**Co I → PASS**, RYA-564; **N I NLTE-OWED → CURATION-OWED**, RYA-556) and the RYA-653/596 note-honesty corrections on Ba/Ca/Ti/Ni/Na/Al/Sr. **Disputed values are NOT immortalised:** the RYA-522 `owed` tier freezes no value, so Co 4.965, S 7.486, Ba 2.410, N 8.188 and Sr all land as visible-but-HELD rows, and RYA-581's Ba deblend stays open post-freeze. Verified 29/29 cells against `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` before applying. Ledger guard **3 → 2** failures (Ba's phantom red cleared, 0 undocumented); its Ba `_OWED_NOT_LAUNDERED` entry and the four Class-B gold-lag divergences for Co/N were **deleted per their own retire-on-freeze instructions** — a stale ratified divergence is a hiding place for the next real contradiction. **NOT the two-engine re-emit:** RYA-527 Phase 2 still owes a fresh run on current main, which validates v3 or produces v4 (Ca's provisional flip, Na pending RYA-664, Sr II 2.759 adoption, Co 4.965-vs-4.960). Prior — **RYA-654 — the element status tracker is GENERATED, and the physics_regime GET-DATA-vs-measured class is adjudicated on the EW-vs-synthesis axis.** The tracker had **no generator** (RYA-632 audit): hand-edited, still carrying Co's DEMOTED blue-edge +1.188 and N's already-cleared NLTE debt. It is now built by `scripts/generate_element_status_tracker_rya654.py` — RYA-436 Move B for the results side — with status columns sourced from the **phase_c verdict channel (ratified canonical)**, `tier` from the RYA-522 ratified freeze tiers, `ion`/`regime_verdict` from `config/physics_regime_rya400.yaml`, and the analyst columns from a new hand-authored sidecar `data/audit/element_status_tracker_editorial.yaml`. `--check` diffs committed-vs-regenerated so **hand-editing the CSV is a build break**; the generator **refuses to run off an uncommitted phase_c**, so a Mac-canary re-run cannot reach the ledger. A new explicit **`verdict` column** ends the tier-vs-verdict vocabulary mixing the guard used to infer through. **The six divergent GET-DATA rows re-cut on the EW-measurable-vs-synthesis-sourced axis, using live EW-pool line counts, not documentation state:** **Co/N/K/Cu = verifier-blind on a REAL measurement** (0/0/0/2-but-junk EW-pool lines; measured on the synthesis or Kitt Peak atlas leg) → **RATIFIED as ledger divergences**, verdicts unchanged, no promotion; **P = genuinely stale** — it is the one member with real EW-pool lines (P I 6034.04/6043.12) and its GET-DATA rested on "FUV needs HST/STIS (RYA-119)", a premise phase_c records as **superseded for the Sun** → row **FIXED**, `GET-DATA` → `HARD-carry-forward`; **Sc = measurement-not-trusted** (its only value is the KP Sc II 4246 blue-edge HFS single line, held LOW_CONFIDENCE by phase_c itself) → **NOT annotated, deliberately still RED**. Both RYA-632 count mismatches cleared as a by-product (**PASS 5→6** Co, **NLTE_OWED 1→0** N); the guard now reports **0 undocumented failures** and 2 documented-owed elements (Sc above; **Ba**, whose gold v2 phantom is corrected in the RYA-653 candidate and lands at the RYA-527 v3 re-freeze). **NO measured value, verdict, gold vN or `constants.py`/`STAR_PARAMS` was modified.** Prior — **RYA-653 — the blank-cause honesty tripwire is now SHARED, and gold v2's Ba phantom is corrected in a candidate.** RYA-596's `_assert_blank_cause_is_honest` was phase_c-only; the identical unchecked fallthrough sat one file over in `scripts/build_solar_reference_v2_rya522.py::_scale_and_note` and was actively lying in frozen gold v2's **Ba** row ("no independent-gf line survives the graded cull" against a Ba that RYA-559 **measured** at 2.410). Extracted to **`pipeline/provenance_honesty.py`** — one module, imported by BOTH emitters, no forked copy — and strengthened: the zero-survivor claim is now refused on any row with `n_lines > 0` **or** a measured value. **The gap was wider than the ticket predicted:** rebuilt against today's verdict the OLD builder would have fabricated the same phantom on **five more rows** — Ca/Ti/Ni/Na/Al, carrying 2/10/2/2/1 graded survivors, i.e. RYA-596's exact five re-entering through the other file — and would have **crashed outright on Sr** (`TypeError`, `d=None`); it also stamped Ba with `HFS-resolved synthesis (RYA-411/466/473)`, the Mn/Cu/V tickets' citation, for a measurement that is RYA-559's. Value-less rows now carry the verdict's OWN stated cause (attributed, never invented); `owed`-tier rows that HAVE a verdict value say so (`A(X) … HELD at tier 'owed' (RYA-522), not frozen`) instead of reading as never-measured. **Ba sweep: Ba is the only phantom** — Mg/Y/Zr/Eu keep the claim honestly (0 survivors, no value, RYA-596-verified). **NOTHING IS FROZEN:** the corrected artifact is `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv` + per-cell diff `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` (**29 cells differ vs frozen v2** — because the builder is verdict-sourced by design, it necessarily carries the post-v2 landings: Fe 7.516→**7.466** (RYA-553), Mn 5.47→**5.466** (RYA-546), Co CURATION-OWED→**PASS** 4.965 (RYA-564), S→synthesis **7.486** (RYA-492), N wired **8.188** (RYA-556), Ba **2.410** (RYA-559)). That is gold-v3 content and its freeze is **RYA-527's gate, Ryan's call — deliberately NOT performed here.** ⚠️ Carried forward for the freeze: the candidate's Co reads **4.965** (the live verdict), while RYA-643's corrected re-run gives **4.960** — the pre-existing two-artifact split, not introduced here. RYA-632's guard confirms the correction: **15 reds → 14**, Ba cleared, with the corrected candidate substituted for the immutable frozen v2. Prior — **RYA-313/314 — the merge gate is real for the first time.** CI had **never run** in this repo's history; `main` was PR-gated but with no status check to gate on, so "merge only on green" was unenforceable and merges leaned on `--admin`. Three things landed. **(1) The suite is green** — main was **14 red**; 12 were stale expectations asserting a *closed* defect still exists (RYA-545 Ti→Mallinson 3 lines/+0.052 and its retired 55 Cnc clamp; RYA-540 Cu registered; RYA-526/556 N wired; RYA-522 gold CURRENT→v2), and **2 were a real bug** — RYA-506's iSpec/MOOG scratch-dir `makedirs` was added to one call site while three others passed the same literal path uncreated, now single-sourced as `ensure_ispec_tmp_dir()`. **(2) A latent defect CI itself surfaced: `np.trapz` was REMOVED in NumPy 2.0**, and RYA-517 declares the reference stack as py3.12+numpy 2.2 — so all **12** EW-integration call sites were **already broken on the production runner**, invisible locally because the Mac dev env pins numpy 1.26. Fixed once in `pipeline/_numcompat.py`. Suite now green on **both** stacks (py3.9/numpy1.26 and py3.12/numpy2.5) at 1116 passed / 9 skipped / 1 xfailed. **(3) CI runs on a Sirius self-hosted runner**, not `ubuntu-latest` — 24 of 118 test files import iSpec and cannot be collected on an ephemeral runner (measured: 24 collection errors, 868/1116), which would be a green check hiding 22% of the suite. Sirius has the engines and is the authoritative runner, so this honours RYA-567. Ruleset now requires **`CI / test`**; merge style ratified as **merge-commit** and squash/rebase disabled; the redundant admin-bypass ruleset retired. ⚠️ Repo is PUBLIC → fork-PR runs require approval for all external contributors. Also corrected a **two-month-stale register note**: the RYA-530 "Cu grid NOT committed / NOT wired" line was superseded by RYA-540 and is now fixed — the CI suite is what caught it. Prior — RYA-659 **register re-synced to the RYA-556..652 backlog + a freshness guard so it cannot drift silently again**. The register had gone **11 state-changing tickets stale** — last touched 2026-07-17 (`08265f9`, RYA-553) while `data/audit/element_status_tracker.csv` moved to 2026-07-27 (`97485e9`, RYA-643); RYA-643 changed element state and did **not** bump the register, which is the exact hole "a gate cannot sign off while its rows are stale" exists to close. Synced from the LIVE artifacts (not from memory): **solar 27-el verdict 5/1/20/0 → 6 PASS / 0 NLTE-OWED / 20 CURATION-OWED / 0 DATA-GAP**, PASS = O, C, Fe, Mn, K, **Co** (RYA-564 promoted Co on red HFS lines; RYA-556 took N off NLTE-OWED) — the row's **STALE** flag is cleared for these landings but the re-freeze on the two-engine floor remains **RYA-527**. Post-v22 measured-but-unfrozen landings recorded: **S 7.486** (RYA-557), **Ba 2.410** (RYA-559, +0.15 blend-inflated — deblend owed), **Co 4.965** (RYA-564; RYA-643's corrected re-run gives **4.960**, not yet in the verdict), **Mn 5.466** (RYA-546). **Sr II** RYA-551 superseded by RYA-643: 4077 **2.769 → 2.759**, worst reliable \|Δ\| 0.031 ≤ 0.05 → CONFIRMED-ROBUST and **cleared for RYA-527 adoption — which is NOT the same as adopted** (the live verdict still reads the Sr I EW leg 4.961; near-UV fit quality red-χ² 78 (4077) / 180 (4215) vs Mg 1.4 is a SEPARATE open concern). New **owed-HELD vs owed-BLANK** annotation so "owed" stops reading as "undone" (RYA-596). New machinery registered: RYA-558/561/563/567/279/280/594/596/631/642/652. Durable additions: **`LEDGERS.md`** (repo-root startup index naming the canonical read-set) + **`scripts/check_register_freshness.py`** (loud-fails when a state surface is newer than this register). NO measured value, verdict, gold reference, or `constants.py`/`STAR_PARAMS` was modified by this ticket. Prior — RYA-553 **solar Fe 1D→3D correction APPLIED** — the tabulated Magic-2013 (Stagger 3D, A&A 557 A26) 1D→3D solar Fe offset is now applied to the REPORTED Fe I anchor: A(Fe I) **7.516 (1D-NLTE) → 7.466 (3D-NLTE)**, on the true 3D scale (δ vs Asplund 7.46 = +0.006, was +0.056). Magnitude single-sourced from `FE_1D3D_SOLAR_OFFSET` via `CORRECTIONS_3D['Fe_1D3D_solar_dex'] = -0.05` (no duplicated literal). Applied at the reported-value layer (phase_c verdict → gold → gate), additive after NLTE, **idempotent on `method_scale`** (skips an already-3D anchor, so a re-frozen v3 gold is never double-corrected), logged + pre/post recorded. **`FE_GATE [7.41,7.51]` restored as the real solar reported-value gate**; the scale-aware diagnostic band `[7.44,7.58]` (`FE_ABS_DIAG_HALFWIDTH`) is retired *as the solar gate* but retained for the un-corrected 1D-NLTE measurement layer + off-solar stars (per-Teff/[Fe/H] 3D owed → **RYA-550**). RYA-166 gate A1 rewritten (3D-corrected A(Fe I) ∈ FE_GATE) + guards the correction is applied; all 8 assertions green (uncorrected 7.516 and gross +0.3 both FAIL → gate keeps teeth). Fe II NOT shifted (ionization arbiter untouched). SOLAR ONLY. **Unblocks the RYA-527 v3 freeze on the 3D scale.** Files: `config/constants.py`, `scripts/phase_c_verdict_rya371.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`. Prior — RYA-549 **Fe anchor: integrity gap CLOSED + vintage confirmed BENIGN** (the RYA-524 headline, de-escalated). Gate-0 forensic: `Fe_Bergemann_MPIA` is the INTENTIONAL live source (RYA-319 `5d42702`, chosen for full-benchmark-box coverage); the register's "Amarsi-2022-NN" selection row was STALE (the Amarsi MLP is archived — its Teff≤6500/[Fe/H]≤0 ceiling can't reach Procyon 6554/55 Cnc +0.32). Reconciled the register NLTE-grids Fe row → MPIA (the wiring matrix already matched the code → **code=register=matrix**). VINTAGE: the MPIA Fe NLTE δ is TINY — **+0.010 solar / +0.004 at 55 Cnc** (median over 252 Fe I lines), ionization-balance-gated (RYA-406 Fe I−Fe II ≈0 PASS) → **NOT the Ti/Mn/Cr inflated-over-ionization scaled-Drawin class**; the +0.056 vs Asplund 7.46 is the documented 1D-NLTE-vs-3D scale offset (RYA-336), not the NLTE correction. 55 Cnc residual-risk (param-dependent non-cancelling part) = **−0.006 dex, negligible**. Ab-initio migration STOPPED (no banked Fe grid covers metal-rich; the stake is negligible) → deferred **RYA-550**. **Unblocks RYA-527** (Fe confirmed correct as-is; safe to re-freeze). NO code change (Fe was already correct — RYA-319). Prior — RYA-545 **Ti I wired onto the Mallinson-2024 ab-initio NLTE grid** — the STOP→GO corroboration-accept (Ryan 2026-07-13). Replaced the outdated `Ti_Bergemann2011_MPIA.csv` (Bergemann-2011 **scaled-Drawin** +0.108, inflated ~2×) with `Ti_Mallinson2024_PySME.csv` (Zenodo 10753497, ab-initio **Grumer-Barklem-2020** H collisions) as the production Ti Engine-A source; derived the multi-node grid via PySME/MARCS (Ti I 5689/5648/5662, solar δ **+0.0506** = per-line +0.050/054/051, reproduces the published +0.052; physical Teff/logg trend, hull 2500–8000 K → the old 6500 K wall is gone). **Ti CHECK → registered** — certified by the new **corroboration-accept** standing rule (`docs/SCIENCE_STANDARDS.md`, five bounded criteria, **Ti = first invocation**), NOT the ionization-balance gate (which STOPPED on *precision* — SEM ~0.12–0.16 on the thin solar Ti pools, a documented diagnostic, not a value defect; RYA-544/545). RYA-534 Ti xfail **reason updated (RCA closed)** — NOT flipped to a fake PASS: that gate exercises the **Engine-B TS-Gerber** deck, which still ships the outdated `atom.ti503b` → +0.22 (superseded, RYA-546), so it stays honest strict-xfail until the Engine-B Ti atom is swapped (follow-on **RYA-548**); Ti's PRODUCTION NLTE is now the Engine-A Mallinson wire. Closes RYA-535/542. v1 frozen A(Ti) 5.471 stays (this sets the forward/v2 correction only). Files: `pipeline/pysme_nlte.py` (permanent Ti entry), `config/constants.py`, `data/nlte_grids/Ti_Mallinson2024_PySME.csv`(+prov), `docs/SCIENCE_STANDARDS.md`, `scripts/rya545_derive_ti_grid.py`, `tests/test_ts_gerber_rollout_rya534.py`. Prior — RYA-546 **Mn NLTE re-based onto the ab-initio triplet-exact δ** — solar Mn's correction moved from the MPIA/Bergemann **scaled-Drawin** +0.107 (high-EP reference lines) to the **live Amarsi-2020 GALAH** ab-initio HFS-resolved δ **+0.024** on the measured Den Hartog triplet 6013/16/21 (HFS identical on both legs; corroborated by the RYA-534 Engine-B TS-Gerber +0.043). This **REVERSES the RYA-411 "keep MPIA +0.107" decision** — RYA-411 trusted the "dedicated" B&Gehren-2008 atom, but RYA-546's H-collision **vintage audit** showed that atom IS the outdated scaled-Drawin recipe (the Ti defect, RYA-542/544). **A(Mn)☉ = 5.466 (+0.046 vs Asplund 5.42) → PASS** (was 5.554 on the +0.107 mismatch); **RYA-411 closed**. Landed the hard in-hull guard `pysme_nlte.assert_in_grid_hull` (no silent grid extrapolation). Gold Mn 5.554→5.466 folds at the RYA-527 v3 re-freeze; EW-path grid repoint = RYA-527 fan-out. Prior — RYA-525 **two-engine floor BUILT** — the per-line reference-blind selector + inverse-variance element aggregation + `CROSS_ENGINE_MIX_GATE` + loud-fail guards + tuning firewall are wired (`pipeline/engine_selection.py`, `config.TWO_ENGINE`, `docs/SCIENCE_STANDARDS.md`); the silent-LTE swallow in `abundances_derive.run()` is removed; the RYA-526 two-engine ledger is the guard's exception list; full 27-element both-engine RUN gated on RYA-526 grid coverage → RYA-527. Prior — RYA-530 capability-sweep reconciliation — registered ~14 BUILT-but-unregistered capabilities the ledger had missed [single-source gf architecture; NLTE grids Al/K/S/Sr; HFS-resolved synthesis; EW-verification layer; loader frame/OBJECT contract; multi-arm CNO arm-registry; reflected-solar RV; UV conditioning; IR-native α Cen star-ID; multi-instrument loaders; molecular converter; vac↔air SSOT; 1D→3D applied-delta], corrected the Na/Mg/Si NLTE source drift (was mis-cited Lind/MPIA → Amarsi-2020 PySME, RYA-410), and flagged the [O I] 6300 continuum-renorm dead-end so it isn't rebuilt. Verified against origin/main + code+git evidence; map + edits at `data/audit/rya530_capability_sweep/`. Prior — RYA-535: reverted the premature Ti 'model-atom difference' finding to CHECK + opened the RCA (same Bergemann-2011 atom both engines — refuted by Gerber 2023 Table 1); ruled out unit-clamp + grid-placeholder, departures physical, narrowed to MAFAGS-OS-vs-MARCS atmosphere / downstream (findings: data/audit/rya535_ti_rca/). Prior RYA-534: Family-A TS-Gerber NLTE rollout — **Family-A rollout: 10/11 clean, Ti CHECK (RCA open RYA-535)** — 10 PASS (Na/O/Mg/Ca/Mn/Sr/Ba/Ni/Co/Si); Ti = CHECK, an UNRESOLVED same-atom systematic (+0.221 vs +0.107, Engine-A/B both Bergemann-2011 atom) — RYA-535 reverted the premature 'model-atom' finding, RCA in progress — driver config corrected for all (Sr/Ba ionised-species lookup, Co/Ni GES level-IDs, Si/Ti large-grid disk-contention). Element-general deck `scripts/ts_gerber_gate.py`. Prior — RYA-533: TS-native Gerber NLTE deck forensics = NOT-BUILT on all 5 channels (test-tell/git/tree/grids/registration; RYA-361 scoped it, never executed). Built + Na-VALIDATED on Sirius: Turbospectrum_NLTE BSYN v20.1 compiled + Na Gerber-2023 (A&A 669 A43) grid/atom/aux provisioned (md5-pinned, Sirius-only) + bsyn NLTE deck; Na gate GREEN (median δ −0.068 vs −0.107, PASS, validate-don't-tune). Engine-B TS-native NLTE now SETTLED (Na); per-element rollout mechanical. Resolved 2022-vs-2023: arXiv 2206.00967 = A&A 669 A43. Prior context from RYA-531: corrected the RYA-529 PySME-wholesale row to the two-family NLTE-derivation map + the Engine-A/Engine-B distinction. FINDING: production NLTE is universally a 1D delta CSV (Engine A, multi-source: PySME-derived / MPIA / Korotin / INSPECT / nlte_cno); Engine B (TS synthesis) is LTE-only for all 27 — the Gerber-2022 TS-native NLTE deck is NOT built / NOT provisioned (empty gerber_ts on Sirius). RYA-402 Family-A TS-native was a deliberately-abandoned route, not a re-provision → Engine-B NLTE OWED for RYA-525; per-element map at data/audit/rya531_engineB_map/)._ Prior — **RYA-581 — the Ba II 5853 deblend LANDED: A(Ba) 2.410 → 2.237, verdict CURATION-OWED → PASS, value still HELD.** RYA-559 inverted the `blend_flag=True` pool EW (74.62 mÅ vs the clean solar line ~64 mÅ); an EW inversion **cannot** deblend — one scalar cannot separate barium from the rest of the absorption in the integration window. `scripts/rya581_ba2_deblend_sirius.py` fits the PROFILE instead (RYA-551 in-window pattern, shared RYA-643 fitter): the full VALD3 in-window block (1432 rows / 63 species) is synthesised alongside the 23 Ba II HFS components and A(Ba) is fitted by χ², with the Engine-A Korotin2015 δ −0.0285 READ at the solar node. **A_LTE 2.266 → A_NLTE 2.237** (HARPS primary; IAG 2.205; inter-arm σ 0.016). The blend is demonstrably MODELLED not culled — red χ² **3.90 → 0.71 (5.49×)** with it present, 6.61 mÅ under the core dominated by **Fe I 5.65 mÅ**. **Honest split:** only **−0.020 dex** of the −0.173 comes from the blend model; **−0.153** comes from abandoning the EW inversion — the blend sits in the WINGS, so it dominates the residual without moving A much. Independent check: the fitted core EW **66.24 mÅ** reproduces the RYA-559 calibration (A=2.27→66.5) and the literature clean line. `_ba_reclassify` branches on ticket into a deblend fold; the RYA-559 record survives as the loader fallback. ⚠️ **NOT promoted to gold:** one line, σ is precision-only, and **Ba emits no two-engine record** (the synthesis route is unwired in `_dedicated_engine_B()`, as for Co) ⇒ gate 3 stays UNEVALUABLE and cannot see 2.237. Flagged for the RYA-527 freeze review. ⚠️ `RELIABLE_RCHI2 = 60.0` is inherited from RYA-564 (not 551/560, which have no rchi2 ceiling) and is **UNRATIFIED**; RYA-585/PR #189 adds an equivalent `fit_profile_deblend` to the same shared module with a ~5.0 ceiling — the two must converge before both land. Not load-bearing here: Ba fits at red χ² 0.71.** Prior — **RYA-665 — gold v3 is FROZEN: the pre-527 mini-freeze.** `data/reference/solar/CURRENT` moves **v2 → v3**, promoting the RYA-653 corrected candidate through `scripts/promote_solar_reference.py` (RYA-469 write-once + hash manifest; no hand-edit path). **Exactly two frozen VALUES change** — **Fe I 7.516→7.466** (RYA-553 tabulated Magic-2013 1D→3D) and **Mn I 5.47→5.466** (RYA-546 ab-initio, reversing RYA-411's keep-MPIA) — plus two verdicts (**Co I → PASS**, RYA-564; **N I NLTE-OWED → CURATION-OWED**, RYA-556) and the RYA-653/596 note-honesty corrections on Ba/Ca/Ti/Ni/Na/Al/Sr. **Disputed values are NOT immortalised:** the RYA-522 `owed` tier freezes no value, so Co 4.965, S 7.486, Ba 2.410, N 8.188 and Sr all land as visible-but-HELD rows, and RYA-581's Ba deblend stays open post-freeze. Verified 29/29 cells against `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` before applying. Ledger guard **3 → 2** failures (Ba's phantom red cleared, 0 undocumented); its Ba `_OWED_NOT_LAUNDERED` entry and the four Class-B gold-lag divergences for Co/N were **deleted per their own retire-on-freeze instructions** — a stale ratified divergence is a hiding place for the next real contradiction. **NOT the two-engine re-emit:** RYA-527 Phase 2 still owes a fresh run on current main, which validates v3 or produces v4 (Ca's provisional flip, Na pending RYA-664, Sr II 2.759 adoption, Co 4.965-vs-4.960). Prior — **RYA-654 — the element status tracker is GENERATED, and the physics_regime GET-DATA-vs-measured class is adjudicated on the EW-vs-synthesis axis.** The tracker had **no generator** (RYA-632 audit): hand-edited, still carrying Co's DEMOTED blue-edge +1.188 and N's already-cleared NLTE debt. It is now built by `scripts/generate_element_status_tracker_rya654.py` — RYA-436 Move B for the results side — with status columns sourced from the **phase_c verdict channel (ratified canonical)**, `tier` from the RYA-522 ratified freeze tiers, `ion`/`regime_verdict` from `config/physics_regime_rya400.yaml`, and the analyst columns from a new hand-authored sidecar `data/audit/element_status_tracker_editorial.yaml`. `--check` diffs committed-vs-regenerated so **hand-editing the CSV is a build break**; the generator **refuses to run off an uncommitted phase_c**, so a Mac-canary re-run cannot reach the ledger. A new explicit **`verdict` column** ends the tier-vs-verdict vocabulary mixing the guard used to infer through. **The six divergent GET-DATA rows re-cut on the EW-measurable-vs-synthesis-sourced axis, using live EW-pool line counts, not documentation state:** **Co/N/K/Cu = verifier-blind on a REAL measurement** (0/0/0/2-but-junk EW-pool lines; measured on the synthesis or Kitt Peak atlas leg) → **RATIFIED as ledger divergences**, verdicts unchanged, no promotion; **P = genuinely stale** — it is the one member with real EW-pool lines (P I 6034.04/6043.12) and its GET-DATA rested on "FUV needs HST/STIS (RYA-119)", a premise phase_c records as **superseded for the Sun** → row **FIXED**, `GET-DATA` → `HARD-carry-forward`; **Sc = measurement-not-trusted** (its only value is the KP Sc II 4246 blue-edge HFS single line, held LOW_CONFIDENCE by phase_c itself) → **NOT annotated, deliberately still RED**. Both RYA-632 count mismatches cleared as a by-product (**PASS 5→6** Co, **NLTE_OWED 1→0** N); the guard now reports **0 undocumented failures** and 2 documented-owed elements (Sc above; **Ba**, whose gold v2 phantom is corrected in the RYA-653 candidate and lands at the RYA-527 v3 re-freeze). **NO measured value, verdict, gold vN or `constants.py`/`STAR_PARAMS` was modified.** Prior — **RYA-653 — the blank-cause honesty tripwire is now SHARED, and gold v2's Ba phantom is corrected in a candidate.** RYA-596's `_assert_blank_cause_is_honest` was phase_c-only; the identical unchecked fallthrough sat one file over in `scripts/build_solar_reference_v2_rya522.py::_scale_and_note` and was actively lying in frozen gold v2's **Ba** row ("no independent-gf line survives the graded cull" against a Ba that RYA-559 **measured** at 2.410). Extracted to **`pipeline/provenance_honesty.py`** — one module, imported by BOTH emitters, no forked copy — and strengthened: the zero-survivor claim is now refused on any row with `n_lines > 0` **or** a measured value. **The gap was wider than the ticket predicted:** rebuilt against today's verdict the OLD builder would have fabricated the same phantom on **five more rows** — Ca/Ti/Ni/Na/Al, carrying 2/10/2/2/1 graded survivors, i.e. RYA-596's exact five re-entering through the other file — and would have **crashed outright on Sr** (`TypeError`, `d=None`); it also stamped Ba with `HFS-resolved synthesis (RYA-411/466/473)`, the Mn/Cu/V tickets' citation, for a measurement that is RYA-559's. Value-less rows now carry the verdict's OWN stated cause (attributed, never invented); `owed`-tier rows that HAVE a verdict value say so (`A(X) … HELD at tier 'owed' (RYA-522), not frozen`) instead of reading as never-measured. **Ba sweep: Ba is the only phantom** — Mg/Y/Zr/Eu keep the claim honestly (0 survivors, no value, RYA-596-verified). **NOTHING IS FROZEN:** the corrected artifact is `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv` + per-cell diff `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` (**29 cells differ vs frozen v2** — because the builder is verdict-sourced by design, it necessarily carries the post-v2 landings: Fe 7.516→**7.466** (RYA-553), Mn 5.47→**5.466** (RYA-546), Co CURATION-OWED→**PASS** 4.965 (RYA-564), S→synthesis **7.486** (RYA-492), N wired **8.188** (RYA-556), Ba **2.410** (RYA-559)). That is gold-v3 content and its freeze is **RYA-527's gate, Ryan's call — deliberately NOT performed here.** ⚠️ Carried forward for the freeze: the candidate's Co reads **4.965** (the live verdict), while RYA-643's corrected re-run gives **4.960** — the pre-existing two-artifact split, not introduced here. RYA-632's guard confirms the correction: **15 reds → 14**, Ba cleared, with the corrected candidate substituted for the immutable frozen v2. Prior — **RYA-313/314 — the merge gate is real for the first time.** CI had **never run** in this repo's history; `main` was PR-gated but with no status check to gate on, so "merge only on green" was unenforceable and merges leaned on `--admin`. Three things landed. **(1) The suite is green** — main was **14 red**; 12 were stale expectations asserting a *closed* defect still exists (RYA-545 Ti→Mallinson 3 lines/+0.052 and its retired 55 Cnc clamp; RYA-540 Cu registered; RYA-526/556 N wired; RYA-522 gold CURRENT→v2), and **2 were a real bug** — RYA-506's iSpec/MOOG scratch-dir `makedirs` was added to one call site while three others passed the same literal path uncreated, now single-sourced as `ensure_ispec_tmp_dir()`. **(2) A latent defect CI itself surfaced: `np.trapz` was REMOVED in NumPy 2.0**, and RYA-517 declares the reference stack as py3.12+numpy 2.2 — so all **12** EW-integration call sites were **already broken on the production runner**, invisible locally because the Mac dev env pins numpy 1.26. Fixed once in `pipeline/_numcompat.py`. Suite now green on **both** stacks (py3.9/numpy1.26 and py3.12/numpy2.5) at 1116 passed / 9 skipped / 1 xfailed. **(3) CI runs on a Sirius self-hosted runner**, not `ubuntu-latest` — 24 of 118 test files import iSpec and cannot be collected on an ephemeral runner (measured: 24 collection errors, 868/1116), which would be a green check hiding 22% of the suite. Sirius has the engines and is the authoritative runner, so this honours RYA-567. Ruleset now requires **`CI / test`**; merge style ratified as **merge-commit** and squash/rebase disabled; the redundant admin-bypass ruleset retired. ⚠️ Repo is PUBLIC → fork-PR runs require approval for all external contributors. Also corrected a **two-month-stale register note**: the RYA-530 "Cu grid NOT committed / NOT wired" line was superseded by RYA-540 and is now fixed — the CI suite is what caught it. Prior — RYA-659 **register re-synced to the RYA-556..652 backlog + a freshness guard so it cannot drift silently again**. The register had gone **11 state-changing tickets stale** — last touched 2026-07-17 (`08265f9`, RYA-553) while `data/audit/element_status_tracker.csv` moved to 2026-07-27 (`97485e9`, RYA-643); RYA-643 changed element state and did **not** bump the register, which is the exact hole "a gate cannot sign off while its rows are stale" exists to close. Synced from the LIVE artifacts (not from memory): **solar 27-el verdict 5/1/20/0 → 6 PASS / 0 NLTE-OWED / 20 CURATION-OWED / 0 DATA-GAP**, PASS = O, C, Fe, Mn, K, **Co** (RYA-564 promoted Co on red HFS lines; RYA-556 took N off NLTE-OWED) — the row's **STALE** flag is cleared for these landings but the re-freeze on the two-engine floor remains **RYA-527**. Post-v22 measured-but-unfrozen landings recorded: **S 7.486** (RYA-557), **Ba 2.410** (RYA-559, +0.15 blend-inflated — deblend owed), **Co 4.965** (RYA-564; RYA-643's corrected re-run gives **4.960**, not yet in the verdict), **Mn 5.466** (RYA-546). **Sr II** RYA-551 superseded by RYA-643: 4077 **2.769 → 2.759**, worst reliable \|Δ\| 0.031 ≤ 0.05 → CONFIRMED-ROBUST and **cleared for RYA-527 adoption — which is NOT the same as adopted** (the live verdict still reads the Sr I EW leg 4.961; near-UV fit quality red-χ² 78 (4077) / 180 (4215) vs Mg 1.4 is a SEPARATE open concern). New **owed-HELD vs owed-BLANK** annotation so "owed" stops reading as "undone" (RYA-596). New machinery registered: RYA-558/561/563/567/279/280/594/596/631/642/652. Durable additions: **`LEDGERS.md`** (repo-root startup index naming the canonical read-set) + **`scripts/check_register_freshness.py`** (loud-fails when a state surface is newer than this register). NO measured value, verdict, gold reference, or `constants.py`/`STAR_PARAMS` was modified by this ticket. Prior — RYA-553 **solar Fe 1D→3D correction APPLIED** — the tabulated Magic-2013 (Stagger 3D, A&A 557 A26) 1D→3D solar Fe offset is now applied to the REPORTED Fe I anchor: A(Fe I) **7.516 (1D-NLTE) → 7.466 (3D-NLTE)**, on the true 3D scale (δ vs Asplund 7.46 = +0.006, was +0.056). Magnitude single-sourced from `FE_1D3D_SOLAR_OFFSET` via `CORRECTIONS_3D['Fe_1D3D_solar_dex'] = -0.05` (no duplicated literal). Applied at the reported-value layer (phase_c verdict → gold → gate), additive after NLTE, **idempotent on `method_scale`** (skips an already-3D anchor, so a re-frozen v3 gold is never double-corrected), logged + pre/post recorded. **`FE_GATE [7.41,7.51]` restored as the real solar reported-value gate**; the scale-aware diagnostic band `[7.44,7.58]` (`FE_ABS_DIAG_HALFWIDTH`) is retired *as the solar gate* but retained for the un-corrected 1D-NLTE measurement layer + off-solar stars (per-Teff/[Fe/H] 3D owed → **RYA-550**). RYA-166 gate A1 rewritten (3D-corrected A(Fe I) ∈ FE_GATE) + guards the correction is applied; all 8 assertions green (uncorrected 7.516 and gross +0.3 both FAIL → gate keeps teeth). Fe II NOT shifted (ionization arbiter untouched). SOLAR ONLY. **Unblocks the RYA-527 v3 freeze on the 3D scale.** Files: `config/constants.py`, `scripts/phase_c_verdict_rya371.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`. Prior — RYA-549 **Fe anchor: integrity gap CLOSED + vintage confirmed BENIGN** (the RYA-524 headline, de-escalated). Gate-0 forensic: `Fe_Bergemann_MPIA` is the INTENTIONAL live source (RYA-319 `5d42702`, chosen for full-benchmark-box coverage); the register's "Amarsi-2022-NN" selection row was STALE (the Amarsi MLP is archived — its Teff≤6500/[Fe/H]≤0 ceiling can't reach Procyon 6554/55 Cnc +0.32). Reconciled the register NLTE-grids Fe row → MPIA (the wiring matrix already matched the code → **code=register=matrix**). VINTAGE: the MPIA Fe NLTE δ is TINY — **+0.010 solar / +0.004 at 55 Cnc** (median over 252 Fe I lines), ionization-balance-gated (RYA-406 Fe I−Fe II ≈0 PASS) → **NOT the Ti/Mn/Cr inflated-over-ionization scaled-Drawin class**; the +0.056 vs Asplund 7.46 is the documented 1D-NLTE-vs-3D scale offset (RYA-336), not the NLTE correction. 55 Cnc residual-risk (param-dependent non-cancelling part) = **−0.006 dex, negligible**. Ab-initio migration STOPPED (no banked Fe grid covers metal-rich; the stake is negligible) → deferred **RYA-550**. **Unblocks RYA-527** (Fe confirmed correct as-is; safe to re-freeze). NO code change (Fe was already correct — RYA-319). Prior — RYA-545 **Ti I wired onto the Mallinson-2024 ab-initio NLTE grid** — the STOP→GO corroboration-accept (Ryan 2026-07-13). Replaced the outdated `Ti_Bergemann2011_MPIA.csv` (Bergemann-2011 **scaled-Drawin** +0.108, inflated ~2×) with `Ti_Mallinson2024_PySME.csv` (Zenodo 10753497, ab-initio **Grumer-Barklem-2020** H collisions) as the production Ti Engine-A source; derived the multi-node grid via PySME/MARCS (Ti I 5689/5648/5662, solar δ **+0.0506** = per-line +0.050/054/051, reproduces the published +0.052; physical Teff/logg trend, hull 2500–8000 K → the old 6500 K wall is gone). **Ti CHECK → registered** — certified by the new **corroboration-accept** standing rule (`docs/SCIENCE_STANDARDS.md`, five bounded criteria, **Ti = first invocation**), NOT the ionization-balance gate (which STOPPED on *precision* — SEM ~0.12–0.16 on the thin solar Ti pools, a documented diagnostic, not a value defect; RYA-544/545). RYA-534 Ti xfail **reason updated (RCA closed)** — NOT flipped to a fake PASS: that gate exercises the **Engine-B TS-Gerber** deck, which still ships the outdated `atom.ti503b` → +0.22 (superseded, RYA-546), so it stays honest strict-xfail until the Engine-B Ti atom is swapped (follow-on **RYA-548**); Ti's PRODUCTION NLTE is now the Engine-A Mallinson wire. Closes RYA-535/542. v1 frozen A(Ti) 5.471 stays (this sets the forward/v2 correction only). Files: `pipeline/pysme_nlte.py` (permanent Ti entry), `config/constants.py`, `data/nlte_grids/Ti_Mallinson2024_PySME.csv`(+prov), `docs/SCIENCE_STANDARDS.md`, `scripts/rya545_derive_ti_grid.py`, `tests/test_ts_gerber_rollout_rya534.py`. Prior — RYA-546 **Mn NLTE re-based onto the ab-initio triplet-exact δ** — solar Mn's correction moved from the MPIA/Bergemann **scaled-Drawin** +0.107 (high-EP reference lines) to the **live Amarsi-2020 GALAH** ab-initio HFS-resolved δ **+0.024** on the measured Den Hartog triplet 6013/16/21 (HFS identical on both legs; corroborated by the RYA-534 Engine-B TS-Gerber +0.043). This **REVERSES the RYA-411 "keep MPIA +0.107" decision** — RYA-411 trusted the "dedicated" B&Gehren-2008 atom, but RYA-546's H-collision **vintage audit** showed that atom IS the outdated scaled-Drawin recipe (the Ti defect, RYA-542/544). **A(Mn)☉ = 5.466 (+0.046 vs Asplund 5.42) → PASS** (was 5.554 on the +0.107 mismatch); **RYA-411 closed**. Landed the hard in-hull guard `pysme_nlte.assert_in_grid_hull` (no silent grid extrapolation). Gold Mn 5.554→5.466 folds at the RYA-527 v3 re-freeze; EW-path grid repoint = RYA-527 fan-out. Prior — RYA-525 **two-engine floor BUILT** — the per-line reference-blind selector + inverse-variance element aggregation + `CROSS_ENGINE_MIX_GATE` + loud-fail guards + tuning firewall are wired (`pipeline/engine_selection.py`, `config.TWO_ENGINE`, `docs/SCIENCE_STANDARDS.md`); the silent-LTE swallow in `abundances_derive.run()` is removed; the RYA-526 two-engine ledger is the guard's exception list; full 27-element both-engine RUN gated on RYA-526 grid coverage → RYA-527. Prior — RYA-530 capability-sweep reconciliation — registered ~14 BUILT-but-unregistered capabilities the ledger had missed [single-source gf architecture; NLTE grids Al/K/S/Sr; HFS-resolved synthesis; EW-verification layer; loader frame/OBJECT contract; multi-arm CNO arm-registry; reflected-solar RV; UV conditioning; IR-native α Cen star-ID; multi-instrument loaders; molecular converter; vac↔air SSOT; 1D→3D applied-delta], corrected the Na/Mg/Si NLTE source drift (was mis-cited Lind/MPIA → Amarsi-2020 PySME, RYA-410), and flagged the [O I] 6300 continuum-renorm dead-end so it isn't rebuilt. Verified against origin/main + code+git evidence; map + edits at `data/audit/rya530_capability_sweep/`. Prior — RYA-535: reverted the premature Ti 'model-atom difference' finding to CHECK + opened the RCA (same Bergemann-2011 atom both engines — refuted by Gerber 2023 Table 1); ruled out unit-clamp + grid-placeholder, departures physical, narrowed to MAFAGS-OS-vs-MARCS atmosphere / downstream (findings: data/audit/rya535_ti_rca/). Prior RYA-534: Family-A TS-Gerber NLTE rollout — **Family-A rollout: 10/11 clean, Ti CHECK (RCA open RYA-535)** — 10 PASS (Na/O/Mg/Ca/Mn/Sr/Ba/Ni/Co/Si); Ti = CHECK, an UNRESOLVED same-atom systematic (+0.221 vs +0.107, Engine-A/B both Bergemann-2011 atom) — RYA-535 reverted the premature 'model-atom' finding, RCA in progress — driver config corrected for all (Sr/Ba ionised-species lookup, Co/Ni GES level-IDs, Si/Ti large-grid disk-contention). Element-general deck `scripts/ts_gerber_gate.py`. Prior — RYA-533: TS-native Gerber NLTE deck forensics = NOT-BUILT on all 5 channels (test-tell/git/tree/grids/registration; RYA-361 scoped it, never executed). Built + Na-VALIDATED on Sirius: Turbospectrum_NLTE BSYN v20.1 compiled + Na Gerber-2023 (A&A 669 A43) grid/atom/aux provisioned (md5-pinned, Sirius-only) + bsyn NLTE deck; Na gate GREEN (median δ −0.068 vs −0.107, PASS, validate-don't-tune). Engine-B TS-native NLTE now SETTLED (Na); per-element rollout mechanical. Resolved 2022-vs-2023: arXiv 2206.00967 = A&A 669 A43. Prior context from RYA-531: corrected the RYA-529 PySME-wholesale row to the two-family NLTE-derivation map + the Engine-A/Engine-B distinction. FINDING: production NLTE is universally a 1D delta CSV (Engine A, multi-source: PySME-derived / MPIA / Korotin / INSPECT / nlte_cno); Engine B (TS synthesis) is LTE-only for all 27 — the Gerber-2022 TS-native NLTE deck is NOT built / NOT provisioned (empty gerber_ts on Sirius). RYA-402 Family-A TS-native was a deliberately-abandoned route, not a re-provision → Engine-B NLTE OWED for RYA-525; per-element map at data/audit/rya531_engineB_map/)._
**Version: v29** · _Last updated: 2026-08-08 · By: Mr. Code (**RYA-673 — the two-engine floor has been a PARTIAL-wiring floor, and nothing measured it until now.** RYA-669 found `_dedicated_engine_B()` never wired Co/Ba; this audit asked the question across the whole canonical set by calling the orchestrator's OWN coverage functions over real solar data. **27 species: `both` 10 · `A_only` 0 · `B_only` 7 · `neither` 10.** **Only ONE of the six PASS elements (Fe) is confirmed on two engines** — C/O/Mn are `B_only`, and **K I (gold-tier PASS 5.099) and Co I are wired to NEITHER engine**. **Six synthesis-required species have no Engine B at all** (N/Co/Ba/Sc/Eu/Zr): their raw-EW leg is deliberately suppressed by RYA-520, so Engine B is not a second opinion, it is their ONLY leg. **The RYA-525 loud-fail cannot catch this**: it iterates the UNION of the three coverage sources, so a species absent from all three never enters the loop and is skipped in silence — the guard catches a partially covered species and is blind to a completely uncovered one. **`neither` splits in two and the halves need opposite responses:** P/N/Co/K/Ba/Sc are MEASURED off-orchestrator (chiefly the RYA-460 Kitt Peak channel `phase_c` reads directly) and therefore carry a real value with NO cross-engine confirmation and no way to acquire one until wired; Al/Y/Eu/Zr are genuinely unmeasured. Cheapest class is `NO_HARNESS_INVOCATION` — **Co, Ba and S already have ratified synthesis results committed in the repo that the orchestrator simply never reads**. New `two_engine_wiring_status` column on the tracker (RYA-654 generator) so a one-engine PASS stops looking like a two-engine one. **DISCOVERY ONLY — no wiring changed, no engine touched, no verdict regenerated, no ticket filed** (Ryan directs which). Prior — **RYA-665 — gold v3 is FROZEN: the pre-527 mini-freeze.** `data/reference/solar/CURRENT` moves **v2 → v3**, promoting the RYA-653 corrected candidate through `scripts/promote_solar_reference.py` (RYA-469 write-once + hash manifest; no hand-edit path). **Exactly two frozen VALUES change** — **Fe I 7.516→7.466** (RYA-553 tabulated Magic-2013 1D→3D) and **Mn I 5.47→5.466** (RYA-546 ab-initio, reversing RYA-411's keep-MPIA) — plus two verdicts (**Co I → PASS**, RYA-564; **N I NLTE-OWED → CURATION-OWED**, RYA-556) and the RYA-653/596 note-honesty corrections on Ba/Ca/Ti/Ni/Na/Al/Sr. **Disputed values are NOT immortalised:** the RYA-522 `owed` tier freezes no value, so Co 4.965, S 7.486, Ba 2.410, N 8.188 and Sr all land as visible-but-HELD rows, and RYA-581's Ba deblend stays open post-freeze. Verified 29/29 cells against `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` before applying. Ledger guard **3 → 2** failures (Ba's phantom red cleared, 0 undocumented); its Ba `_OWED_NOT_LAUNDERED` entry and the four Class-B gold-lag divergences for Co/N were **deleted per their own retire-on-freeze instructions** — a stale ratified divergence is a hiding place for the next real contradiction. **NOT the two-engine re-emit:** RYA-527 Phase 2 still owes a fresh run on current main, which validates v3 or produces v4 (Ca's provisional flip, Na pending RYA-664, Sr II 2.759 adoption, Co 4.965-vs-4.960). Prior — **RYA-654 — the element status tracker is GENERATED, and the physics_regime GET-DATA-vs-measured class is adjudicated on the EW-vs-synthesis axis.** The tracker had **no generator** (RYA-632 audit): hand-edited, still carrying Co's DEMOTED blue-edge +1.188 and N's already-cleared NLTE debt. It is now built by `scripts/generate_element_status_tracker_rya654.py` — RYA-436 Move B for the results side — with status columns sourced from the **phase_c verdict channel (ratified canonical)**, `tier` from the RYA-522 ratified freeze tiers, `ion`/`regime_verdict` from `config/physics_regime_rya400.yaml`, and the analyst columns from a new hand-authored sidecar `data/audit/element_status_tracker_editorial.yaml`. `--check` diffs committed-vs-regenerated so **hand-editing the CSV is a build break**; the generator **refuses to run off an uncommitted phase_c**, so a Mac-canary re-run cannot reach the ledger. A new explicit **`verdict` column** ends the tier-vs-verdict vocabulary mixing the guard used to infer through. **The six divergent GET-DATA rows re-cut on the EW-measurable-vs-synthesis-sourced axis, using live EW-pool line counts, not documentation state:** **Co/N/K/Cu = verifier-blind on a REAL measurement** (0/0/0/2-but-junk EW-pool lines; measured on the synthesis or Kitt Peak atlas leg) → **RATIFIED as ledger divergences**, verdicts unchanged, no promotion; **P = genuinely stale** — it is the one member with real EW-pool lines (P I 6034.04/6043.12) and its GET-DATA rested on "FUV needs HST/STIS (RYA-119)", a premise phase_c records as **superseded for the Sun** → row **FIXED**, `GET-DATA` → `HARD-carry-forward`; **Sc = measurement-not-trusted** (its only value is the KP Sc II 4246 blue-edge HFS single line, held LOW_CONFIDENCE by phase_c itself) → **NOT annotated, deliberately still RED**. Both RYA-632 count mismatches cleared as a by-product (**PASS 5→6** Co, **NLTE_OWED 1→0** N); the guard now reports **0 undocumented failures** and 2 documented-owed elements (Sc above; **Ba**, whose gold v2 phantom is corrected in the RYA-653 candidate and lands at the RYA-527 v3 re-freeze). **NO measured value, verdict, gold vN or `constants.py`/`STAR_PARAMS` was modified.** Prior — **RYA-653 — the blank-cause honesty tripwire is now SHARED, and gold v2's Ba phantom is corrected in a candidate.** RYA-596's `_assert_blank_cause_is_honest` was phase_c-only; the identical unchecked fallthrough sat one file over in `scripts/build_solar_reference_v2_rya522.py::_scale_and_note` and was actively lying in frozen gold v2's **Ba** row ("no independent-gf line survives the graded cull" against a Ba that RYA-559 **measured** at 2.410). Extracted to **`pipeline/provenance_honesty.py`** — one module, imported by BOTH emitters, no forked copy — and strengthened: the zero-survivor claim is now refused on any row with `n_lines > 0` **or** a measured value. **The gap was wider than the ticket predicted:** rebuilt against today's verdict the OLD builder would have fabricated the same phantom on **five more rows** — Ca/Ti/Ni/Na/Al, carrying 2/10/2/2/1 graded survivors, i.e. RYA-596's exact five re-entering through the other file — and would have **crashed outright on Sr** (`TypeError`, `d=None`); it also stamped Ba with `HFS-resolved synthesis (RYA-411/466/473)`, the Mn/Cu/V tickets' citation, for a measurement that is RYA-559's. Value-less rows now carry the verdict's OWN stated cause (attributed, never invented); `owed`-tier rows that HAVE a verdict value say so (`A(X) … HELD at tier 'owed' (RYA-522), not frozen`) instead of reading as never-measured. **Ba sweep: Ba is the only phantom** — Mg/Y/Zr/Eu keep the claim honestly (0 survivors, no value, RYA-596-verified). **NOTHING IS FROZEN:** the corrected artifact is `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv` + per-cell diff `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` (**29 cells differ vs frozen v2** — because the builder is verdict-sourced by design, it necessarily carries the post-v2 landings: Fe 7.516→**7.466** (RYA-553), Mn 5.47→**5.466** (RYA-546), Co CURATION-OWED→**PASS** 4.965 (RYA-564), S→synthesis **7.486** (RYA-492), N wired **8.188** (RYA-556), Ba **2.410** (RYA-559)). That is gold-v3 content and its freeze is **RYA-527's gate, Ryan's call — deliberately NOT performed here.** ⚠️ Carried forward for the freeze: the candidate's Co reads **4.965** (the live verdict), while RYA-643's corrected re-run gives **4.960** — the pre-existing two-artifact split, not introduced here. RYA-632's guard confirms the correction: **15 reds → 14**, Ba cleared, with the corrected candidate substituted for the immutable frozen v2. Prior — **RYA-313/314 — the merge gate is real for the first time.** CI had **never run** in this repo's history; `main` was PR-gated but with no status check to gate on, so "merge only on green" was unenforceable and merges leaned on `--admin`. Three things landed. **(1) The suite is green** — main was **14 red**; 12 were stale expectations asserting a *closed* defect still exists (RYA-545 Ti→Mallinson 3 lines/+0.052 and its retired 55 Cnc clamp; RYA-540 Cu registered; RYA-526/556 N wired; RYA-522 gold CURRENT→v2), and **2 were a real bug** — RYA-506's iSpec/MOOG scratch-dir `makedirs` was added to one call site while three others passed the same literal path uncreated, now single-sourced as `ensure_ispec_tmp_dir()`. **(2) A latent defect CI itself surfaced: `np.trapz` was REMOVED in NumPy 2.0**, and RYA-517 declares the reference stack as py3.12+numpy 2.2 — so all **12** EW-integration call sites were **already broken on the production runner**, invisible locally because the Mac dev env pins numpy 1.26. Fixed once in `pipeline/_numcompat.py`. Suite now green on **both** stacks (py3.9/numpy1.26 and py3.12/numpy2.5) at 1116 passed / 9 skipped / 1 xfailed. **(3) CI runs on a Sirius self-hosted runner**, not `ubuntu-latest` — 24 of 118 test files import iSpec and cannot be collected on an ephemeral runner (measured: 24 collection errors, 868/1116), which would be a green check hiding 22% of the suite. Sirius has the engines and is the authoritative runner, so this honours RYA-567. Ruleset now requires **`CI / test`**; merge style ratified as **merge-commit** and squash/rebase disabled; the redundant admin-bypass ruleset retired. ⚠️ Repo is PUBLIC → fork-PR runs require approval for all external contributors. Also corrected a **two-month-stale register note**: the RYA-530 "Cu grid NOT committed / NOT wired" line was superseded by RYA-540 and is now fixed — the CI suite is what caught it. Prior — RYA-659 **register re-synced to the RYA-556..652 backlog + a freshness guard so it cannot drift silently again**. The register had gone **11 state-changing tickets stale** — last touched 2026-07-17 (`08265f9`, RYA-553) while `data/audit/element_status_tracker.csv` moved to 2026-07-27 (`97485e9`, RYA-643); RYA-643 changed element state and did **not** bump the register, which is the exact hole "a gate cannot sign off while its rows are stale" exists to close. Synced from the LIVE artifacts (not from memory): **solar 27-el verdict 5/1/20/0 → 6 PASS / 0 NLTE-OWED / 20 CURATION-OWED / 0 DATA-GAP**, PASS = O, C, Fe, Mn, K, **Co** (RYA-564 promoted Co on red HFS lines; RYA-556 took N off NLTE-OWED) — the row's **STALE** flag is cleared for these landings but the re-freeze on the two-engine floor remains **RYA-527**. Post-v22 measured-but-unfrozen landings recorded: **S 7.486** (RYA-557), **Ba 2.410** (RYA-559, +0.15 blend-inflated — deblend owed), **Co 4.965** (RYA-564; RYA-643's corrected re-run gives **4.960**, not yet in the verdict), **Mn 5.466** (RYA-546). **Sr II** RYA-551 superseded by RYA-643: 4077 **2.769 → 2.759**, worst reliable \|Δ\| 0.031 ≤ 0.05 → CONFIRMED-ROBUST and **cleared for RYA-527 adoption — which is NOT the same as adopted** (the live verdict still reads the Sr I EW leg 4.961; near-UV fit quality red-χ² 78 (4077) / 180 (4215) vs Mg 1.4 is a SEPARATE open concern). New **owed-HELD vs owed-BLANK** annotation so "owed" stops reading as "undone" (RYA-596). New machinery registered: RYA-558/561/563/567/279/280/594/596/631/642/652. Durable additions: **`LEDGERS.md`** (repo-root startup index naming the canonical read-set) + **`scripts/check_register_freshness.py`** (loud-fails when a state surface is newer than this register). NO measured value, verdict, gold reference, or `constants.py`/`STAR_PARAMS` was modified by this ticket. Prior — RYA-553 **solar Fe 1D→3D correction APPLIED** — the tabulated Magic-2013 (Stagger 3D, A&A 557 A26) 1D→3D solar Fe offset is now applied to the REPORTED Fe I anchor: A(Fe I) **7.516 (1D-NLTE) → 7.466 (3D-NLTE)**, on the true 3D scale (δ vs Asplund 7.46 = +0.006, was +0.056). Magnitude single-sourced from `FE_1D3D_SOLAR_OFFSET` via `CORRECTIONS_3D['Fe_1D3D_solar_dex'] = -0.05` (no duplicated literal). Applied at the reported-value layer (phase_c verdict → gold → gate), additive after NLTE, **idempotent on `method_scale`** (skips an already-3D anchor, so a re-frozen v3 gold is never double-corrected), logged + pre/post recorded. **`FE_GATE [7.41,7.51]` restored as the real solar reported-value gate**; the scale-aware diagnostic band `[7.44,7.58]` (`FE_ABS_DIAG_HALFWIDTH`) is retired *as the solar gate* but retained for the un-corrected 1D-NLTE measurement layer + off-solar stars (per-Teff/[Fe/H] 3D owed → **RYA-550**). RYA-166 gate A1 rewritten (3D-corrected A(Fe I) ∈ FE_GATE) + guards the correction is applied; all 8 assertions green (uncorrected 7.516 and gross +0.3 both FAIL → gate keeps teeth). Fe II NOT shifted (ionization arbiter untouched). SOLAR ONLY. **Unblocks the RYA-527 v3 freeze on the 3D scale.** Files: `config/constants.py`, `scripts/phase_c_verdict_rya371.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`. Prior — RYA-549 **Fe anchor: integrity gap CLOSED + vintage confirmed BENIGN** (the RYA-524 headline, de-escalated). Gate-0 forensic: `Fe_Bergemann_MPIA` is the INTENTIONAL live source (RYA-319 `5d42702`, chosen for full-benchmark-box coverage); the register's "Amarsi-2022-NN" selection row was STALE (the Amarsi MLP is archived — its Teff≤6500/[Fe/H]≤0 ceiling can't reach Procyon 6554/55 Cnc +0.32). Reconciled the register NLTE-grids Fe row → MPIA (the wiring matrix already matched the code → **code=register=matrix**). VINTAGE: the MPIA Fe NLTE δ is TINY — **+0.010 solar / +0.004 at 55 Cnc** (median over 252 Fe I lines), ionization-balance-gated (RYA-406 Fe I−Fe II ≈0 PASS) → **NOT the Ti/Mn/Cr inflated-over-ionization scaled-Drawin class**; the +0.056 vs Asplund 7.46 is the documented 1D-NLTE-vs-3D scale offset (RYA-336), not the NLTE correction. 55 Cnc residual-risk (param-dependent non-cancelling part) = **−0.006 dex, negligible**. Ab-initio migration STOPPED (no banked Fe grid covers metal-rich; the stake is negligible) → deferred **RYA-550**. **Unblocks RYA-527** (Fe confirmed correct as-is; safe to re-freeze). NO code change (Fe was already correct — RYA-319). Prior — RYA-545 **Ti I wired onto the Mallinson-2024 ab-initio NLTE grid** — the STOP→GO corroboration-accept (Ryan 2026-07-13). Replaced the outdated `Ti_Bergemann2011_MPIA.csv` (Bergemann-2011 **scaled-Drawin** +0.108, inflated ~2×) with `Ti_Mallinson2024_PySME.csv` (Zenodo 10753497, ab-initio **Grumer-Barklem-2020** H collisions) as the production Ti Engine-A source; derived the multi-node grid via PySME/MARCS (Ti I 5689/5648/5662, solar δ **+0.0506** = per-line +0.050/054/051, reproduces the published +0.052; physical Teff/logg trend, hull 2500–8000 K → the old 6500 K wall is gone). **Ti CHECK → registered** — certified by the new **corroboration-accept** standing rule (`docs/SCIENCE_STANDARDS.md`, five bounded criteria, **Ti = first invocation**), NOT the ionization-balance gate (which STOPPED on *precision* — SEM ~0.12–0.16 on the thin solar Ti pools, a documented diagnostic, not a value defect; RYA-544/545). RYA-534 Ti xfail **reason updated (RCA closed)** — NOT flipped to a fake PASS: that gate exercises the **Engine-B TS-Gerber** deck, which still ships the outdated `atom.ti503b` → +0.22 (superseded, RYA-546), so it stays honest strict-xfail until the Engine-B Ti atom is swapped (follow-on **RYA-548**); Ti's PRODUCTION NLTE is now the Engine-A Mallinson wire. Closes RYA-535/542. v1 frozen A(Ti) 5.471 stays (this sets the forward/v2 correction only). Files: `pipeline/pysme_nlte.py` (permanent Ti entry), `config/constants.py`, `data/nlte_grids/Ti_Mallinson2024_PySME.csv`(+prov), `docs/SCIENCE_STANDARDS.md`, `scripts/rya545_derive_ti_grid.py`, `tests/test_ts_gerber_rollout_rya534.py`. Prior — RYA-546 **Mn NLTE re-based onto the ab-initio triplet-exact δ** — solar Mn's correction moved from the MPIA/Bergemann **scaled-Drawin** +0.107 (high-EP reference lines) to the **live Amarsi-2020 GALAH** ab-initio HFS-resolved δ **+0.024** on the measured Den Hartog triplet 6013/16/21 (HFS identical on both legs; corroborated by the RYA-534 Engine-B TS-Gerber +0.043). This **REVERSES the RYA-411 "keep MPIA +0.107" decision** — RYA-411 trusted the "dedicated" B&Gehren-2008 atom, but RYA-546's H-collision **vintage audit** showed that atom IS the outdated scaled-Drawin recipe (the Ti defect, RYA-542/544). **A(Mn)☉ = 5.466 (+0.046 vs Asplund 5.42) → PASS** (was 5.554 on the +0.107 mismatch); **RYA-411 closed**. Landed the hard in-hull guard `pysme_nlte.assert_in_grid_hull` (no silent grid extrapolation). Gold Mn 5.554→5.466 folds at the RYA-527 v3 re-freeze; EW-path grid repoint = RYA-527 fan-out. Prior — RYA-525 **two-engine floor BUILT** — the per-line reference-blind selector + inverse-variance element aggregation + `CROSS_ENGINE_MIX_GATE` + loud-fail guards + tuning firewall are wired (`pipeline/engine_selection.py`, `config.TWO_ENGINE`, `docs/SCIENCE_STANDARDS.md`); the silent-LTE swallow in `abundances_derive.run()` is removed; the RYA-526 two-engine ledger is the guard's exception list; full 27-element both-engine RUN gated on RYA-526 grid coverage → RYA-527. Prior — RYA-530 capability-sweep reconciliation — registered ~14 BUILT-but-unregistered capabilities the ledger had missed [single-source gf architecture; NLTE grids Al/K/S/Sr; HFS-resolved synthesis; EW-verification layer; loader frame/OBJECT contract; multi-arm CNO arm-registry; reflected-solar RV; UV conditioning; IR-native α Cen star-ID; multi-instrument loaders; molecular converter; vac↔air SSOT; 1D→3D applied-delta], corrected the Na/Mg/Si NLTE source drift (was mis-cited Lind/MPIA → Amarsi-2020 PySME, RYA-410), and flagged the [O I] 6300 continuum-renorm dead-end so it isn't rebuilt. Verified against origin/main + code+git evidence; map + edits at `data/audit/rya530_capability_sweep/`. Prior — RYA-535: reverted the premature Ti 'model-atom difference' finding to CHECK + opened the RCA (same Bergemann-2011 atom both engines — refuted by Gerber 2023 Table 1); ruled out unit-clamp + grid-placeholder, departures physical, narrowed to MAFAGS-OS-vs-MARCS atmosphere / downstream (findings: data/audit/rya535_ti_rca/). Prior RYA-534: Family-A TS-Gerber NLTE rollout — **Family-A rollout: 10/11 clean, Ti CHECK (RCA open RYA-535)** — 10 PASS (Na/O/Mg/Ca/Mn/Sr/Ba/Ni/Co/Si); Ti = CHECK, an UNRESOLVED same-atom systematic (+0.221 vs +0.107, Engine-A/B both Bergemann-2011 atom) — RYA-535 reverted the premature 'model-atom' finding, RCA in progress — driver config corrected for all (Sr/Ba ionised-species lookup, Co/Ni GES level-IDs, Si/Ti large-grid disk-contention). Element-general deck `scripts/ts_gerber_gate.py`. Prior — RYA-533: TS-native Gerber NLTE deck forensics = NOT-BUILT on all 5 channels (test-tell/git/tree/grids/registration; RYA-361 scoped it, never executed). Built + Na-VALIDATED on Sirius: Turbospectrum_NLTE BSYN v20.1 compiled + Na Gerber-2023 (A&A 669 A43) grid/atom/aux provisioned (md5-pinned, Sirius-only) + bsyn NLTE deck; Na gate GREEN (median δ −0.068 vs −0.107, PASS, validate-don't-tune). Engine-B TS-native NLTE now SETTLED (Na); per-element rollout mechanical. Resolved 2022-vs-2023: arXiv 2206.00967 = A&A 669 A43. Prior context from RYA-531: corrected the RYA-529 PySME-wholesale row to the two-family NLTE-derivation map + the Engine-A/Engine-B distinction. FINDING: production NLTE is universally a 1D delta CSV (Engine A, multi-source: PySME-derived / MPIA / Korotin / INSPECT / nlte_cno); Engine B (TS synthesis) is LTE-only for all 27 — the Gerber-2022 TS-native NLTE deck is NOT built / NOT provisioned (empty gerber_ts on Sirius). RYA-402 Family-A TS-native was a deliberately-abandoned route, not a re-provision → Engine-B NLTE OWED for RYA-525; per-element map at data/audit/rya531_engineB_map/)._

---

## Targets & stellar parameters — MIRROR ← config/constants.py

**Authoritative source is `config/constants.py` → `STAR_PARAMS` (loaded from `config/stars.yaml`, RYA-298). The table below is a script-generated view — do NOT hand-edit it; run `python scripts/gen_state_register_targets.py --write` to refresh, `--check` to detect drift.**

NATIVE caveats on the mirrored values (these are register annotations, not source data):
- **Sun Teff = 5772 K** is authoritative (`stars.yaml`, GBS Heiter+2015). The old `5777` (legacy `STAR_SOLAR`) and `5778` (`ASTRO['Teff_sun']`, a physical constant) were single-sourced away by RYA-298/355 — the seed's "5772 or 5777" discrepancy **resolves to 5772**.
- **Procyon** canonical = GBS (Heiter+2015 6554/4.0, Jofré+2014 [Fe/H]=+0.03, ξ=1.8) — **NOT** the former Allende-Prieto 2002 spectroscopic 6530/3.96, and **NOT** 6804. `feh` is SOLVED, so `feh_ref` is a reference seed only.
- **α Cen A / B [Fe/H] mirror = +0.20**, but this is **PENDING ratification (RYA-435, Backlog)**: `stars.yaml` stores 0.20/0.20 while citing Jofré 2014 Table 3 (NLTE), which actually gives **A=+0.26, B=+0.22** — a citation/value mismatch. The α Cen A abundance run (Aug-2026 deadline, RYA-116) must NOT anchor on the unreconciled value until RYA-435 lands.
- **55 Cnc A** is the LAST target; ξ is SOLVED (init 1.0, x-check 0.85–1.35), not pinned.
- `synthetic_no_logg` is a test fixture (no fundamental logg), not a science target — shown for completeness, flagged.

<!-- BEGIN GENERATED: targets (scripts/gen_state_register_targets.py) -->
<!-- generated from config/constants.py STAR_PARAMS (stars.yaml, __version__=2026-05-28); regenerate with: python scripts/gen_state_register_targets.py --write -->
| Star | Teff (K) | log g | [Fe/H] ref | ξ (km/s) | pin/solve policy | Source (verbatim from stars.yaml) |
|---|---|---|---|---|---|---|
| Sun (Star Zero) | 5772 ± 1 | 4.438 | +0.00 | 1.00 (pinned) | pin: teff,logg,xi · solve: feh | GBS: Heiter+2015 (Teff/logg), Jofré+2014 ([Fe/H], ξ) |
| Procyon | 6554 ± 84 | 4.0 | +0.03 | 1.80 (pinned) | pin: teff,logg,xi · solve: feh | GBS: Heiter+2015 (Teff/logg), Jofré+2014 ([Fe/H], ξ=1.8); broadening: Bruntt+2010 vsini=2.8 + adopted RT vmac=6.0 (Gray scale; FT estimator deferred to RYA-316) |
| α Cen A | 5792 ± 16 | 4.3 | +0.20 | 1.10 (pinned) | pin: teff,logg,xi · solve: feh | GBS: Heiter+2015 (Teff/logg), Jofré+2014 ([Fe/H], ξ); broadening: Bruntt+2010 (MNRAS 405,1907) Table B1 vsini=1.9 + vmac=2.3 (RT, Gray scale) |
| α Cen B | 5231 ± 20 | 4.53 | +0.20 | 1.00 (pinned) | pin: teff,logg,xi · solve: feh | GBS: Heiter+2015 (Teff/logg), Jofré+2014 ([Fe/H], ξ); broadening: Bruntt+2010 (MNRAS 405,1907) Table B1 vsini=1.0 + vmac=0.8 (RT, Gray scale) |
| 55 Cnc A | 5196 ± 24 | 4.45 | +0.31 | 1.00 (solved; x-check 0.85–1.35) | pin: teff,logg · solve: feh,xi | von Braun et al. 2011, ApJ 740, 49; ξ x-check Teske+2013 / Ecuvillon+2004 |
| synthetic_no_logg (test fixture) | 5800 ± 100 | 4.4 | +0.00 | — | pin: — · solve: teff,logg,feh,xi | synthetic test fixture — no fundamental logg available |
<!-- END GENERATED: targets -->

_Target order: Sun → Procyon → α Cen A/B → 55 Cnc A (**55 Cnc LAST**). 27-element canonical list per RYA-109 (`TARGET_ELEMENTS` in constants.py)._

**A(Fe)☉ reference (NATIVE, from constants.py):** `SOLAR_ASPLUND2021['Fe'] = 7.46` (Asplund 2021, 3D-true) is the differential zero-point. The pipeline MEASURES 1D-NLTE **7.516** (~+0.05 above 3D-true); the reported solar anchor now applies the tabulated Magic-2013 1D→3D correction (RYA-553) → **3D-NLTE 7.466** on the true scale, gated on `FE_GATE [7.41,7.51]` — see the Solar Fe I row.

---

## Solar anchor (Star Zero) — NATIVE

| Component | Verdict | Value | Established by | Status | Reopen only if |
|---|---|---|---|---|---|
| Solar Fe I (value) | 3D-NLTE anchor — PASS | **7.466** (3D-corrected; 1D-NLTE measurement 7.516, −0.05 Magic-2013 1D→3D) | RYA-247 (NLTE unit fix), RYA-336 (scale-aware gate), **RYA-553 (3D correction APPLIED)**, **RYA-681 (guard re-keyed on the value)** | SETTLED-WITH-CAVEAT — the value **7.466 is correct** and the correction's idempotency is now keyed on the VALUE, not on a prose label (RYA-681: `pipeline/solar_scale_provenance.py`; `scale_state` column + value cross-check at ±0.025 = \|correction\|/2). `FE_GATE [7.41,7.51]` UNCHANGED — it cannot see a doubled correction (0.05 == its half-width), so RYA-166 A1 gained an orthogonal scale-identity assertion instead (7.416 → FAIL). ⚠️ **CAVEAT: gold v3's Fe `method_scale` still reads `1D-NLTE (Fe I)` beside the 3D value, so `phase_c` LOUD-FAILS and the verdict channel is blocked** — clearing it needs a ratified gold v4 (RYA-669, Ryan's call); gold is write-once (RYA-469) | gold v4 re-freeze to fix the LABEL (RYA-669/RYA-527); off-solar per-Teff/[Fe/H] 3D generalisation (RYA-550) |
| Solar Fe I (scatter) | honest floor, NOT a defect | 0.138 dex (RYA-407 verdict c) | RYA-407 (PR #48, MERGED `220f263`) | SETTLED as honest floor — no cited mechanism reaches 0.10; gate threshold too strict → recalibrated to a G-anchor acceptance profile by RYA-446 (MERGED #71) | principled new vetting genuinely lowers it |
| Solar Fe II (arbiter) | balanced — PASS | synth arbiter ≈7.486–7.500, ΔFe(I−II) −0.007…−0.015 | RYA-305/341 (arbiter ratified), RYA-406 (gate scores it) | SETTLED — the ratified ionization arbiter is **SYNTHESIS** Fe II; on main (RYA-405 confirmed the solved stack is integrated, NOT regressed) | 3D-NLTE, or new synth Fe II (RYA-338 flux-space) |
| Solar Fe II (EW-path) | high BY DESIGN | ~7.700 (blend-limited EW pool) | RYA-352 / RYA-406 | SETTLED as DIAGNOSTIC — demoted to a reported line (not a gate metric); EW-vs-synth ~0.21 = documented blend-bias, within band | — |
| Solar Fe II gate scoring | reconciled to the arbiter | Fe I−Fe II −0.007…−0.015 PASS (cited tolerance) | RYA-406 (PR #47, MERGED `2ecc2a2`) | SETTLED — merged into the validated main | — |
| Solar Fe II 509-report anomaly | 7.657 / n=3 | neither arbiter (7.486) nor EW-path (7.700) | RYA-509 | OPEN — RYA-509 reproduced the banked v1 baseline, which predates the RYA-406 gate reconciliation; per-line provenance owed → RYA-515 | — |
| Solar reproducibility | from-raw deterministic | Δ=0.000 on 13 species; EW md5 `230c27c4` | RYA-509 (Mac leg; pushed, NOT merged) | SETTLED for reproducibility ONLY — **reproducibility ≠ correctness**: it reproduces the *banked v1 baseline* (hence the 509 Fe II anomaly above), it does not re-derive the corrected arbiter | cross-env reproduction (RYA-511) |
| Solar 13-species verdict (v1) | banked baseline | PASS-4 (O, C, Fe, K) | pre-June v1 baseline | STALE — predates the June Fe stack; does not reflect the RYA-406/407 gate state | RYA-251 sign-off supersedes |
| NLTE unit convention | absolute A(Fe); `_A_FE_SOLAR = 7.46` offset | — | RYA-247 fix | SETTLED for Fe; **Ca/Ti/Cr grids not yet verified for the same convention** | verify Ca/Ti/Cr grid units (RYA-245/256) |
| Solar gold reference | **v3 — verdict-sourced, tiered** | frozen-VALUE changes vs v2 are exactly two: **Fe I 7.516→7.466** (RYA-553 1D→3D Magic-2013) and **Mn I 5.47→5.466** (RYA-546 ab-initio); verdicts **Co I → PASS** (RYA-564) and **N I NLTE-OWED → CURATION-OWED** (RYA-556); Ba/Ca/Ti/Ni/Na/Al/Sr note-honesty corrections (RYA-653/596). Tiers unchanged: gold=C/O/K/Mn/Fe/Sc, gf_floor=Cr/Si, upper_limit=Li, **owed(held, no value)**=S/Sr/N/Co/Ti/Ni/Al/Na/Ca/P/Mg/Ba/Y/Zr/Eu/Cu/V | RYA-665 (frozen+hashed from the RYA-653 corrected candidate; RYA-522 tiers; RYA-521 channel) | SETTLED — `CURRENT`→v3 (`data/reference/solar/CURRENT`); v1 **and v2** retained immutable + SUPERSEDED (RYA-469 write-once). ⚠️ **Co is verdict PASS at tier `owed` ⇒ its 4.965 is HELD, NOT frozen** — the tier system, working as ratified | RYA-527 **Phase 2**: the fresh two-engine re-emit on current main, which validates v3 or produces v4 |
| Post-v22 value landings — **v3 froze Mn ONLY; S/Ba/Co remain HELD** | real measurements in the live verdict; **only Mn is now frozen gold** — S/Ba/Co sit at tier `owed`, so v3 records them in the row's note and freezes NO value. Do NOT read S/Ba/Co as gold | **S I 7.486** (+0.366 vs Asplund 7.12, σ 0.045, n=2) · **Ba II 2.237** (−0.033, n=1; was 2.410 pre-deblend) · **Co I 4.965** (+0.025, σ 0.069, n=5) · **Mn I 5.466** (+0.046, σ 0.15, n=3) | S — RYA-557 (RYA-492 Costa-Silva+2020 gf synthesis); Ba — RYA-581 (Ba II 5853 in-window blend fit + Korotin2015 NLTE), superseding RYA-559; Co — RYA-564 (red-line HFS synthesis); Mn — RYA-546 (ab-initio Amarsi re-derivation) | **Mn FROZEN in v3** (tier `gold`, 5.47→5.466, RYA-665). **S/Ba/Co still MEASURED-HELD** — the RYA-522 `owed` tier freezes no value, so the v3 freeze did NOT promote them; each appears in v3 as `A(X) N.NNN HELD at tier 'owed' (RYA-522), not frozen`. Their adoption is an RYA-527 Phase-2 species call, not a freeze mechanic. Per-element caveats that must travel with the numbers: **S** — the +0.366 is a gf-SCALE floor (RYA-161), the CS gf moved it only ~−0.03 (7.516→7.486); confirmed the audit prediction, still CURATION-OWED, do NOT tune. **Ba** — the deblend is **DONE (RYA-581, PR #190)**: the 2.410 came from inverting a `blend_flag=True` pool EW (74.62 mÅ vs the clean ~64 mÅ line), and an EW inversion cannot deblend. An in-window blend fit (full VALD3 block modelled, RYA-551 pattern) gives **A(Ba) 2.237** — verdict **PASS** (|−0.033| ≤ TOL_PASS). Only −0.020 dex of the −0.173 shift is the blend model; −0.153 is abandoning the EW inversion. ⚠️ still **HELD not frozen**: it rests on ONE line, σ 0.016 is inter-arm precision only, and Ba has **no two-engine record** (synthesis route unwired in `_dedicated_engine_B()`) ⇒ gate 3 UNEVALUABLE. Second clean Ba II line (6141.713/6496.897) is the confirm. **Co** — the blue-edge Co I 3845 value (+1.188, SNR~24 / χ²ᵣ~3100) is **RETIRED as untrusted**, demoted to diagnostic-only; ⚠️ **RYA-643's corrected re-run gives A(Co) 4.960, not 4.965** — the verdict artifact predates it by one day and has not adopted it (see the Sr II row: same cleared-≠-adopted class). **Mn** — 5.466 supersedes 5.554 (RYA-546 reversed RYA-411) | the v3 re-freeze lands, or a deblend/re-run moves one |
| Solar 27-el verdict | **6 PASS / 0 NLTE-OWED / 20 CURATION-OWED / 0 DATA-GAP** | PASS: O, C, Fe, Mn, K, **Co** | RYA-519 (wiring) / RYA-371 (phase_c, regenerated 2026-07-27); **RYA-556** (N off NLTE-OWED) / **RYA-564** (Co promoted); per-element detail → RYA-463 registry | CURRENT for the counts (read live from `docs/audit/solar_phase_c_verdict_rya371.md`, gen 2026-07-27) — the v22 "5/1/20/0 STALE" flag is **cleared**: N's NLTE debt is discharged (RYA-556 wired the registered N I grid → NLTE-OWED bucket = 0) and Co promoted on red HFS lines (RYA-564). **The verdict is still NOT the re-freeze** — the two-engine re-run (verdict v-next, gold v3) remains **RYA-527**, THE Beta gate; several measured values below are queued for it and not yet in this verdict | RYA-527 re-freeze lands |
| owed-HELD vs owed-BLANK (**read this before calling any "owed" element undone**) | two different states the single word "owed" was hiding | **owed-HELD** — the graded firewall left survivors and DID produce a value, now held unfrozen by the ratified RYA-522 `owed` tier (which freezes no value BY DESIGN): **Ca 6.324** (3 survivors), **Ti 5.471** (10), **Ni 6.946** (2), **Na 6.264** (2), **Al 7.406** (1) — values live in frozen gold v1. **owed-BLANK** — genuinely zero survivors, claim verified: **Mg** (0/5; 5528-vs-5711 discordant 0.21 dex, RYA-592), **Y** (0/3, data gap), **Zr** (0/6, sub-floor — RYA-560/643), **Eu** (0/1, HFS owed) | **RYA-596** (adjudication + `_assert_blank_cause_is_honest` tripwire) / RYA-522 (tier) | SETTLED as a distinction — RYA-596 killed the phantom "no line survives" cause string that the verdict was asserting *without checking it* on the five HELD rows (the tell: `n_lines` 2/10/2/2/1 sitting next to "no line survives"). **CAVEAT (RYA-596, own ticket owed):** for **Zr and Eu** the blank is **not gf-grade-driven** — all their lines are MED tier and die on SAT/BLEND/HIERR (RYA-395 quality cuts); the "pool gf is Kurucz/ungraded" wording is right for Mg/Y only | a tier re-ratification (RYA-522) or a broadened pool (RYA-161/162) surfaces a held value |

---

## Procyon (calibration check) — NATIVE

| Component | Verdict | Value | Established by | Status | Reopen only if |
|---|---|---|---|---|---|
| Procyon Fe | reproducible anchor | Fe I 7.571 (NLTE) / Fe II 7.535 | RYA-506 (clean-from-raw, MERGED PR #126 `3425995`) | SETTLED — supersedes the un-reproducible 7.593 | — |
| Procyon Fe I/II NLTE gap | diagnostic | +0.036 (NLTE widens from +0.023 LTE) | RYA-506 | OPEN — carried to the ξ-pin review (RYA-322) | RYA-322 |

---

## Pipeline integrity — NATIVE

| Component | Verdict | Value | Established by | Status | Reopen only if |
|---|---|---|---|---|---|
| theo-EW spawn/fork bug | root-caused + fixed | force-fork + all-zero guard + tmp makedirs | RYA-506 (MERGED `3425995`) | SETTLED | — |
| fork coverage | all entry points + both machines | centralized `pipeline/_runtime.py` force-fork + BLAS pins, imported by every entry point | RYA-514 (MERGED #129) | SETTLED — fork confirmed on Sirius py3.12 | — |
| Authoritative channel | **the phase_c verdict is THE single source** | raw `run()` EW output = DIAGNOSTIC-ONLY (in-file labeled); divergence guard on the gold freeze | RYA-521 (MERGED) / RYA-520 | SETTLED — consumers read `read_solar_reference('CURRENT')`; `assert_authoritative_is_verdict` rejects the raw file | — |
| Wiring integrity ("merged ≠ wired") | a merged capability can be orphaned (never invoked) — a silent-omission class | RYA-463 problem-children registry was BUILT but ORPHANED; now wired as the per-element disposition source + completeness/loud-fail guards | RYA-519 (MERGED #131) | SETTLED — run('solar') emits a value or a cited disposition for every target; 0 silent | a new orphaned merged capability |
| grade application | `line_grade` applied to EW aggregation | — | RYA-329 | OPEN — computed, not applied (silent leak). NOTE: RYA-407 found the Fe I scatter is an honest floor, so 329 is NOT the scatter driver (earlier guess retracted) | — |
| per-line engine provenance | `{engine, correction, source}` stamp per line | — | RYA-250 / RYA-512 / RYA-515 | OPEN — no per-line stamp yet; verdicts not fully auditable | — |
| Engine (all 27 elements) | Turbospectrum synthesis + MOOG EW comparison | `RADIATIVE_TRANSFER_CODE='turbospectrum'`, `EW_BASELINE_CODE='moog'` | RYA-285 (Done) | SETTLED — reverses MOOG-for-speed; Turbospectrum-via-EW not viable (RYA-234) | — |
| Single-source gf architecture | ONE authoritative log gf per physical line (no duplication) | `data/linelists/canonical_gf.csv` (145 887 lines) resolved at load by `gf_resolver.py` for BOTH synth (branching-preserved HFS rescale) + EW paths; `species.py` canonicalises species/ion (no silent mis-key); `check_stewardship.py` CI fails on any UNTRACKED duplicated-and-divergent canonical value (gf/STAR_PARAMS/provenance/blend_flag) | RYA-345/350/353/354/355/358/368/408 (`6eb2569`/`812d39c`/`bd881ad`/`aab6e2b`/`58a62e2`) | SETTLED — wired in abundances_derive/synth/CNO; test-covered; **surfaced by RYA-530** (was BUILT✗NOT-REGISTERED) | a canonical value gains a 2nd divergent source |
| HFS-resolved synthesis | measures HFS-split lines the single-profile EW path SAT-culls | flux-space synth fit over native GES hyperfine components (`cno_synthesis._fit_element`); output → problem_children/gold via RYA-519 | RYA-411/466/473/476 (`eb5592d`/`723dec8`/`10a016c`/`77259c3`) | SETTLED (method) — Mn gold-tier A=5.554 measured this way; Cu+V measured, held; **surfaced by RYA-530** | new HFS element measured |
| EW-verification layer | per-line EW-integrity QA, flags-only (validate-don't-tune) | BAD_FIT/ABUND_OUTLIER/COG_FLAG + literature LIT_DEVIATION; `assert_no_ew_mutation`; charter C I 5380 / Li 6707 / Eu 6645; `ew_integrity.py` | RYA-458 (`e5321e6`) | SETTLED — wired in abundances_derive → `{star}_ew_integrity.csv` → problem_children; **surfaced by RYA-530** | — |
| Loader frame/OBJECT contract | executable anti-silent-substitution guard at every loader boundary | OBJECT-by-header (no glob) + velocity-frame (no double-BERV) + wave-scale (unit + vac/air, ×10/×10⁴ gate); raise-never-default; `frame_object_contract.py` | RYA-481/264 (`b359189`) | SETTLED — enforced; closes 6 documented incidents; **surfaced by RYA-530** | a loader bypasses the contract |
| Multi-arm CNO arm-registry | loud-fail instead of silently synthesising a non-solar star against Vesta sunlight | per-star `STAR_ARMS` (region+diagnostics+loader+readiness); `run_cno`/`run_phase_a` resolve from it, `ArmNotWired` raise; `cno_synthesis.py` | RYA-464 (`9ebbfbf`) | SETTLED — solar + Procyon declared (Procyon UVES ready; UV/IR deferred); **surfaced by RYA-530** | new star needs its arms declared |
| Reflected-solar RV frame | asteroid-ephemeris conditioning (Vesta) | two-leg Horizons model anchored to Fe-core bulk velocity, held-out verified, per-frame (no coadd); body-ID bug fixed; `reflected_solar_rv.py` | RYA-372/394 (`0874f6c`/`01d001b`) | SETTLED (Vesta optical); reused by CRIRES+ IR (RYA-373); **surfaced by RYA-530** | — |
| vac↔air SSOT | single Birch&Downs-1994 converter (`wavelength_util`), identity <2000 Å | all loaders/conditioning + the two converting intake scripts routed through it — no second formula in-repo | RYA-264/501 (`ba0b92d`/`2a1a3e8`) | SETTLED — no duplicate impl; **surfaced by RYA-530** | a new vac/air conversion bypasses it |
| Two-engine floor (governing law) | every element on BOTH 1D-NLTE + synthesis; report the best engine by a PRE-DECLARED line-quality criterion **per LINE** (σ / REW-saturation / blend flag / COG / HFS — NEVER reference-proximity), aggregated inverse-variance to the element (clause-4 = INDETERMINATE-regime only; `CROSS_ENGINE_MIX_GATE` flags mixed-but-disagreeing → adjudicate, never silent-average); missing synth grid → RAISE (acquire, never silent EW-1D); rejected engine recorded per line but excluded from value + budget; cross-engine spread = separate diagnostic, never in the error bar | RYA-525 (ratified 2026-07-05; criterion + appendix ratified 2026-07-10) | **BUILT (RYA-525)** — selector + aggregation + `CROSS_ENGINE_MIX_GATE` + loud-fail guards + reference-blind firewall in `pipeline/engine_selection.py`; knobs in `config.TWO_ENGINE`; law in `docs/SCIENCE_STANDARDS.md`; **silent-LTE swallow in `abundances_derive.run()` REMOVED**; 29-case test green; sample record `data/audit/rya525_two_engine/`. Guard reads the RYA-526 two-engine ledger as its exception list. **Full 27-element both-engine RUN gated on RYA-526 grid coverage → executes under RYA-527** | a selector keys on reference-proximity; the 527 run regresses |
| Ratified-exclusion enforcement (Cr II) | a ratified scientific exclusion must be **unrepresentable**, not remembered | the RYA-240 Cr II exclusion (COG artifact) is enforced inside the two-engine floor: Cr reports **Cr I** (owed); Cr II is DIAGNOSTIC-ONLY until independently verified — the floor cannot silently promote it | RYA-558 (MERGED PR#158 `0f7b917`) | SETTLED — guard in `pipeline/engine_selection.py`, test-covered. The *consumer* (Cr I gf-floor 6.022) rides the unmerged RYA-527 stack; the guard itself is on main | Cr II is independently verified (retires RYA-240) |
| Floor-promotion gate (CURATION-OWED → PASS) | **STRICT three-gate, registry-sourced** — Ryan-ratified: a missing cross-engine delta (`dCE`) **FAILS**, it does not abstain | promotes **Ca only** on the current pools; **Mg = ratified HOLD at 7.614** (gate 3 fails — Mg I 5528 EW 340 mÅ ≈ 3.4× the ratified saturation knee ⇒ no Engine-A EW route ⇒ `dCE` None) | RYA-561 (guard MERGED PR#164 `35f4359`) | SETTLED as law + guard — thresholds read from the registry, not literals (`tests/test_floor_promotion_rya561.py`). **Never merge the full 561 branch** — its consumer rides the unmerged RYA-527 stack | the strict-vs-abstain ratification is revisited, or Mg's saturation blocker clears |
| Upper-limit disposition enforcement | an `UPPER_LIMIT` element must not emit a point value | the two-engine floor was emitting a forbidden point value **A(Li) 1.409**; the guard makes that unrepresentable — Li carries the RYA-103/458 `UPPER_LIMIT` disposition through to the floor | RYA-563 (guard MERGED PR#159 `7f75287`) | SETTLED — guard on main (`tests/test_upper_limit_guard_rya563.py`); consumer rides the unmerged RYA-527 stack | a clean (non-CN-blended) Li determination lands |
| Fe-pool integrity (gate statistic = reported pool) | the gate scatter was computed over an **un-ceiling'd** pool — the COG cut hit the output CSV but not the gate statistic | fixed + locked with regression tests, so the statistic and the reported pool can no longer diverge | RYA-279 / RYA-280 (`tests/test_pool_integrity_rya279_280.py`) | SETTLED — regression-locked in `pipeline/abundances_derive.py` | a new pool filter bypasses the shared ceiling |
| Blank-cause honesty tripwire | **no stage emits or inherits a causal/provenance claim it did not itself establish** | `pipeline/provenance_honesty.py` is the SINGLE source of the tripwire; **both** emitters import it — `scripts/phase_c_verdict_rya371.py` (RYA-596) and `scripts/build_solar_reference_v2_rya522.py` (RYA-653). The zero-survivor claim is refused on any row carrying `n_lines > 0` **or** a measured value | RYA-596 (MERGED PR#168 `5c1e92d`) → generalized by RYA-653 | SETTLED — the phase_c-only gap is CLOSED. Gold v2's **Ba** row asserted the cull against a Ba that RYA-559 measured (**2.410**); rebuilt from the live verdict the row now reads `synthesis: Ba II 5853 HFS (RYA-559) — A(X) 2.410 HELD at tier 'owed' (RYA-522)`. Pre-fix, a rebuild would ALSO have fabricated the claim on Ca/Ti/Ni/Na/Al (2/10/2/2/1 survivors) and crashed on Sr. Corrected artifact `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv` is a **candidate, NOT frozen** — the freeze is RYA-527's gate | a third emitter appears; the corrected candidate is promoted (RYA-527) |
| Rest-frame + gsig fitter (single-sourced) | a fitter defect that was copy-pasted **four** times | the HARPS solar arm is **+0.76 km/s, not rest-frame**, and the broadening grid railed at its 1.5 km/s floor; the defect lived in 4 independent copies (Sr / Zr / **Co** / Mg). Now single-sourced in `scripts/solar_profile_fit.py` + guard tests; arm velocity is measured abundance-blind (HARPS +0.756, IAG +0.278) and **raises** rather than defaulting to zero | RYA-643 (MERGED PR#174, main `51bc1dd`) | SETTLED (defect) — Mg regression gate reproduces the committed artifact **exactly (0/121 values differ)** ⇒ behaviour-preserving refactor. Corrected re-runs **ALL CONFIRMED-ROBUST** (Sr II 2.759, Zr unchanged/still owed, Co 4.960 / PASS survives). **OPEN:** near-UV red-χ² 78–180 (Sr II row); three Co lines now rail at the TOP of the widened gsig grid (5301/5331 at 8.0 km/s, 6632 moved −0.133) — all sub-floor + excluded, and now *visibly* bad rather than hidden | a fifth copy of the fitter appears; the railed Co lines are adjudicated |
| Results-ledger discipline | per-element status is git-tracked and **generated**, not chat-tracked or hand-synced | `data/audit/element_status_tracker.csv` (27 rows, **GENERATED**) built by `scripts/generate_element_status_tracker_rya654.py` from phase_c + `data/audit/element_status_tracker_editorial.yaml`; discipline in `docs/CONVENTIONS.md` (**there is no CONTRIBUTING.md**) | RYA-594 (MERGED PR#165 `33e7d41`) → RYA-654 (generator) | SETTLED — the tracker is a **read-set member** (see `LEDGERS.md`). **Hand-editing it is a build break**: `--check` diffs committed vs regenerated. Cross-artifact contradictions are guarded by **RYA-632** `pipeline/ledger_consistency_guard.py` | neither is merged yet — RYA-654 carries the RYA-632 commit |
| Engine B substrate (NLTE synthesis engines) | Corrects the RYA-529 PySME-wholesale row. Production NLTE is universally a 1D delta CSV added to the TS/EW LTE abundance (**Engine A**; `nlte_corrections` + `nlte_cno`) — there is **no bsyn in-synthesis NLTE** anywhere in production. RYA-402's "two-family" split maps how the Engine-A delta is DERIVED: **Family A** (O/Na/Mg/Si/Ca/Ti/Mn/Co/Ni/Sr/Ba) was planned as TS-native Gerber-2022 (A&A 666 A18) but **never executed** — Na/Mg/Si/N from Amarsi-2020 via **PySME** (RYA-410/526), Ca/Ti/Mn via **MPIA**, Ba via **Korotin**, Sr via **INSPECT**, C/O via **nlte_cno** (Amarsi-2019), Co/Ni **LTE**; **Family B** (Al/K/S) via **PySME** on Amarsi `.grd` (Zenodo 3982506; Cu PySME-validated but unregistered). **Engine B = Turbospectrum synthesis**, LTE for all 27 in production TODAY; the Gerber TS-native NLTE departure deck (NLTEINFOFILE/MODELATOMFILE/DEPARTUREFILE + 4D interp) is now **BUILT + Na-VALIDATED on Sirius (RYA-533)** — Turbospectrum_NLTE BSYN v20.1 compiled, Na Gerber-2023 (A&A 669 A43) grid/atom provisioned, deck reproduces the solar Na anchor (median δ **−0.068** vs −0.107, PASS). **Rollout (RYA-534): COMPLETE — 11/11 provisioned + deck-validated.** 10 reproduce their independent published 1D-NLTE anchor within tol (PASS): Na, O, Mg, Ca, Mn, Sr, Ba, Ni, Co, Si. **Ti = CHECK — unresolved SAME-ATOM systematic (RCA open, RYA-535).** Engine-A (MPIA Bergemann-2011, MAFAGS-OS) and Engine-B (TS-Gerber, MARCS) use the SAME atom — Gerber 2023 Table 1 lists Ti = Bergemann 2011; `atom.ti503b` merely ships it. So +0.221 vs +0.107 canNOT be a model-atom difference; it is a same-atom systematic of unknown origin (atmosphere MAFAGS-OS-vs-MARCS / interpolation / downstream), RCA-mandatory before any label. NOT registered. (Mn's ~½ carries the SAME open provenance question — RYA-411/535.) Highlights: O 777 −0.105 vs Amarsi-2019 1D −0.134 = RYA-362 cross-check; Sr II/Ba II = ionised-species fix; Ni/Co = line-ID fix | Engine-A 1D-NLTE: VALIDATED (RYA-529 Na PySME leg δ −0.129 vs −0.107 on Sirius; Ca/Ti/Cr/Mn/Ba/Sr published grids; C/O nlte_cno). **Engine-B TS-native NLTE synthesis: BUILT + Na-VALIDATED (RYA-533)** — Step-0 forensics = NOT-BUILT on all 5 channels (RYA-361 scoped it, never executed), then built from scratch on Sirius: TS_NLTE v20.1 compiled + Na Gerber-2023 grid(15.9 GB)/atom/aux provisioned (md5-pinned, Sirius-only) + bsyn NLTE deck (interpol departures → babsma → bsyn NLTE-vs-LTE COG). GATE GREEN: Na I 5682 δ−0.065 / 5688 δ−0.072, **median −0.068 vs −0.107 ± 0.05 → PASS** (validate-don't-tune; the ~0.04 offset = Gerber-atom vs Lind-2011). Cross-engine Na: INSPECT −0.107 / PySME −0.129 / TS-Gerber −0.068 = the RYA-525 model-atom-systematic diagnostic. All 11 Family-A elements provisioned + deck-validated (RYA-534 rollout → `data/audit/rya534_family_a_rollout/rollout_status.md`): 10 PASS vs anchor; Ti an UNRESOLVED same-atom systematic (+0.221 vs +0.107; RCA open RYA-535) — NOT registered, NOT a 'model-atom difference' (same Bergemann-2011 atom). Mn's ~½ shares the same open provenance question (RYA-411/535). Deck `scripts/ts_gerber_na_gate_rya533.py` (Na) + `scripts/ts_gerber_gate.py` (element-general, RYA-534); test `tests/test_ts_gerber_nlte_rya533.py`; map → `data/audit/rya531_engineB_map/`; build → `data/audit/rya533_ts_gerber_build/` | RYA-402 / RYA-529 / RYA-531 / RYA-533 | SETTLED — Engine-A 1D-NLTE multi-source + validated; **Engine-B TS-native NLTE deck BUILT + Na-VALIDATED on Sirius (RYA-533, gate green δ−0.068 vs −0.107)**. Per-element Engine-B NLTE rollout beyond Na is mechanical (grids exist for O/Mg/Si/Ca/Ti/Mn/Co/Ni/Sr/Ba) and continues as needed. RYA-531 finding held: TS-native was a deliberately-abandoned route (RYA-402 skipped Option 1) — now built | grid/atom set changes; engine regression; either Na gate (PySME or TS-Gerber) reopens; Engine-B NLTE gate passes |

---

## NLTE grids — NATIVE (selection is the register's truth; coverage is per-target)

| Species | Grid / source | Coverage note | Cite | Status |
|---|---|---|---|---|
| Fe I / Fe II | **MPIA Bergemann MAFAGS-OS 1D-NLTE** (`Fe_Bergemann_MPIA.csv`, RYA-319) — supersedes the Amarsi-2022-NN MLP (the prior row was STALE, never updated after RYA-319; the Amarsi MLP is ARCHIVED — Teff≤6500/[Fe/H]≤0 ceiling can't cover Procyon 6554/55 Cnc +0.32; kept only for the RYA-283 3D-vs-1D solar cross-check) | covers the full benchmark box (Teff 5000–6600, logg 4.0–4.6, [Fe/H] −0.2..+0.35); ionization-balance-gated (RYA-406/407) | RYA-319 (`5d42702`) / **RYA-549** (forensic + vintage) | SETTLED — **code = register = matrix (RYA-549)**; NLTE δ TINY (+0.010 solar / +0.004 at 55 Cnc), ionization-balance-gated → **NOT the Ti/Mn/Cr scaled-Drawin inflated class**; ab-initio migration deferred (no covering grid, negligible stake) → RYA-550 |
| Ca I | MPIA MAFAGS-OS / Mashonkina 2017 (`Ca_Mashonkina2017.csv`) | confirm coverage at [Fe/H]=+0.31 | constants.py `NLTE_CORRECTION_ELEMENTS` / RYA-235 | SETTLED (selection) |
| Ti I | **Mallinson 2024 (A&A 687 A5) via PySME** (`Ti_Mallinson2024_PySME.csv`, ab-initio Grumer-Barklem-2020 H collisions; Zenodo 10753497) — **supersedes** `Ti_Bergemann2011_MPIA.csv` (Bergemann-2011 scaled-Drawin, inflated ~2×; retained on disk, unwired) | solar δ **+0.0506** (5689/5648/5662), reproduces published +0.052; corroboration-certified (RYA-545) | RYA-545 / constants.py | **registered** — Mallinson-2024 ab-initio (PySME/MARCS), δ=+0.0506; corroboration-certified (two-instrument, RYA-545); ionization-balance precision-limited (SEM≫0.05 gate — documented diagnostic, not a value defect); supersedes Ti_Bergemann2011 scaled-Drawin +0.108 (RYA-546) |
| Cr I | MPIA MAFAGS-OS / Bergemann & Cescutti 2010 | Cr II deliberately excluded (RYA-240 COG artifact) | constants.py / RYA-235/240 | SETTLED (selection) |
| Na / Mg / Si | **Amarsi 2020 via PySME** (`*_Amarsi2020_PySME.csv`, RYA-410 re-source) — supersedes the legacy Lind-2011(Na)/Bergemann-MPIA(Mg/Si) CSVs (still on disk, unwired) | feh axis is [Fe/H] RELATIVE — no solar offset (unlike Fe) | RYA-410 / RYA-165/396 | SETTLED (selection) — **source corrected by RYA-530** (row previously mis-cited Lind/MPIA) |
| Ba II / Mn I | Korotin 2015 (Ba, VizieR) · Bergemann MPIA (Mn) | — | RYA-165/396 | SETTLED (selection) |
| Al I | Amarsi 2020 GALAH departure via PySME → `Al_Amarsi2020_PySME.csv`; atom Nordlander & Lind 2017 | subordinate 6696/6698 (not the +0.2 resonance 3961); solar median δ −0.022 | RYA-402 Family-B (`c10bd52`) | SETTLED (selection + wiring) — **surfaced by RYA-530** (built, was off-table) |
| K I | Amarsi 2020 GALAH via PySME → `K_Amarsi2020_PySME.csv` | resonance 7665/7699, large negative (solar −0.27/−0.31); 7665 in O₂ A-band → clean 7699; EW measurement telluric-blocked (RYA-380) | RYA-402/462 (`ea72a42`) | SETTLED (selection + wiring); measurement GET-DATA-pending — **surfaced by RYA-530** |
| S I | Amarsi 2025 (A&A 703 A35) via PySME → `S_Amarsi2025_PySME.csv` | optical 6748/6757 high-EP, small negative (solar −0.016); NIR 9212/28/37 separate indicator | RYA-402 Family-B (`f1e35fa`) | SETTLED (selection + wiring) — **surfaced by RYA-530** |
| Sr II | WORKING = Bergemann 2012a/INSPECT → `Sr_Bergemann2012_INSPECT.csv` (applied). PRIMARY = Mashonkina 2022/INASAN NOT vendored (WAF-403, manual pull owed) | 4077 primary / 4215 Fe I-blended cross-check; [Fe/H] ceiling +0.0 → solar/55 Cnc out-of-hull, loud | RYA-421/428/433; **measurement RYA-551 → superseded by RYA-643** | **MEASURED, CLEARED, NOT ADOPTED** — RYA-551 delivered the Sr II Turbospectrum synthesis (4077 PRIMARY, 4215/4161 cross-check, 4305 EXCLUDED at ~zero sensitivity). RYA-643 then found that harness carried the two RYA-592 defects (no rest-frame handling; broadening grid railing at its 1.5 km/s floor) and re-ran it on the corrected single-source fitter: **A(Sr II) 4077 2.769 → 2.759** (Δ −0.010), 4215 2.724 → 2.693 (Δ −0.031); **worst reliable \|Δ\| 0.031 ≤ the 0.05 gate → CONFIRMED-ROBUST, cleared for RYA-527 adoption.** ⚠️ **TWO things "cleared" does NOT mean.** (a) **NOT adopted:** the live verdict channel still reads the **Sr I EW leg** (A 4.961, +2.13, `EW (SUSPECT)`, gold v1) — Sr II 2.759 is queued, not wired; closing that wiring gap is **RYA-527**. (b) **NOT fit-quality-cleared:** robustness-to-the-defect ≠ fitness-for-adoption — near-UV red-χ² is **78.3** (4077, improved from 95.7) and **180.0** (4215, from 199.0) against Mg's **1.4** in the optical, i.e. the model does not reproduce that window. A SEPARATE open concern; RYA-643 explicitly did not settle it — **surfaced by RYA-530** |
| C I / O I | Amarsi 2019/2020 grids (owned by `nlte_cno`) | C/O flagship prerequisite | RYA-359 | OPEN — status per C/O track |
| N I | Amarsi 2020 N grid → `N_Amarsi2020_PySME.csv` (Zenodo 3982506, md5-verified; derived via PySME on Sirius, grids never on Mac) | N I red near-LTE at the Sun (solar δ −0.0115/−0.0145/−0.0154, max\|Δ\|=0.0000 vs the RYA-369 load-test); **warm-star indicator** — 4 cool metal-rich weak-line COG nodes excluded → out-of-hull loud-flag (genuine weak-line info-loss; bracket-widening worsened it), not shipped as spurious +0.2 | RYA-526 (wired into `NLTE_CORRECTION_ELEMENTS`) / **RYA-556 (wired into the phase_c Kitt Peak channel)** | SETTLED (selection + wiring) — production resolves the solar δ live. **N is OFF NLTE-OWED (RYA-556)**: the registered grid is now applied in the verdict channel, so the NLTE-OWED bucket is 0. Live verdict **A(N) = 8.188 (+0.358 vs Asplund 7.83), CURATION-OWED** — measured on 3 independent Kitt Peak red multiplets that AGREE (1D-LTE mean 8.202, spread 0.033). **The wired per-line NLTE δ is −0.0115/−0.0145/−0.0154 (mean −0.0138), which REFUTES the old audit's predicted ≈ −0.37** — N I red is near-LTE at the Sun. The surviving +0.36 is therefore a **gf/data-channel floor (→ RYA-161)**, NOT an NLTE debt; do NOT tune. NOT validated off-Sun: Teff-bracket owed (Procyon / α Cen B, RYA-369) | 3D-NLTE, or a new N I grid |
| — (principle) | Amarsi/Balder **and** Bergemann/MPIA are **independent measurements**; inter-model spread feeds the uncertainty budget | not "pick one" | RYA-282 / project | SETTLED (principle) |

_NLTE-grid notes: **Cu** — ~~departure inputs staged but the grid is NOT committed and NOT wired~~ **SUPERSEDED by RYA-540** (`70456d2`): the Caliskan-2024 departure grid is **derived, committed (`data/nlte_grids/Cu_Caliskan2024_PySME.csv`) and WIRED** (`NLTE_CORRECTION_ELEMENTS['Cu']`, applied in the default run; solar δ +0.001, reproducing Shi+2014's small-positive optical Cu). The RYA-530 note above was written pre-540 and read as a grid gap for two months — corrected at RYA-313 when the CI suite caught the tests still asserting it. **The blocker never moved: Cu production stays GET-DATA on measured-line QUALITY** (5 lines, red_chi2 8–127; RYA-395/466) — registered-and-still-owed, the same shape as owed-HELD. **V** — genuine void, no public NLTE grid → HFS-LTE carry-forward (RYA-470/526)._

---

## 3D models & model atmospheres — NATIVE

| Item | Selection | Use | Cite | Status |
|---|---|---|---|---|
| 3D hydro grid | STAGGER — Magic et al. 2013 | 1D→3D Fe correction trajectory; C/O 3D path | RYA-336 / project | SETTLED (selection); 3D-RT *synthesis* engine not yet in hand (RYA-444) |
| 1D→3D correction (applied delta) | `A(3D-NLTE)=A(1D-NLTE)+δ3d` from vendored `data/threed_grids/solar3d_metals_rya399.csv` (Si −0.01 Amarsi&Asplund 2017; Ti +0.06, Cr +0.03 Scott 2015); `3D_unavailable` flag if no node; wired via `nlte_corrections.apply_threed_corrections` | Si/Ti/Cr (+ C/O via nlte_cno) | RYA-399 (`a3c5796`) | SETTLED (applied) — **surfaced by RYA-530**; FINDING: 3D is NOT the lever for the Si/Ti/Cr residual (\|δ\|≤0.1); **distinct** from the still-absent 3D-RT synthesis engine (RYA-444). `CORRECTIONS_3D` scalar dict is now LIVE — carries the solar Fe 1D→3D entry (`Fe_1D3D_solar_dex = -0.05`, Magic 2013, applied at the reported layer, RYA-553) alongside the O 6300/6363 −0.07 Caffau anchors; the Si/Ti/Cr grid path (`solar3d_metals_rya399.csv`) is separate |
| Model atm (FGK dwarfs) | ATLAS9 / Castelli–Kurucz (plane-parallel, LTE) | 1D LTE base | constants.py `MODEL` | SETTLED (selection) |
| Model atm (M dwarfs) | MARCS.GES | 1D LTE base | [confirm] | [confirm] |

---

## Line lists — atomic + molecular — NATIVE

| List | Source + version | DOI / cite | Status |
|---|---|---|---|
| Atomic | VALD3 (manual web extraction; see `skills/codex-vald-extraction`) | vald.astro.uu.se | SETTLED |
| OH (mid-IR) | ExoMol **MYTHOS** 20240526 | 10.1093/mnras/stae2803 (Mitev 2025) | SETTLED — verified RYA-503 |
| NH (mid-IR) | ExoMol **kNigHt** 20240301 | 10.1093/mnras/stae1340 (Perri & McKemmish 2024) | SETTLED — RYA-503; 2kNigHt (no DOI) deferred |
| CH (mid-IR) | **MoLLIST** (Masseron 2014) | 10.1051/0004-6361/201423956 | SETTLED — RYA-503 |
| CO (mid-IR) | CO_IR_Li2015 (vendored) | RYA-360 / RYA-503 | SETTLED — CO round-trip validated (117783/117783) |
| Molecular scope | mid-IR ro-vibrational fundamentals acquired; synthesis is **v2** (Orion-gated) | RYA-503/504 | SETTLED (v1/v2 boundary) |
| Molecular linelist converter (tool) | ExoMol/MoLLIST `.states/.trans` → Turbospectrum `.bsyn` (`scripts/molecular_linelist_convert.py`); CO round-trip hard gate; single path for future molecular species | RYA-503 (PR#124) / RYA-360 | SETTLED — **surfaced by RYA-530** (tool distinct from the lists above) |

---

## Instruments — NATIVE

| Band | Instruments | Archive | Notes |
|---|---|---|---|
| Optical | HARPS (R~115k), ESPRESSO, UVES, FEROS | archive.eso.org | HARPS-N (TNG, ia2.inaf.it/tng); Keck/HIRES (koa.ipac.caltech.edu) |
| UV | HST **STIS** (all science gratings) + **COS G130M/G160M FUV** | mast.stsci.edu | **WFC3 EXCLUDED (RYA-119)** — corrects the pinned doc |
| Near-IR / IR | CRIRES+, NIRPS | eso.org | SPIRou / ESPaDOnS (cadc.nrc.ca); NARVAL (see Scope/drift, source [OPEN]) |
| IR reference atlases | ACE-FTS (telluric-free), NSO Kitt Peak photatl, Wallace NIR | on disk | solar IR reference / atlas fallback (RYA-513) |
| Telluric routing | CRIRES+/UVES-red/ESPRESSO-red/FEROS-red/NIRPS = molecfit + per-night GDAS · SPIRou = APERO+Wapiti | — | RYA-424 (confirmed). **NIRPS nuance:** 424 lists NIRPS under molecfit, but the actual α Cen NIRPS work (RYA-494) used the native-DRS `FLUX_TELL` product + Wallace verify — reconcile before an IR NIRPS run |
| UV conditioning (no-telluric sibling of Telluric routing) | STIS/COS FUV/NUV standing stage (`uv_conditioning.py`) — vac→air @2000 Å boundary, scattered-light + chromospheric-core masks, FUV-synthesis-not-EW refusal, NLTE-grid-owed loud-flag; `analysis_ready` manifest (RYA-424 schema) + UV line-selection policy | — | RYA-426/190 (`a97e745`) — **surfaced by RYA-530**; UV arm production-deferred on RYA-359 |
| IR-native α Cen star-ID | RV-ephemeris orbit (Kervella 2016; `acen_orbit.py`) predicts A/B heliocentric RV per epoch → PRIMARY discriminator for IR frames the optical classifier can't reach; `rv_bounds` rejects off-orbit frames | — | RYA-423/431 (`b5fa4a6`) — **surfaced by RYA-530**; RV>OBJECT authority ranking owed (RYA-495) |
| Multi-instrument loaders | UVES (BERV/TOPOCENT + product guards + registry, wired) · HST STIS/COS UV (wired, `ready=False` pending RYA-359 grid) · SPIRou; common `base_loader` SpectrumData contract | — | RYA-272/471 (`0fd75f2`/`a77b6fa`) — **surfaced by RYA-530**; SETTLED (UVES) / DEFERRED (HST UV) |

---

## Infrastructure — NATIVE

| Component | Verdict | Value | Established by | Status | Reopen only if |
|---|---|---|---|---|---|
| Sirius | **authoritative runner — BUILT** | venv312 (py3.12.13/numpy2.2.6, byte-identical to Mac), iSpec compiled, NLTE grids, engines, RYA-514 fork confirmed. **⚠️ "`/mnt/codex-data`: … engines" is imprecise — the engines are SYMLINKS to `/srv/codex/engines/` on the OS drive `/dev/sdb2` (~85 G free), not the data drive. Full inventory → "Compute assets — where everything lives"** | RYA-511 Phase 0 (done) / **RYA-313 (asset inventory + path audit)** | SETTLED — all runs execute on Sirius now; reproduced Fe I 7.516/n=62. **RYA-527-ready: every iSpec path the pipeline reads verified present 2026-08-05** | — |
| Compute stack | **py3.12 + numpy 2.2** (reference stack, off EOL 3.9.6) | exact-zero cross-machine floor (per-line bit-identical); Mac↔Sirius drift = **null** | RYA-517 (merged) | SETTLED — Sirius alone authoritative (no dual-machine cross-confirm required) | new engine ceiling |
| Trust model | Sirius = the authoritative runner (null drift) | forced-fork + single-thread BLAS both machines | RYA-506 / RYA-511 / RYA-517 | SETTLED — "Sirius = native fork" dead (py3.14 forkserver); 517 proved null drift → Sirius authoritative | — |
| Sirius scope (downloads too) | **all downloads + grid extractions run on Sirius, never the Mac** | a Mac-side `.grd` extraction filled the disk | RYA-526 | SETTLED — extends "runs on Sirius" to downloads/extractions | — |
| Sirius-only compute — **ENFORCED, loud-fail** | the convention above is now machinery, not a habit | grid/compute paths resolve through the SSOT resolver (`config/constants.py`); a local/Mac grid path **raises** — there is no silent local-grid fallback, and grid provenance is asserted at use | RYA-567 (MERGED PR#161 `e786008`) | SETTLED — wired across `pysme_nlte` / `nlte_cno` / `threed_corrections` / `curate_nonfe_pools`. OPEN: the Mac EW leg still runs locally (RYA-509) | a new compute path bypasses the resolver |
| Codex Systems Catalog | per-system master index — **index-by-pointer, never physics** | `data/catalog/system_catalog.csv` (**13 systems**) + `pipeline/system_catalog.py` validator that **rejects any physics-like column** (a stellar parameter must never be duplicated here) | RYA-631 (MERGED PR#167 `9701909`) | SETTLED — the catalog indexes identity + pipeline lifecycle and points at the real sources. ⚠️ **`STAR_PARAMS` lives in `config.constants` (from `config/stars.yaml`), NOT `pipeline.constants`** — live keys are solar / procyon / alpha_cen_a / alpha_cen_b / 55cnc_a (+ the `synthetic_no_logg` fixture, which has no catalog row). `benchmark_candidate` is in `POINTER_EXEMPT_ROLES` (brief was self-contradictory; ratified by merge). Owed: identity pass (SIMBAD/NEA, cited) | a physics column is proposed for the catalog |
| Instrument + holdings catalogs | canonical, agent-readable — **read the holdings registry BEFORE proposing any download** | `data/catalog/instrument_catalog.csv` (**25 instruments**) + `instrument_modes.csv` (**11 modes**) + `holdings_manifest_registry.csv` (**11 holdings**), with README; rebuilt quick-start in the repo README | RYA-642 / RYA-652 (MERGED PR#175, main `51bc1dd`) | SETTLED — these are read-set members; see `LEDGERS.md`. The holdings registry is the **anti-reinvent** surface: it records what we already hold | a new instrument/mode is verified in use, or data is acquired |
| Ledger read-set + register freshness | the register itself is now guarded against silent drift | **`LEDGERS.md`** (repo root) names the canonical read-set and what each ledger owns, so a cold agent / Sirius-local model / collaborator knows what to read and in what order — that knowledge previously lived only in Claude.ai memory + chat. **`scripts/check_register_freshness.py`** loud-fails when any state-changing surface is newer than this register (history mode) or when a PR touches state without touching the register (`--since-main`); the surface registry is single-sourced in `pipeline/state_surfaces.py` | RYA-659 / **RYA-313+314 (CI)** | SETTLED — the guard reproduces the drift that motivated it (register 2026-07-17 `08265f9` vs tracker 2026-07-27 `97485e9`), and **the CI harness now exists**: `.github/workflows/ci.yml` runs the freshness guard blocking (PR + push modes) inside the `CI / test` job. The v23 reopen-trigger ("a CI harness lands") is DISCHARGED. RYA-632's ledger guard is pre-wired in the same job as `continue-on-error` and activates when that branch merges, with no edit to the workflow | the guards move out of the `CI / test` job (the required-check name would change) |
| CI + merge protocol | **PR-gated, CI-gated, no admin bypass** | `main` requires a PR (ruleset "Require PR on Main", **bypass list empty** so it binds admins) + the **`CI / test`** status check green. House merge style = **merge-commit (`--no-ff`)**, ratified by Ryan 2026-08-05 per the RYA-305 precedent; squash and rebase are disabled at the ruleset. The redundant ruleset "Require Pull Request before merging" (which despite its name carried NO pull_request rule, only deletion/force-push/update, plus an admin `bypass_mode: always`) is retired | RYA-313 (protocol, Ryan 2026-06-14) / RYA-314 (workflow) | SETTLED — the gate is real for the first time: CI had **never run** in this repo's history before RYA-313. Runs on a **Sirius self-hosted runner**, not `ubuntu-latest`, because 24 of 118 test files import iSpec (external, not vendored, compiled MOOG/SPECTRUM) and cannot even be COLLECTED on an ephemeral runner — measured 24 collection errors / 868 of 1116 passing, i.e. a green check hiding 22% of the suite. Sirius carries the engines and is the declared authoritative runner, so CI there also honours **RYA-567**. ⚠️ **This repo is PUBLIC** — fork-PR policy is set to *require approval for all external contributors* and the trigger is `pull_request` (never `pull_request_target`); residual accepted risk = the runner runs as `damienabraxas`, which has NOPASSWD sudo (unprivileged service account = follow-on) | the runner goes offline (no merges can gate), or the repo goes private (the fork-approval control can relax) |
| venv isolation | **PySME in its own dedicated venv**; the RYA-517 reference `venv312` (astropy/pandas exact pins) stays untouched — never install into it | a transient `pysme-astro` install downgraded venv312 (astropy/pandas); restored to exact pins | RYA-526 / RYA-517 | SETTLED | deliberate reference-stack bump |
| NIRPS solar from-raw | walled | wave_THAR global FP-comb starvation vs stale 2022-11 prior | RYA-498 (parked) | SETTLED (make-do: atlas fallback) | 2023-epoch 3.3.12 WAVE_MATRIX becomes trivially available |
| α Cen NIRPS | adopt existing reduced ADP | S1D_FINAL_A (HA, 28 frames) | RYA-500 (reframed) / RYA-494 | SETTLED — absolute-mode, version-caveat; NIRPS = α Cen **A** not B (RYA-494) | fresher prior (as above) |
| **Sirius storage reliability** | ⚠️ **OPEN RISK — the drives STALL under sustained write, and the cause is not yet known** | **2026-08-06:** the OS SSD `/dev/sdb2` (unbranded **NT-512 2280**) stopped answering writes for **>40 s** during an `apt-daily-upgrade` → NVIDIA **DKMS** rebuild. The kernel's default **30 s** SCSI command timeout fired (`hostbyte=DID_TIME_OUT … cmd_age=40s`), ext4 **aborted its journal**, root remounted **`emergency_ro`**, the CI runner crash-looped, and **every merge in the repo was blocked for 16 h**. Recovered by reboot + boot-time `e2fsck` (→ `Filesystem state: clean`; the only casualties were two DKMS build artifacts, resolved by `debugfs -R ncheck` — **no science data, engines, or repo content**). **THIS IS NOT MEDIA FAILURE:** `smartctl -H` **PASSED** on both drives · **0** reallocated/pending/uncorrectable sectors · **0** `UDMA_CRC_Error_Count` (so the SATA links are electrically sound) · sdb's own SMART error log reads **"No Errors Logged"** · 31 °C · 1084 power-on hours. ⚠️ **AND IT IS NOT ONE DRIVE:** `sda` (the WD data drive, a *different* SATA port) threw the same failure class on **2026-07-27** — `WRITE FPDMA QUEUED` failures + `SError: { PHYRdyChg CommWake DevExch }` + `hard resetting link`, 3× in 34 min. **Two drives, two ports, same class, both SMART-clean ⇒ suspect the HOST** (SATA controller / power delivery / firmware), not either disk. **Mitigation IN FORCE:** `/etc/udev/rules.d/60-ssd-command-timeout.rules` raises the SCSI command timeout **30 s → 180 s** on both non-rotational disks — a **seatbelt, not a cure**: it makes a stall survivable (the write pauses) instead of fatal (journal abort → read-only root). `smartd` now installed + enabled. Evidence, backups (engines + runner creds) and the rule → `/mnt/codex-data/incident_rya660_20260806/` | RYA-660 (incident 2026-08-06, recovered same night) | **OPEN RISK — mitigated but UNEXPLAINED.** Do not read "recovered" as "resolved": the stall mechanism is intact and can recur under any sustained write. Two owed: **RYA-661** alerting (nothing warned for 16 h — and note the trap, `systemctl is-active` reported `active` while the runner was dead, so runner health MUST be read from the GitHub runners API, not systemd) and **RYA-662** the single-runner CI SPOF decision | a second stall occurs on either drive · SMART degrades off 0 · the host-side cause is identified or ruled out · the OS drive is replaced |
| Orion | planned 2nd runner (3D-RT / molecular-synthesis era) | — | project | PENDING | — |

---

## Compute assets — WHERE EVERYTHING LIVES (NATIVE, RYA-313)

_The archive of what is **downloaded, built and ready to compute**. Verified on Sirius 2026-08-05; re-verify with `python scripts/check_register_freshness.py` + the CI preflight step, which loud-fails on a missing venv/iSpec/mount._

**⚠️ The engines are NOT on the data drive.** `/mnt/codex-data/engines/{ispec_src,pysme_src,TSFitPy,Turbospectrum_NLTE}` are **symlinks into `/srv/codex/engines/`**, which sits on the **OS drive `/dev/sdb2` (468 G, 81 % used, ~85 G free)** — a tighter budget than the data drive. Only `nirps_drs` is physically under `/mnt/codex-data`. This corrects the Infrastructure row's older "`/mnt/codex-data`: … engines" shorthand.

| Asset | Path (Sirius) | State | Version / provenance |
|---|---|---|---|
| **iSpec** | `/srv/codex/engines/ispec_src` (via `/mnt/codex-data/engines/ispec_src`) — resolve with `ISPEC_DIR` env var | **BUILT + RUNNABLE** (305 M) | engine set below; `ISPEC_DIR` override already supported by `config/constants.py` |
| ├ Turbospectrum (in iSpec) | `…/synthesizer/turbospectrum/` — **runtime binaries in `bin/`** | **BUILT** | `bertrandplez/Turbospectrum_NLTE` commit `9fbc391`, May 2025 → **BABSMA v20.1**. **Mac runs the SAME v20.1** — verified by executing both; no cross-machine engine drift |
| ├ MOOG / moog-scat | `…/synthesizer/moog/MOOGSILENT` | **BUILT** | moog `nov2019`; moog-scat `January 6, 2023` |
| ├ SPECTRUM | `…/synthesizer/spectrum` | **BUILT** | `2.77c` |
| ├ ARES · SME | `…/synthesizer/{ARES,sme}` | **BUILT** | ARES 2 (28/06/2020); SME 574 |
| ├ Model atmospheres | `…/input/atmospheres/{ATLAS9.Castelli, MARCS.GES}` | **PRESENT** | the RYA-285 1D-LTE base |
| ├ Molecular line lists | `…/input/linelists/turbospectrum/molecules/` | **INSTALLED 2026-08-05 (RYA-313)** — **100 files / 58 M**: CH (12CH+13CH), CN, C2, OH, NH, `CO_IR_Li2015.dat` | installed **from the repo's own vendored copies** (`data/linelists/molecular/turbospectrum/`, git-tracked, provenance-stamped), not re-downloaded. **Install the FULL vendored set** — a partial install (CH/CN/C2/CO only) trips the RYA-355/360 iSpec↔vendored drift guard on the omitted species (OH, NH), which is the guard working as designed |
| └ *(not installed, by design)* | mid-IR `*_rovib.bsyn` (CH MoLLIST · OH MYTHOS · NH kNigHt) | **VENDORED IN REPO, `in_ispec:false`** | RYA-503 — installing into iSpec is a **v2** step (RYA-444/504 boundary); deliberately excluded |
| **Standalone Turbospectrum_NLTE** | `/srv/codex/engines/Turbospectrum_NLTE` (99 M) | **BUILT** — Engine-B TS-native NLTE deck | RYA-533, v20.1, gfortran; separate from iSpec's copy |
| **PySME** | `/srv/codex/engines/pysme_src` (21 M) + `venv_pysme` | **BUILT** | Engine-A delta derivation (RYA-410/529) |
| **TSFitPy** | `/srv/codex/engines/TSFitPy` (83 M) + `venv_tsfitpy` | **BUILT** | RYA-542 atmosphere cross-check |
| **NIRPS DRS** | `/mnt/codex-data/engines/nirps_drs` (**131 G**, on the data drive) | present | RYA-494/500 |
| **NLTE grids** | `/mnt/codex-data/grids/nlte` (+ `gerber_overflow` 148 G) | **PROVISIONED** | md5-pinned (RYA-540); Sirius-only (RYA-567) |
| **Model-atmosphere grids** | `/mnt/codex-data/grids/model_atmospheres` (506 M) | **PROVISIONED** | — |
| **Spectra · line lists** | `/mnt/codex-data/{spectra,linelists}` (2.3 G · 30 M) | **STAGED** | per-target staging = RYA-420 |
| **Reference venv** | `/mnt/codex-data/venv312` (963 M) | **BUILT — DO NOT INSTALL INTO IT** | py3.12.13 / numpy 2.2.6, exact astropy+pandas pins (RYA-517); a stray `pysme-astro` install once downgraded it (RYA-526) |
| **CI venv** | `/mnt/codex-data/venv_ci` (839 M) | **BUILT 2026-08-05 (RYA-313)** | py3.12.13 from `requirements.txt` (numpy 2.5.1, pytest 9.1.1, astropy 8.0.1) — CI proves the DECLARED dependency set installs and passes |
| **CI runner** | `~/actions-runner`, systemd `actions.runner.damienabraxas-exoplanetcodex.sirius` | **ONLINE** | labels `self-hosted,Linux,X64,sirius`; git-lfs 3.7.1 installed 2026-08-05 |

**RYA-527 readiness (verified path-by-path, 2026-08-05):** every iSpec path the pipeline actually reads is present on Sirius — `input/abundances/Asplund.2009/stdatom.dat`, `chemical_elements_symbols.dat`, `atmospheres/{ATLAS9.Castelli,MARCS.GES}`, `isotopes/SPECTRUM.lst`, `linelists/transitions`, `linelists/turbospectrum/molecules`, `regions/42000_VALD`, plus the MOOG/SPECTRUM/Turbospectrum binaries. **Sirius is provisioned to execute the RYA-527 two-engine run.** iSpec's `input/{grid,minigrid,isochrones,spectra}` are absent on Sirius and that is fine — grep confirms the pipeline never reads them (they are iSpec-interactive extras).

**Git LFS:** `data/linelists/vald_*_raw.txt` (22 files, ~460 M) is LFS-tracked. **A fresh clone without `git-lfs` silently yields 132-byte pointer files where line lists should be** — `check_stewardship` then reports "no parseable transitions", which reads like data corruption. CI fetches LFS and asserts the content materialised (RYA-313).

---

## Gates — NATIVE

| Gate | Detail | Status | Cite |
|---|---|---|---|
| Solar β sign-off | 4-phase gate. Phase-1 Fe: Fe II arbiter PASS (406, merged), Fe I value PASS (336, on main), Fe I scatter threshold recalibrated (446, merged); remaining = explain the 509 Fe II 7.657/n=3 anomaly (via 515) | OPEN — much closer than "fails everything"; nothing proceeds to a science target until all 4 phases sign off | RYA-251 (supersedes RYA-101) |

---

## Scope / drift corrections — NATIVE
_Supersede the pinned project-instructions doc (a May-2026 snapshot) wherever they conflict._

| Item | Current truth | Cite |
|---|---|---|
| Target order | Sun → Procyon → α Cen A/B → 55 Cnc A (**55 Cnc LAST, not primary**) | project / RYA-109 |
| HST | STIS + COS G130M/G160M only; **WFC3 EXCLUDED** | RYA-119 |
| Telluric | instrument-aware (see Instruments) — NOT cr2res+molecfit universally | RYA-424 |
| NARVAL source | claimed **tbl.omp.eu** (PolarBase = CFHT/ESPaDOnS only) | **[OPEN]** — no ticket citation found in-repo; do NOT treat as settled until sourced |
| Target engine | Turbospectrum synthesis (all 27) + MOOG EW comparison | RYA-285 / constants.py |
| VALD | manual web extraction (per skill); synthesis-era central-depth 0.001 | RYA-389 [confirm] |
| [O I] 6300 measured-O | continuum-renorm measurement = **STOP (continuum-limited)**; production adopts Caffau-2015 8.73, [O I] is a cross-check only — do NOT re-attempt the renorm proof | RYA-447→455 (STOP proofs in `pipeline/diagnostics/`); flagged by RYA-530 |

---

## Versioning & edit protocol — NATIVE

**History:** the register lives in git — every edit is a commit, so `git log` / `git diff` IS the full historical version record. No separate versioning system.

**In-file:** the header carries `Version: vN · date · by · change`. The Changelog below logs each meaningful update in one human-readable line (git holds the full diff).

**When the register gets updated (triggers):**
1. A gate signs off — **mandatory**; a gate cannot sign off while its rows are stale.
2. A verdict changes — a component settles, regresses, or is superseded.
3. A milestone is reached.
4. "We now know X works / doesn't" — a validated finding.

**Who decides:**
- BIG moments (verdict changes, new SETTLED rows, milestones) — **Ryan + Claude decide together.**
- MIRROR rows (params, abundances) — script-regenerated from source (`scripts/gen_state_register_targets.py`), automatic, no decision needed.
- Mr. Code proposes register updates in end-of-session comments; Ryan + Claude ratify the significant ones.

**Claude's standing reminder duty:** when a register-worthy moment lands in conversation — a verdict, a confirmed fix, a milestone, a "this works now" — Claude proactively says *"this belongs in the register."* Ryan need not remember to ask.

## Changelog
- **v31** (2026-08-08) — **RYA-681 — the RYA-553 Fe 1D→3D idempotency guard is re-keyed on the VALUE; the `FE_GATE` blind spot is closed by an orthogonal scale-identity check, NOT by narrowing the gate.** **Why:** RYA-669 measured a fresh `phase_c` run landing **A(Fe I) 7.416** — the Magic-2013 −0.05 dex correction applied to a value that already carried it — with `tests/test_solar_calibration_gate.py` **9 passed** and the RYA-632 ledger guard **0 undocumented**. **(1) Root cause, both ends of a loop.** `phase_c_verdict_rya371.py` skipped the offset iff the gold row's `method_scale` contained `3D`; `build_solar_reference_v2_rya522.py::_scale_and_note` returned the LITERAL `"1D-NLTE (Fe I)"` for Fe regardless of the verdict. So the value and the label were maintained by different processes inside a closed loop (gold vN → verdict → candidate → freeze → gold vN+1), making this a **ratchet**, not a one-off: RYA-665 froze the post-correction 7.466 under the pre-correction label, the guard re-armed, and each further freeze cycle would subtract another 0.05. **(2) The re-keying (§2A, option 1+3 combined).** New single-purpose module **`pipeline/solar_scale_provenance.py`**. An explicit machine-readable **`scale_state`** column on the gold row is the authority, written by the builder FROM the verdict's own `fe_1d3d_correction` record — **data copied from data, never a hand-maintained string** — and Fe's prose label is now DERIVED (`method_scale_label()`) so the two cannot desync. Because a column alone cannot help the already-frozen v3, the declaration is **cross-checked against the value**: the two scale states are separated by a KNOWN tabulated distance and both centres are independently published (Asplund 2021 3D-true **7.46**; 3D-true + the granulation term **7.51**), so *which scale a number is on* is a two-hypothesis classification with the boundary at their midpoint — **half-width |correction|/2 = 0.025, derived from the correction itself, not a tuned tolerance**, and it moves automatically if the tabulated offset is ever revised. `resolve_gold_scale()` **RAISES `ScaleProvenanceError`** on (a) declaration-vs-value disagreement, (b) no declaration at all — the old guard's `''` fallback silently meant *apply* — and (c) a value on neither scale. A silent re-arm is now a loud load failure, and it generalises to any element that ever acquires a reported-layer correction (keyed off `CORRECTIONS_3D`, no per-element edit). **(3) The gate (§2B) — `FE_GATE [7.41,7.51]` is deliberately UNCHANGED.** It is a symmetric ±0.05 *physical acceptance* window on the 3D-true reference, and the correction is also 0.05, so a doubled correction lands at `A_correct − 0.05`, which is inside the window **for any correct anchor at or above 7.46**. That is structural, not an accident of today's numbers, and no symmetric numeric window centred on the reference can separate *correct* from *corrected twice* when the correction is comparable to its half-width. Narrowing to h < 0.044 would catch this instance while leaving the correct anchor only 0.038 dex of headroom and still missing a smaller doubled correction — fitting the coincidence, not the defect. The defect is a **provenance** error, so RYA-166 **gate A1 gains an orthogonal provenance assertion**: the reported anchor must classify onto the **3D-NLTE** scale, and where the record says the correction was applied, the value it was applied TO must classify onto **1D-NLTE** (with a doubled correction that pre-value is 7.466, a 3D number — caught). **7.466 → 3D, PASS, margin 0.019 dex (≈4× its 0.006 offset); 7.416 → NEITHER, FAIL; 7.516 → 1D, FAIL.** A1 also no longer requires `applied is True`, so a future 3D-declared gold does not break the gate. **9 assertions in, 9 out.** The same identity is reported by the `abundances_derive` solar Fe gate. **(4) The regression test (§2C) — the actual hole.** `tests/test_fe_1d3d_idempotency_rya681.py`, 26 tests, drives the **shipped** A1 gate (not a copy) against substituted verdicts: **FAILS on 7.416** (`reported A(Fe I)=7.416 is not on the 3D-NLTE scale (classified None…)`) and **PASSES on 7.466** — both directions exercised, plus a full-artifact substitution run showing 10 passed → 3 failed. It also asserts the *premise* (7.416 IS inside FE_GATE; |correction| == gate half-width) so the reason for the orthogonal check cannot silently lapse, and covers: correction applied exactly once on a v2-shaped row, skipped on a v4-shaped row, **converging rather than ratcheting** under repeated application, and raising on gold v3's shape / an already-doubled anchor / an undeclared scale / a mis-stamped `scale_state`. **(5) ⚠️ STANDING BLOCKER — intended, loud, and Ryan's to clear.** Gold v3's Fe row is genuinely self-contradictory, so `phase_c` now **refuses to regenerate** instead of silently emitting 7.416. Gold is write-once (RYA-469) and **the wrong cell is the `method_scale` LABEL, not the value — 7.466 is the correct 3D-scale number** — so this clears only via a **ratified gold v4**, which is **RYA-669's decision**. Seventeen tests that re-run `build_verdicts` off the live gold carry a **CONDITIONAL** xfail whose reason is single-sourced in `tests/gold_scale_blocker.py` and read off the live row, so promoting a self-consistent gold **retires all seventeen automatically**; nothing is suppressed unconditionally, and the RYA-166 gate plus the new verdict-artifact assertions run unblocked and fail on 7.416. **(6) Scope discipline:** no gold vN, no `CURRENT`, no `hash_manifest.json`, no verdict artifact, no `constants.py`/`STAR_PARAMS`, and **no measured value** was modified; gold v3 sha256 `47ad869efd06c046377d04a5b1bb1254edc4e6174d05aff4be7244a99a583421` verified unchanged. The RYA-653 corrected candidate IS regenerated (the builder changed) — **no value moved**, only Fe's `method_scale` `1D-NLTE (Fe I)` → `3D-NLTE (Fe I, Magic 2013)`, its note, and the new `scale_state` column; it is now the self-consistent shape a gold v4 would take. `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` is a historical vs-v2 snapshot and is deliberately NOT regenerated. Ledger guard unchanged: 2 documented-owed, **0 undocumented**. Files: `pipeline/solar_scale_provenance.py` (new), `tests/gold_scale_blocker.py` (new), `tests/test_fe_1d3d_idempotency_rya681.py` (new), `scripts/phase_c_verdict_rya371.py`, `scripts/build_solar_reference_v2_rya522.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`, `tests/test_phase_c_verdict_rya371.py`, `tests/test_blank_cause_attribution_rya596.py`, `tests/test_ew_integrity_rya458.py`, `tests/test_mn_hfs_synthesis_rya473.py`, `tests/test_solar_ba_synthesis_rya559.py`, `data/reference/solar/solar_abundances_corrected_candidate_rya653.csv`, `CODEX_STATE_REGISTER.md`, `SEQUENCE.md`.
- **v30** (2026-08-08) — **RYA-565 — Europium adjudicated: the "finished treatment" claim was FALSE, and Eu stays owed-no-value.** Eu emitted no value while `nlte_two_engine_coverage.csv` claimed *"LTE synthesis is the finished treatment"*. Settled by **doing** the measurement: an LTE in-window HFS blend-fit synthesis (`scripts/rya565_eu2_synth_sirius.py`, Sirius-only per RYA-567) over **5 HFS-resolved LWHS Eu II lines × 2 arms × 4 legs**, blends modelled verbatim from the VALD3 block (RYA-551/585 pattern). **Result: OWED-NO-VALUE.** Eu II 6645.0905 / HARPS: **A_LTE +0.702, rchi2 0.16, dEW/dA 13.9 mÅ/dex** vs the RYA-551/560/585 floor **40.0**, not railed. **The fit is not the failure — it is good** (61σ detection; Eu-only synthetic EW 6.34 mÅ reproduces the committed 6.8 mÅ `hfs_total_ew`). The decisive diagnostic is **`cog_linearity` 0.95**: the line sits on the **linear** curve of growth where sensitivity per mÅ is already maximal, so the shortfall is **intrinsic weakness — not saturation or blend swamping — and no S/N can lift it over the floor**. Re-running 6645 buys nothing. **Binding error is SYSTEMATIC:** line-to-line scatter **0.349 dex** (range 0.94 over 5 lines), cross-arm spread 0.099, vs σ_A(noise) **0.0074** — photon noise ~47× smaller. A 0.35 dex spread alone disqualifies a point value; forcing one on an upper-limit-risk r-process element is what the **Li caution (RYA-563)** exists to prevent, so none was emitted. **Measured, not asserted:** the as-shipped VALD leg fits **+0.300 dex** above the GES leg vs **log10(2)=0.3010** predicted from Turbospectrum applying `isotopfrac` itself — the isotope double-application is confirmed by experiment. `canonical_gf.csv`'s Eu II rows store the naive cross-isotope sum (exactly log10(2) above physical); the harness asserts that checksum and loud-fails on drift rather than rewriting the single source. **Registry reconciliation:** "finished treatment" → noise-floor-limited/owed pending a cleaner line or higher-SNR blue arm (Eu II 4129/4205); **the same optimistic claim was found in a SECOND file the ticket did not name** — `nlte_grid_availability.csv` (`synth-covered` → `synth-attempted-owed`, "LTE-synth covers" → "does NOT cover"); `element_status_tracker_editorial.yaml` `action_needed` retargeted to the blue arm (classification → `GENUINELY-OWED (noise-floor)`), tracker regenerated **Eu row only**. **LTE by design** — Eu II is the majority ion, no departure grid in either engine (`NLTE_unavailable_LTE_robust`, RYA-458); the blocker was never NLTE. ⚠️ **Two defects surfaced, deliberately left alone:** frozen gold v3 holds **`Eu,I`** while measurement/tracker/EW reference are all **Eu II** (same class as RYA-663's `Sr I`/no-`Sr II` row), and the machine-sourced tracker reason still reads *"no independent-gf line survives the graded cull"* when the truer reason is the sensitivity floor (6645 **is** LWHS-adjudicated) — that string comes from the phase_c verdict, not regenerated here. **NO measured value, gold reference, verdict or `constants.py` modified; gold v3 sha256 verified unchanged.**
- **v29** (2026-08-08) — **RYA-581: the Ba II 5853 deblend LANDED — A(Ba) 2.410 → 2.237, verdict CURATION-OWED → PASS, value still HELD.** **Why:** RYA-559 derived 2.410 by inverting the OBSERVED pool EW (74.62 mÅ), which carries `blend_flag=True` and is ~10 mÅ over the clean solar line (~64 mÅ). An EW inversion **cannot** deblend — a single scalar has no way to separate barium from the rest of the absorption inside the integration window, so the neighbours were charged to Ba; RYA-559 recorded this and routed the debt to RYA-581. **What changed:** `scripts/rya581_ba2_deblend_sirius.py` (new) fits the PROFILE instead, on the RYA-551 Sr II in-window pattern and the shared RYA-643 fitter — the full VALD3 in-window block (1432 rows, 63 species, 5846.7–5860.7 Å) is synthesised alongside the 23 Ba II HFS/isotope components (rescaled onto `canonical_gf` `gf_001470`, log gf −1.000) with the Ba II VALD rows removed so the measured element is never double-counted, and A(Ba) is fitted by χ². Engine-A Korotin2015 δ **−0.0285** is READ at the solar node from the production grid. **A_LTE 2.266 → A_NLTE 2.237** on HARPS (primary, per RYA-421/551); IAG gives 2.205; inter-arm σ 0.016. **The blend is modelled, not culled** — the harness runs a Ba-alone CONTROL every time, so the deblend is a measured quantity: red χ² **3.90 → 0.71 (5.49× better)** with the blend present, depositing 6.61 mÅ under the core (±0.4 Å), dominated by **Fe I 5.65 mÅ** (then CC I 0.42, CN I 0.17, V I 0.10). **The honest split:** of the −0.173 dex total, only **−0.020** is the in-window blend model and **−0.153** is abandoning the EW inversion — the blend sits largely in the WINGS, so it dominates the residual without moving A much, whereas the EW integration swallowed it whole. **Independent check:** the fitted synthetic core EW **66.24 mÅ** reproduces the RYA-559 calibration (A=2.27 → 66.5 mÅ) and the literature clean line (~64–66 mÅ) — the profile fit recovered the CLEAN line the pool EW never isolated. Robustness: fit-window sweep 0.4/0.5/0.6/0.8 Å → 2.235/2.235/2.237/2.238; damping sensitivity (VALD vdW vs Unsold) −0.000 dex; not railed, gsig not railed, dEW/dA 43.4 mÅ/dex. `scripts/phase_c_verdict_rya371.py`: `_load_ba_synthesis` now prefers `data/results/solar_ba_deblend_rya581.json` and `_ba_reclassify` branches on ticket into a dedicated deblend fold; the RYA-559 record is deliberately left intact as the loader fallback. **Verdict moves to PASS** (|−0.033| ≤ TOL_PASS 0.10 and the fit meets the reliability floor) — reported honestly rather than force-held, per the RYA-581 spec. **⚠️ NOT promoted to gold, and the disposition cannot move:** it rests on **ONE line**; σ 0.016 is inter-arm precision only (no gf / Korotin-δ / 1D-MARCS / single-line terms, now labelled `sigma_basis` in the record); and **Ba emits no two-engine record** — the synthesis route is unwired in `_dedicated_engine_B()`, exactly as for Co/RYA-564 — so **gate 3 stays UNEVALUABLE and cannot see 2.237**. Flagged for the RYA-527 freeze review; the second clean Ba II line (6141.713 / 6496.897) is the confirming follow-up. **⚠️ Two unratified thresholds for Ryan:** `RELIABLE_RCHI2 = 60.0` is inherited from **RYA-564 only** (`rya564_co1_synth_sirius.py:96`) — RYA-551 defines no reliability constant and RYA-560 gates on sensitivity + railed alone — and has never been separately ratified; and **RYA-585/PR #189** adds an equivalent `fit_profile_deblend` to the same shared `scripts/solar_profile_fit.py` carrying a **~5.0** ceiling. The duplication and the 60.0-vs-5.0 divergence need one ratified answer before both land. Not load-bearing for this result: Ba fits at red χ² 0.71, so `reliable` is identical under 60, 5, or no ceiling. Validate-don't-tune held throughout — profile, canonical gf and Korotin δ all READ, never fitted toward Asplund 2.27. Suite green on Sirius (1183 passed); RYA-166 gate 9/9; gold v3 sha256 unchanged.
- **v28** (2026-08-07) — **RYA-665 — gold v3 is FROZEN: the pre-527 mini-freeze.** `data/reference/solar/CURRENT` moves **v2 → v3**, promoting the RYA-653 corrected candidate through `scripts/promote_solar_reference.py` (RYA-469 write-once + hash manifest; no hand-edit path). **Exactly two frozen VALUES change** — **Fe I 7.516→7.466** (RYA-553 tabulated Magic-2013 1D→3D) and **Mn I 5.47→5.466** (RYA-546 ab-initio, reversing RYA-411's keep-MPIA) — plus two verdicts (**Co I → PASS**, RYA-564; **N I NLTE-OWED → CURATION-OWED**, RYA-556) and the RYA-653/596 note-honesty corrections on Ba/Ca/Ti/Ni/Na/Al/Sr. **Disputed values are NOT immortalised:** the RYA-522 `owed` tier freezes no value, so Co 4.965, S 7.486, Ba 2.410, N 8.188 and Sr all land as visible-but-HELD rows, and RYA-581's Ba deblend stays open post-freeze. Verified 29/29 cells against `docs/audit/solar_gold_rya653_diff_vs_frozen_v2.md` before applying. Ledger guard **3 → 2** failures (Ba's phantom red cleared, 0 undocumented); its Ba `_OWED_NOT_LAUNDERED` entry and the four Class-B gold-lag divergences for Co/N were **deleted per their own retire-on-freeze instructions** — a stale ratified divergence is a hiding place for the next real contradiction. **NOT the two-engine re-emit:** RYA-527 Phase 2 still owes a fresh run on current main, which validates v3 or produces v4 (Ca's provisional flip, Na pending RYA-664, Sr II 2.759 adoption, Co 4.965-vs-4.960). Files: `data/reference/solar/solar_abundances_v3.csv` (new, frozen), `data/reference/solar/CURRENT`, `data/reference/solar/hash_manifest.json`, `data/audit/known_verdict_divergences.yaml`, `pipeline/ledger_consistency_guard.py`, `tests/test_data_namespacing_rya469.py`, `tests/test_solar_gold_v2_rya522.py`, `tests/test_element_status_tracker_generator_rya654.py`, `tests/test_ledger_consistency_guard.py`, `CODEX_STATE_REGISTER.md`.
- **v27** (2026-08-07) — **RYA-654: the element status tracker becomes a GENERATED product, and the physics_regime GET-DATA-vs-measured divergence class is adjudicated.** **Why:** RYA-632's audit found the tracker had **no generator at all** — hand-edited, referenced only by `docs/CONVENTIONS.md`, and stale in exactly the way that predicts (Co still at the DEMOTED blue-edge +1.188, N still NLTE-OWED after RYA-556 cleared it), which is what its own count check was tripping on. **(1) Generator** `scripts/generate_element_status_tracker_rya654.py` (RYA-436 Move B, results side). One source per column: status (`verdict`/`verdict_value`/`sigma`/`n_lines`/`delta_vs_asplund`/`method`) ← the phase_c verdict channel, **ratified canonical for status**; `tier` ← the RYA-522 ratified freeze tiers, read from `build_solar_reference_v2_rya522.confidence_of` rather than re-implemented; `ion`/`regime_verdict` ← `config/physics_regime_rya400.yaml`; the analyst columns (classification, action, engine vintages, tickets, notes) ← a new hand-authored **`data/audit/element_status_tracker_editorial.yaml`**, seeded verbatim so no provenance was lost. The frozen gold reference is **deliberately not a source** — it owns VALUES, not status, and it currently lags on Co/N. `--check` makes a hand-edit a build break; the generator **refuses to run off an uncommitted phase_c** (the enforceable Sirius-only lever — the artifact carries no host/run-id, flagged as owed), and the emitted header pins the phase_c blob + commit. A new explicit **`verdict` column** retires the tier→verdict inference the guard used to need, because a verdict (live status) and a tier (freeze decision) are different things. The **Fe II arbiter row** is emitted from `FE_IONIZATION_SYNTH_ARBITER` — which moved it from the hand-typed **7.486** to the constant's **7.500**. **(2) The six divergent GET-DATA rows, re-cut on the EW-measurable-vs-synthesis-sourced axis** using live EW-pool line counts rather than documentation state: **Co (0 EW lines** — HFS, EW path cannot reach it; measured by red-line synthesis, RYA-564), **N (0** — "no clean EW N I in VIS" per the row's own hazard; measured on the KP atlas, RYA-460/556), **K (0** — resonance doublet in the O2 A-band, EW leg RYA-380-blocked; KP 7699 + NLTE, **PASS and frozen in gold**), **Cu (2, both junk** — 4767 at EW 197.8 mÅ unphysical, 4704 high-Rydberg; the 5 standard lines are HFS ⇒ measured by synthesis, RYA-466) → all four **RATIFIED as ledger divergences** in `data/audit/known_verdict_divergences.yaml`. An entry annotates a **channel**, never a promotion: all four keep the verdicts their results artifacts already carried. **P** is the one member with **real EW-pool lines** (P I 6034.04 / 6043.12, clean) and its GET-DATA rested on "FUV needs HST/STIS (RYA-119)" — a premise phase_c records as **superseded for the Sun** (the near-IR multiplet is ground-reachable, A(P) 6.61) → **row FIXED**, `GET-DATA` → `HARD-carry-forward` (remaining blockers: no P I NLTE grid + a +1.20 gf-scale residual, RYA-161/162). **Sc** is **measurement-not-trusted** — its only value is the KP Sc II 4246 blue-edge HFS single line, which phase_c itself holds LOW_CONFIDENCE "before any PASS" — so there is nothing to diverge from: **no annotation, no fix, deliberately still RED**. **(3) Result:** both RYA-632 count mismatches cleared as a by-product of generation (**PASS 5→6**, Co; **NLTE_OWED 1→0**, N), unblocking RYA-564/556 from the guard; the guard now reports **0 undocumented failures**, with 2 documented-owed elements — Sc, and **Ba** (gold v2's phantom blank cause; corrected in the **RYA-653 candidate**, lands at the **RYA-527** gold v3 re-freeze, which also retires the two gold-lag divergence entries for Co/N). New `_OWED_NOT_LAUNDERED` mechanism **annotates a red without suppressing it** — it may never become a second exceptions file, and a test enforces that. **(4) Housekeeping:** the guard now single-sources its three paths from `pipeline/state_surfaces.py` as RYA-659 asked; the editorial sidecar is registered there as a state surface. **Scope discipline:** no measured value, verdict, gold vN, `constants.py` or `STAR_PARAMS` was modified. **Ti confirmed untouched** (LOCKED, 81 measured Ti I lines, clean across all four artifacts). ⚠️ Noted, not fixed: this file's header sat at **v25** while the changelog already had a **v26** — RYA-653 bumped the header without a changelog entry and RYA-660 the reverse; v25 has no entry. Files: `scripts/generate_element_status_tracker_rya654.py`, `data/audit/element_status_tracker{,_editorial.yaml}.csv`, `data/audit/known_verdict_divergences.yaml`, `config/physics_regime_rya400.yaml`, `pipeline/ledger_consistency_guard.py`, `pipeline/state_surfaces.py`, `docs/CONVENTIONS.md`, `docs/physics_regime_map_rya400.md`, `LEDGERS.md`, `tests/test_element_status_tracker_generator_rya654.py`, `tests/test_ledger_consistency_guard.py`.
- **v26** (2026-08-06) — **RYA-660: Sirius storage reliability recorded as an OPEN RISK** (Infrastructure table). The OS SSD stalled >40 s under an `apt`/DKMS write burst, the kernel's 30 s SCSI timeout turned that into an aborted ext4 journal and a **read-only root**, the CI runner crash-looped, and **every merge was blocked 16 h** before anyone noticed. Recovered (reboot + `e2fsck` → `Filesystem state: clean`; the only lost files were DKMS build artifacts). **The row exists because "recovered" ≠ "resolved":** SMART is **PASSED / 0 reallocated / 0 CRC / no logged errors** on *both* drives, and `sda` threw the same failure class on a *different* SATA port on 2026-07-27 — so the cause is most likely **host-side** (controller / power / firmware) and is still unexplained. Mitigated only by a **30 s → 180 s** SCSI command-timeout udev rule (a seatbelt: the stall becomes survivable, not absent). Alerting owed **RYA-661**, single-runner CI SPOF decision owed **RYA-662**. No science value, verdict, gold reference, or constant was touched.
- **v24** (2026-08-05) — **RYA-313/314: the merge gate becomes real.** RYA-313 adopted PR-gated merges on 2026-06-14, but **CI had never run in this repo's history** — no `.github/`, zero workflow runs — so the ruleset's "require status checks" list was empty and there was nothing to gate on. **(1) Green suite (prerequisite).** `main` was **14 red**. Adjudicated each against live truth, not edited to pass. **12 were stale expectations** — tests asserting a *closed* defect still exists: **RYA-545** (Ti moved to the Mallinson-2024 ab-initio grid → 3 lines / δ+0.052, not MPIA's 8+ / +0.108; and its 2500–8000 K hull retired the 55 Cnc clamp, so Ti leaves `STILL_CLAMPED`), **RYA-540** (Cu grid derived + registered, so "Cu must not be registered" ×3 are closed gaps), **RYA-526/556** (N wired; the problem_children registry already carries N I as `DATA_GAP` — "NOT physics-owed: the grid is WIRED" — not `NLTE_OWED`), **RYA-522** (gold v2 re-freeze moved `CURRENT`; the three tests making a *historical reproduction* claim against the RYA-398 graded-cull diagnostics now name **v1** explicitly instead of following the moving pointer). Distinctions preserved rather than flattened: **Cu's GRID is present while Cu's PRODUCTION stays GET-DATA on measured-line QUALITY** (RYA-395/466) — registered-and-still-owed, the same shape as owed-HELD; and the physics_regime LOCKED invariant is **refined, not weakened** — `LOCKED ⇒ n>0` is kept intact while the converse gains an adjudicated one-element exception list that self-invalidates if Cu ever locks or loses lines. **2 were a real bug:** RYA-506 added the iSpec/MOOG scratch-dir `makedirs` to ONE call site while three others handed the same literal path to `ispec.determine_abundances` without creating it → `FileNotFoundError: /tmp/ispec_codex/...` whenever the dir was absent; single-sourced as `abundances_derive.ensure_ispec_tmp_dir()`. **(2) A latent defect CI itself surfaced — `np.trapz` was REMOVED in NumPy 2.0** (renamed `trapezoid`). `requirements.txt` floors `numpy>=1.26` with no ceiling and **RYA-517 declares the reference stack as py3.12 + numpy 2.2**, so **all 12 `np.trapz` call sites** (EW integration in `lines_fit`, `cno_synthesis`, `abundances_derive`, `pysme_nlte`, the O I 6300 audit, plus five Sirius synthesis scripts) were **already broken on the production runner** — invisible locally because the Mac dev env pins numpy 1.26. Resolved once in `pipeline/_numcompat.py`; NOT papered over by pinning numpy<2, which would contradict the ratified RYA-517 stack. Suite now green on **both** stacks with identical counts: py3.9.6/numpy1.26.4 and py3.12.13/numpy2.5.1 → **1116 passed, 9 skipped, 1 xfailed**. **(3) CI on a Sirius self-hosted runner.** NOT `ubuntu-latest`: 24 of 118 test files import iSpec (external, **not vendored**, compiled MOOG/SPECTRUM), so an ephemeral runner cannot even COLLECT them — measured on a clean tracked-files-only checkout with no `../ispec`: **24 collection errors, 868/1116 passing**, i.e. a green check hiding 22% of the suite, the dishonest-green RYA-314 §2.2 forbids. Sirius already carries the engines (verified `iSpec OK; MOOG support: True` in venv312/py3.12.13), is the declared authoritative runner (RYA-511/517), and running CI there honours **RYA-567** instead of fighting it; `ISPEC_DIR` is set from the environment (the override already existed) rather than symlinking the checkout. All guard families run **inside the `test` job** so the required-check string stays `CI / test` as guards are added: pytest (blocking) · register-freshness RYA-659 (blocking, PR + push modes, `fetch-depth: 0`) · ledger-consistency RYA-632 (`continue-on-error`; that branch is unmerged so it currently logs "no module" and blocks nothing, activating when it lands with no edit here). A preflight step loud-fails on a missing venv/iSpec or an unmounted `/mnt/codex-data` (RYA-419 sentinel). **(4) Ruleset.** `Require PR on Main` now also requires the `CI / test` check; **`allowed_merge_methods` restricted to `merge`** (merge-commit house style, Ryan-ratified 2026-08-05 per the RYA-305 precedent — squash and rebase disabled); the redundant `Require Pull Request before merging` ruleset (which despite its name carried **no** `pull_request` rule — only deletion/force-push/update — plus an admin `bypass_mode: always`) is retired. **SECURITY — the repo is PUBLIC**, so a self-hosted runner would execute fork-PR code on real hardware: the fork-PR policy is raised to **require approval for all external contributors** (was "first-time contributors") and the trigger is `pull_request`, never `pull_request_target` (read-only token, no secrets). Residual, knowingly accepted: the runner runs as `damienabraxas`, which holds NOPASSWD sudo → moving it to an unprivileged service account is a flagged follow-on. **(5) Register correction:** the RYA-530 note "Cu — grid NOT committed to `nlte_grids/` and NOT wired" had been **stale for two months** (superseded by RYA-540 `70456d2`) and is corrected — the CI suite is what caught it, via the tests that still asserted it. **(6) Sirius provisioned for RYA-527 + a new "Compute assets — where everything lives" section** (the archive of what is downloaded, built and ready to compute). Findings: **the engines are NOT on the data drive** — `ispec_src`/`pysme_src`/`TSFitPy`/`Turbospectrum_NLTE` are symlinks into `/srv/codex/engines/` on the **OS drive** (`/dev/sdb2`, 81 % used, ~85 G free), correcting the Infrastructure row's long-standing "`/mnt/codex-data`: … engines" shorthand. **Molecular line lists INSTALLED on Sirius** (**100 files / 58 M**: CH 12CH+13CH, CN, C2, OH, NH, `CO_IR_Li2015.dat` — the FULL vendored set; a partial install trips the RYA-355/360 iSpec↔vendored drift guard on the omitted species, which CI duly caught) from the repo's own git-tracked vendored copies — no re-download, no Mac→Sirius haul — while deliberately **excluding** the mid-IR `*_rovib.bsyn` (RYA-503 `in_ispec:false`, a v2 boundary). **`test_turbospectrum_binaries_exist` was asserting the wrong directory**: iSpec invokes `<ts>/bin/babsma_lu` at runtime while the test checked the `exec-gf/` BUILD dir — an incidental layout detail where the two machines legitimately differ (Mac keeps real binaries in `exec-gf/` with `bin/` symlinks; Sirius builds straight into `bin/`). The old assertion passed on the Mac and failed on Sirius while testing neither machine's ability to synthesize; it now asserts the runtime path is present AND executable. **Engine parity confirmed by execution: both machines run BABSMA v20.1** (iSpec's bundled `bertrandplez/Turbospectrum_NLTE` commit `9fbc391`) — no cross-machine engine drift, so RYA-517's null-drift claim extends to the synthesis engine. **RYA-527 readiness verified path-by-path**: every iSpec path the pipeline reads is present on Sirius; `input/{grid,minigrid,isochrones,spectra}` are absent and provably unused. Also documented the **Git-LFS trap**: a fresh clone without `git-lfs` yields 132-byte pointer files where line lists should be, which `check_stewardship` reports as "no parseable transitions" — CI now fetches LFS and asserts the content materialised. Files: `.github/workflows/ci.yml`, `pipeline/_numcompat.py`, `pipeline/abundances_derive.py`, `pipeline/curate_nonfe_pools.py`, `pipeline/cno_synthesis.py`, `pipeline/lines_fit.py`, `pipeline/pysme_nlte.py`, `pipeline/diagnostics/oi6300_ew_continuum_audit.py`, six `scripts/*.py`, nine `tests/*.py`, `CODEX_STATE_REGISTER.md`.
- **v23** (2026-08-04) — **RYA-659 register re-synced to the RYA-556..652 backlog + LEDGERS.md + a register-freshness guard.** **Why:** the register had drifted **11 state-changing tickets** — last touched 2026-07-17 (`08265f9`, RYA-553) while `data/audit/element_status_tracker.csv` moved to 2026-07-27 (`97485e9`, RYA-643). RYA-643 changed element state and did **not** bump the register: the exact hole "a gate cannot sign off while its rows are stale" exists to close. Every number below was read from the LIVE artifact (`docs/audit/solar_phase_c_verdict_rya371.md` gen 2026-07-27 / `data/reference/solar/solar_abundances_v{1,2}.csv` / `data/audit/element_status_tracker{,_drift}.md`), never from the brief. **(1) Solar 27-el verdict 5/1/20/0 → 6 PASS / 0 NLTE-OWED / 20 CURATION-OWED / 0 DATA-GAP**, PASS = O, C, Fe, Mn, K, **Co**; the v22 STALE flag is cleared for these landings, but the re-freeze on the two-engine floor remains **RYA-527** (verdict v-next, gold v3). **(2) N I off NLTE-OWED (RYA-556)** — the registered N I grid is wired into the phase_c Kitt Peak channel; live A(N) **8.188** (+0.358), CURATION-OWED. The wired per-line δ is −0.0115/−0.0145/−0.0154 (**mean −0.0138**), which **REFUTES the old audit's predicted ≈ −0.37** — N I red is near-LTE at the Sun, so the surviving +0.36 is a gf/data-channel floor (**RYA-161**), not an NLTE debt. **(3) Post-v22 value landings recorded as MEASURED-awaiting-v3-freeze** (a new row; explicitly NOT presented as frozen gold, which still reads `owed` for S/Ba/Co): **S I 7.486** (RYA-557, Costa-Silva+2020 gf synthesis — confirmed the audit prediction; the CS gf moved it only ~−0.03, the +0.366 is a gf-SCALE floor), **Ba II 2.410** (RYA-559, Ba II 5853 HFS + Korotin2015 NLTE, off the prior blank — **but the pool EW is ~+0.15 blend-inflated**, clean-EW cross-check 2.187–2.231, deblend owed), **Co I 4.965** (RYA-564 red-line HFS; the blue-edge Co I 3845 +1.188 is RETIRED as untrusted), **Mn I 5.466** (RYA-546, consistent — supersedes 5.554). **(4) Sr II — RYA-551 superseded by RYA-643:** 4077 **2.769 → 2.759** (Δ −0.010), 4215 2.724 → 2.693 (Δ −0.031), worst reliable \|Δ\| **0.031 ≤ 0.05 → CONFIRMED-ROBUST, cleared for RYA-527 adoption**. Both caveats carried explicitly so "cleared" is not misread as "adopted": (a) the live verdict channel **still reads the Sr I EW leg** (4.961, +2.13) — a wiring gap to close at RYA-527; (b) near-UV fit quality is a SEPARATE open concern — red-χ² **78.3** (4077) / **180.0** (4215) vs Mg **1.4**; robustness-to-the-defect ≠ fitness-for-adoption. **(5) New owed-HELD vs owed-BLANK row (RYA-596)** so "owed" stops reading as "undone": **owed-HELD** (firewall left survivors, value exists in gold v1, held unfrozen by the ratified RYA-522 tier) = Ca **6.324** (3 survivors), Ti **5.471** (10), Ni **6.946** (2), Na **6.264** (2), Al **7.406** (1); **owed-BLANK** (zero survivors, claim verified) = Mg (0/5), Y (0/3), Zr (0/6), Eu (0/1) — with the RYA-596 caveat that for **Zr/Eu the blank is SAT/BLEND/HIERR-driven, not gf-grade-driven**. **(6) New machinery registered:** RYA-558 (Cr II ratified-exclusion guard in the two-engine floor), RYA-561 (registry-sourced STRICT three-gate floor-promotion guard — promotes Ca only; Mg = ratified HOLD 7.614), RYA-563 (Li UPPER_LIMIT disposition guard), RYA-567 (Sirius-only compute + grid provenance, loud-fail), RYA-279/280 (Fe-pool integrity, regression-locked), RYA-594 (element_status_tracker.csv + discipline in `docs/CONVENTIONS.md`), RYA-596 (blank-cause honesty tripwire — noted **phase_c-only**, the gold builder's `_scale_and_note` gap is still open), RYA-631 (Codex Systems Catalog, 13 systems, index-by-pointer validator), RYA-642/652 (instrument 25 / modes 11 / holdings 11 catalogs), RYA-643 (rest-frame + gsig fitter single-sourced — the defect was **four** copies, Mg regression 0/121 differ). **(7) Durable anti-recurrence:** **`LEDGERS.md`** at repo root names the canonical read-set (register · element tracker · system catalog · instrument+modes catalog · holdings registry) with owner, update trigger and generated-vs-native status, pointed to from `data/catalog/README.md`, `DEV_CYCLE.md` and `README.md`; **`scripts/check_register_freshness.py`** loud-fails when a state surface is newer than the register (history mode) or when a PR changes state without the register (`--since-main`), with the surface registry single-sourced in `pipeline/state_surfaces.py` (importable by RYA-632). **NO CI harness exists** (no `.github/workflows/`, Makefile, tox or pre-commit) — flagged as a follow-on shared with RYA-632; a CI system was deliberately not invented here. **Scope discipline:** no measured value, verdict, gold reference, `constants.py`/`STAR_PARAMS` or gold vN was modified; the MIRROR targets block is untouched and `gen_state_register_targets.py --check` is clean. **Open disagreement surfaced, not resolved:** the verdict artifact reports **A(Co) 4.965** while RYA-643's corrected re-run (one day newer) reports **4.960** — recorded on both rows as the same cleared-≠-adopted class as Sr II; the v3 re-freeze (RYA-527) is where it reconciles. Files: `CODEX_STATE_REGISTER.md`, `LEDGERS.md`, `scripts/check_register_freshness.py`, `pipeline/state_surfaces.py`, `tests/test_register_freshness_rya659.py`, `data/catalog/README.md`, `DEV_CYCLE.md`, `README.md`.
- **v22** (2026-07-17) — **RYA-553 solar Fe 1D→3D correction APPLIED** (unblocks the RYA-527 v3 freeze on the true 3D scale). The tabulated Magic-2013 (Stagger 3D grid, A&A 557 A26) 1D→3D solar Fe offset — long *defined* (`FE_1D3D_SOLAR_OFFSET = 0.05`, used only to centre a diagnostic window) — is now **applied** to the reported Fe I anchor, mirroring the CNO pattern (O I forbidden lines already carry −0.07 Caffau). **(2a)** `CORRECTIONS_3D['Fe_1D3D_solar_dex'] = -FE_1D3D_SOLAR_OFFSET` (single-source magnitude, no duplicated literal; negative because 1D-NLTE runs +0.05 *above* 3D-true). **(2b)** Applied in `phase_c_verdict` `build_verdicts` to the reported/selected Fe I value (the value the RYA-166 gate + RYA-527 gold-v3 freeze read), additive after NLTE: A(Fe I) **7.516 → 7.466** (3D-NLTE), δ vs Asplund +0.006 (was +0.056). Logged + pre/post recorded on the verdict row (`fe_1d3d_correction`); **idempotent on `method_scale`** (skips an already-3D anchor → a re-frozen v3 gold is never double-corrected, the fine-grained analogue of the RYA-334 gross double-add tripwire). Also restored the Fe I raw scatter from the canonical RYA-407 pool (the frozen gold carries no per-line σ), so the regenerated verdict keeps σ=0.139 ≤ 0.1398 floor. **(2c)** `FE_GATE [7.41,7.51]` restored as the real solar reported-value gate (abundances_derive solar Fe gate now shows the 3D-corrected value vs FE_GATE); `FE_ABS_DIAG_HALFWIDTH` retired *as the solar gate* but retained for the un-corrected 1D-NLTE measurement layer + off-solar stars (per-Teff/[Fe/H] 3D owed → RYA-550); `FE_1D3D_SOLAR_OFFSET` kept (now consumed) + RYA-334 tripwire kept. **(2d)** RYA-166 gate A1 → asserts the 3D-corrected A(Fe I) ∈ FE_GATE **and** guards the correction is applied + single-sourced; A2–A8 unchanged; all 9 tests green; a gross zero-point error (7.76) and the uncorrected 7.516 both FAIL (gate keeps teeth). Fe II NOT shifted (ionization arbiter reads a ratified constant). **SOLAR ONLY.** Files: `config/constants.py`, `scripts/phase_c_verdict_rya371.py`, `pipeline/abundances_derive.py`, `tests/test_solar_calibration_gate.py`, `CODEX_STATE_REGISTER.md`.
- **v21** (2026-07-14) — **RYA-549 Fe anchor reconcile + vintage** (the RYA-524 headline, resolved & DE-ESCALATED; unblocks the RYA-527 Beta re-freeze). **Gate-0 forensic (read history, don't assume):** `Fe_Bergemann_MPIA.csv` is the *intentional* live Fe NLTE source — set by **RYA-319** (`5d42702` "switch Fe NLTE to MPIA grid-lookup (single source)", merged `472a237`), chosen because it covers the **full benchmark box** (Teff 5000–6600, logg 4.0–4.6, [Fe/H] −0.2..+0.35) whereas the ab-initio **Amarsi-2022 NN** MLP ceilings at Teff≤6500 / [Fe/H]≤0 (can't reach Procyon 6554 or 55 Cnc +0.32) and is ARCHIVED (kept for the RYA-283 3D-vs-1D solar cross-check only). So the register's NLTE-grids "Amarsi-2022-NN" selection row was **STALE** (never updated post-RYA-319); the code was correct. **Integrity fix:** reconciled that register row → MPIA; the wiring matrix already said MPIA → now **code = register = matrix**. NO code change. **Vintage verdict:** the RYA-524 "Fe on inflated scaled-Drawin" flag is confirmed-but-BENIGN — the MPIA Fe NLTE δ (median over 252 Fe I lines) is **+0.010 at solar / +0.004 at 55 Cnc** (Fe II ≈0), an order of magnitude below the Ti/Mn/Cr inflated class (+0.108), and ionization-balance-gated (RYA-406 Fe I−Fe II ≈0 PASS) — the +0.056 vs Asplund 7.46 is the documented 1D-NLTE-vs-3D scale offset (RYA-336), NOT the NLTE correction. **55 Cnc residual-risk** (the RYA-546 Part-B logic on the anchor): the param-dependent non-cancelling part of [Fe/H] = δ(55 Cnc) − δ(Sun) = +0.004 − 0.010 = **−0.006 dex, negligible**. Ab-initio migration STOPPED (no banked Fe grid covers metal-rich; the correction is +0.01 so the ab-initio-vs-SH difference is bounded well below tol) → deferred to **RYA-550** (low priority). Files: `CODEX_STATE_REGISTER.md`, `data/curation/nlte_grid_availability.csv` (note), `data/audit/rya549_fe_anchor/findings.md`. NO merge-blocking code touched.
- **v20** (2026-07-13) — **RYA-545 Ti I wired onto the Mallinson-2024 ab-initio grid** (the STOP→GO corroboration-accept, Ryan 2026-07-13; MERGES). Swapped the production Ti Engine-A NLTE source from `Ti_Bergemann2011_MPIA.csv` (Bergemann-2011 **scaled-Drawin**, +0.108 — inflated ~2× per the RYA-546 vintage audit) to `Ti_Mallinson2024_PySME.csv` (Zenodo 10753497, ab-initio **Grumer-Barklem-2020** H collisions). Registered Ti permanently in `pipeline/pysme_nlte.py` (NLTE_LINES/_A_SUN/_GRID_FILENAME/_Z; labels energy-matched from the grid's shipped `label_Ti.txt` because our Amarsi-format reader mis-parses the Mallinson level table) and derived the multi-node grid via the RYA-410 PySME/MARCS harness (`scripts/rya545_derive_ti_grid.py`): Ti I 5689/5648/5662 across the 11-node benchmark box, solar δ **+0.0506** (per-line +0.050/054/051, reproduces published +0.052), a physical Teff/logg trend (5100→+0.028, 6200→+0.060, logg 4.0→+0.063; hull 2500–8000 K, so the RYA-505 6500 K Ti wall is retired). **Ti CHECK → registered**, certified by the NEW **corroboration-accept** standing rule (`docs/SCIENCE_STANDARDS.md` — five bounded criteria, **Ti = first invocation**): the ionization-balance gate STOPPED on *precision* (SEM ~0.12–0.16, thin solar Ti pools; RYA-545), but the value is ab-initio (firewall intact), reproduces Mallinson-2024, is corroborated by two independent blend-aware instruments (LTE balance ≈0 / NLTE ≈+0.05), and the incumbent is confirmed wrong (RYA-546) — so it registers on corroboration, the gate recorded as a documented diagnostic (threshold NOT lowered). **RYA-534 Ti xfail: reason UPDATED (RCA closed), NOT flipped to PASS** — that guard exercises the Engine-B TS-Gerber deck, which still runs the outdated `atom.ti503b` (+0.22, superseded per RYA-546), so it stays honest strict-xfail (10 pass + 1 xfail) rather than falsely certifying the TS-Gerber Ti deck; the Engine-B Ti atom swap is a follow-on (**RYA-548**). Closes RYA-535/542. v1 frozen A(Ti) 5.471 stays (forward/v2 correction only). Old CSV retained on disk, unwired (v1 immutable). Files: `pipeline/pysme_nlte.py`, `config/constants.py`, `data/nlte_grids/Ti_Mallinson2024_PySME.csv`(+`.prov.json`), `docs/SCIENCE_STANDARDS.md`, `scripts/rya545_derive_ti_grid.py`, `tests/test_ts_gerber_rollout_rya534.py`.
- **v19** (2026-07-13) — **RYA-546 Mn re-derivation** (the vintage-audit HIGH rec, merges). Re-based solar Mn NLTE onto the ab-initio Amarsi-2020 GALAH grid: on the measured low-EP Den Hartog triplet 6013/16/21 (which NaN on the MPIA high-EP grid — the RYA-411/473 blocker) the **live** `nlte_Mn_scatt_pysme.grd` gives HFS-resolved δ **+0.024** (per-line +0.030/+0.024/+0.017, HFS identical on both legs), vs the registered MPIA scaled-Drawin +0.107 (Bergemann & Gehren 2008) and the RYA-534 Engine-B ab-initio +0.043. Re-ran `measure_mn_hfs_synthesis_rya473` on Sirius (grid now live): A(Mn)_LTE 5.442 + δ +0.024 → **A(Mn)_NLTE 5.466 (+0.046 vs Asplund 5.42) → phase_c PASS** (was 5.554 on the +0.107 high-EP mismatch). **REVERSES RYA-411's "keep MPIA" (which trusted B&Gehren-2008 as "dedicated" — RYA-546 shows it is the outdated scaled-Drawin recipe); closes RYA-411.** Landed Addition B: `pysme_nlte.assert_in_grid_hull` hard-fails on out-of-hull (no silent extrapolation; wired into `nlte_delta`) so 55 Cnc / α Cen target runs are safe. Solar-only this pass; EW-path Mn grid repoint + gold v3 (5.554→5.466) = RYA-527 fan-out. Files: `pipeline/pysme_nlte.py`, `scripts/rya546_mn_gate0.py`, `config/constants.py` (Mn comment reversed, +0.107 SUPERSEDED for solar), `data/audit/mn_hfs_synthesis/*` regenerated, `data/audit/rya546_vintage_audit/mn_rederivation.md`.
- **v18** (2026-07-10) — **RYA-525 two-engine floor BUILT** (the governing-law machinery; full run = RYA-527). `pipeline/engine_selection.py`: per-LINE reference-blind selector (clause 1 validity gates → clause 2 one-eligible → clause 3 regime [clean-weak→Engine-A / hard→Engine-B] → clause 4 INDETERMINATE-only lower-σ, tie→1D-NLTE anchor) → inverse-variance element aggregation carrying only winning-line errors → `CROSS_ENGINE_MIX_GATE` flags mixed-but-disagreeing winners (the Ti lesson) → loud-fail guards (missing grid, single-engine-no-record, reference-proximity firewall) keyed on the RYA-526 two-engine ledger. All knobs in `config.TWO_ENGINE`. Law recorded in `docs/SCIENCE_STANDARDS.md`. **Silent-LTE swallow removed** from `abundances_derive.run()` (the two `except Exception → print → 1D-LTE` blocks around the NLTE calls) — a missing wired grid / NLTE failure now RAISES (RYA-536 Review 6a). 29-case test (`tests/test_two_engine_floor_rya525.py`, incl. the adversarial reference-closer-≠-quality firewall case) + sample records (`data/audit/rya525_two_engine/`). Ti stays CHECK/excluded (535/542) but both engines still run + record the spread.
- **v17** (2026-07-09) — **RYA-530 capability-sweep reconciliation** (capability axis, complement to RYA-524's verdict axis; pre-Beta gate for RYA-527/404). Enumerated live Linear (484 issues, 232 ≥300) + classified against origin/main @ `fe0c8b6` with code+git evidence. Registered the BUILT-but-unregistered capabilities the ledger had missed (the RYA-402 failure-mode): **single-source gf architecture** (gf_resolver + species + canonical_gf.csv 145k + stewardship CI, 345/353/355/358/368/408), **NLTE grids Al/K/S/Sr** (402 Fam-B/462/421 — were off the grid table), **HFS-resolved synthesis** (411/466/473/476; Mn gold measured via it), **EW-verification layer** (458), **loader frame/OBJECT contract** (481/264), **multi-arm CNO arm-registry** (464), **reflected-solar RV** (372/394), **UV conditioning** (426/190), **IR-native α Cen star-ID** (423/431), **multi-instrument loaders** (272/471), **molecular converter** (503), **vac↔air SSOT** (264/501), **1D→3D applied-delta** (399, split from the absent 3D-RT engine). Corrected the **Na/Mg/Si** NLTE row (mis-cited Lind/MPIA → Amarsi-2020 PySME, RYA-410). Cu/V notes added (Cu grid uncommitted, V void). Flagged the **[O I] 6300 continuum-renorm dead-end** (STOP proofs — don't rebuild). Owed-dedup (Linear, not register): 317/330/334/336 merged-but-Backlog; 361 superseded by 533, 362 by 534; 474 (dead `CORRECTIONS_3D`) still owed. Map + edits: `data/audit/rya530_capability_sweep/`.
- **v16** (2026-07-09) — RYA-535: **Ti reverted from 'finding' to CHECK pending RCA.** The v15 '~2× Ti model-atom difference (Gerber-2023 atom.ti503b vs Bergemann-2011)' label is REFUTED by source: Gerber et al. 2023 (A&A 669 A43), Table 1 lists the Ti NLTE atom as **Bergemann (2011)**; `atom.ti503b` is just the release's filename for that atom. Engine-A (our MPIA Bergemann-2011 anchor +0.108, MAFAGS-OS) and Engine-B (TS-Gerber, MARCS) therefore use the **same atom** — a model-atom difference between an atom and itself is impossible. +0.221 vs +0.108 is an unresolved **same-atom systematic** (candidate causes: MAFAGS-OS-vs-MARCS atmosphere / interpolation / downstream EW-inversion), RCA-mandatory. De-encoded the rollout test (Ti now strict-xfail, not an allowed pass → 10 pass + 1 xfail, no 11/11). **Solar 5 PASS intact; the 9 clean Family-A (Na/O/Mg/Ca/Sr/Ba/Ni/Co/Si) + their PASS entries stand unchanged.** Mn's ~½ carries the same open provenance question (RYA-411). NOT merged.
- **v15** (2026-07-08) — RYA-534 rollout **COMPLETE: all 11 Family-A elements provisioned + TS-Gerber-deck-validated on Sirius.** 10 reproduce their published 1D-NLTE anchor within tol (PASS). **Ti = CHECK, honestly NOT forced to pass:** re-gated with strong MPIA-comparable lines (5689/5648/5662, well-measured 8-19 mA) → median +0.221, consistent across all 3, vs our Bergemann-2011 grid +0.107 → recorded (prematurely) as a ~2× Ti model-atom difference. **[CORRECTED in v16, RYA-535: this label is refuted — same Bergemann-2011 atom on both engines; it is an unresolved same-atom systematic, RCA open.]** Ti grid is the largest (~26 GB zip/55 GB bin) and was heavily Keeper-throttled. Per-element md5-pinned provenance for all 11.
- **v14** (2026-07-08) — RYA-534 rollout: **Ba/Ni/Co/Si landed → 10/11**. Ba II 4554 δ −0.018 (Korotin) + Sr II 4215 −0.013 = ionised-species (GES stage=2) fix validated on both. Ni 5018/5035 δ +0.018 (vs Bergemann +0.02) + Co 5000/5013 δ +0.099 (vs +0.10; weak sub-mÅ lines) = the Co/Ni GES level-ID fix validated. Si 5772 δ −0.034 (vs our PySME −0.013). All departures engaged, PASS. Provisioned sequentially one grid at a time (fixed the earlier concurrent-unzip truncation). Only Ti remains — its first 55 GB-grid zip download was truncated (corrupt), clean re-download in flight. Provenance md5-pinned per element.
- **v13** (2026-07-07) — RYA-534 rollout cont'd: **Sr landed** (Na+O+Mg+Ca+Mn+Sr = 6/11). Sr II 4215.519 resonance δ −0.013 vs our Sr_Bergemann2012_INSPECT (~−0.005, near-LTE at solar) → PASS, departures engaged — confirms the ionised-species (GES stage=2) fix. Remaining 5 (Si/Ti/Co/Ni/Ba) re-provisioning one grid at a time.
- **v12** (2026-07-07) — RYA-534 rollout cont'd: **Mn landed** (Na+O+Mg+Ca+Mn = 5/11). Mn I 6013/6021 δ +0.043 vs anchor +0.10 (Bergemann; our MPIA +0.107) → PASS — but ~half the registered Bergemann value: a genuine cross-engine data point, consistent with the RYA-411 Amarsi(+0.05)-vs-Bergemann(+0.107) Mn model-atom discrepancy (Gerber's atom.mn281kbc = Bergemann-2019 sits nearer Amarsi). Remaining 6 (Si/Ti/Co/Ni/Sr/Ba) hit real issues the deck's guard caught (Sr/Ba ionised-species GES lookup; Co/Ni unidentified-upper-level → silent-departure=1 RAISED; Si/Ti large-grid truncation under concurrent-unzip disk contention) — ALL fixed in the driver config; pending clean one-at-a-time re-provision. Rollout doc has the resume.
- **v11** (2026-07-07) — RYA-534 rollout cont'd: **Mg + Ca landed** (Na+O+Mg+Ca = 4/11). Mg I 5711 δ −0.023 vs our Mg_Amarsi2020_PySME −0.022 (cross-engine agree to 0.001); Ca I 6122/6162 δ −0.009 vs Mashonkina +0.017 (PASS within tol; small sign difference on these strong lines, both ~0 — Ca NLTE is line-dependent in sign). Both departures-engaged. Provenance JSONs + gate results in `data/nlte_grids/gerber_ts/` + `data/audit/rya534_family_a_rollout/`. Remaining 7 (Si/Ti/Mn/Co/Ni/Sr/Ba) on-demand.
- **v10** (2026-07-07) — RYA-534: Family-A TS-Gerber NLTE rollout begins (on-demand, one ~5–16 GB grid at a time, Sirius-only). Element-general deck `scripts/ts_gerber_gate.py` (pulls each element's lines from the GES NLTE list by wavelength → level-IDs verbatim, the silent-departure=1 guard). **O landed:** O I 777 triplet median δ −0.105 vs our banked **Amarsi-2019 1D-NLTE** anchor −0.134 (nlte_cno table6; the 3D leg is −0.169) → PASS — doubles as the RYA-362 post-hoc-vs-in-synthesis O cross-check (agree to 0.03 dex). Validate-don't-tune: the first-pass literature −0.20 anchor (Drawin/3D-inflated) was corrected to the independent 1D-NLTE reference, not fitted. **Na + O = 2/11**; remaining 9 (Mg/Si/Ca/Ti/Mn/Co/Ni/Sr/Ba) on-demand. Rollout table → `data/audit/rya534_family_a_rollout/rollout_status.md`; O provenance `data/nlte_grids/gerber_ts/O_gerber2023.prov.json`.
- **v9** (2026-07-06) — RYA-533: TS-native Gerber NLTE synthesis deck (Engine-B NLTE for RYA-525). **Forensics-first verdict = NOT-BUILT** on all 5 channels (0a test-tell: no TS-Gerber reproduction test, only a docstring; 0b git: no surviving 402 branch, merged via the PySME line; 0c tree: `synth_ew_nlte_vs_lte` = NotImplementedError, no bsyn deck; 0d store: empty `gerber_ts/`, no TS_NLTE build; 0e reg: no Gerber flag — RYA-361 scoped it, Backlog/never-started). TS binary hooks ≠ our deck. **Built + Na-VALIDATED on Sirius:** Turbospectrum_NLTE **BSYN v20.1 compiled** (gfortran 15.2); **Na Gerber-2023 grid (15.9 GB) + model atom + aux provisioned** (md5-pinned, Sirius-only); bsyn NLTE deck wired (interpol_modeles_nlte departures → babsma → bsyn NLTE-vs-LTE COG). **GATE GREEN (validate-don't-tune): Na I 5682 δ−0.065 / 5688 δ−0.072, median −0.068 vs anchor −0.107 ± 0.05 → PASS** (the ~0.04 offset = Gerber-atom vs Lind-2011 model-atom difference; cross-engine Na = INSPECT −0.107 / PySME −0.129 / TS-Gerber −0.068 → the RYA-525 model-atom-systematic diagnostic). Silent-error site (line-list level IDs) handled via the GES NLTE line list; deck RAISES if departures don't engage. Engine-B TS-native NLTE **SETTLED for Na**; other 10 Family-A elements = mechanical per-element repeat. Deck `scripts/ts_gerber_na_gate_rya533.py` + test `tests/test_ts_gerber_nlte_rya533.py` (the reproduction test whose absence was the Step-0 tell) + `data/audit/rya533_ts_gerber_build/`. Resolved 2022-vs-2023: arXiv 2206.00967 = **A&A 669, A43**.
- **v8** (2026-07-06) — RYA-531 corrected the RYA-529 row: recorded the two-family NLTE-**derivation** map (Family A TS-Gerber-planned-but-never-executed / Family B PySME) and the **Engine-A vs Engine-B** distinction. Anti-reinvention finding: production NLTE is universally a 1D delta CSV added to the TS/EW LTE abundance (**Engine A**, multi-source — PySME-derived Amarsi CSVs for Na/Mg/Si/Al/K/S/N, MPIA for Fe/Ca/Ti/Cr/Mn, Korotin for Ba, INSPECT for Sr, nlte_cno for C/O, LTE for Ni/P/Co/Y/V/Sc/Li/Eu/Zr/Cu); **Engine B (Turbospectrum synthesis) is LTE-only for all 27** — the Gerber-2022 TS-native departure deck is NOT built / NOT provisioned (empty `gerber_ts/` on Sirius). Engine-B NLTE OWED/loud (the genuine RYA-525 gap; the Na TS-Gerber gate could not run — TS-native never built, RYA-402 skipped it). Per-element Engine-B map → `data/audit/rya531_engineB_map/engineB_per_element.md`.
- **v7** (2026-07-06) — TS NLTE synthesis (RYA-402) re-provisioned + re-validated on **Sirius** (Engine B substrate for the two-engine floor). Amarsi-2020 base-13 b-factor grids (Zenodo 3982506) already on Sirius (RYA-477/526) + `venv_pysme` (py3.12.13 / numpy 2.2.6 / PySME 1.0.2); INSPECT Na −0.107 reproduced (median −0.129) through the **PySME** b-factor synthesis path — RYA-402's TS-`bsyn` deck remains a `NotImplementedError`, so PySME (native `.grd` consumer) is the validated NLTE-correction engine per Ryan's RYA-402 decision, Turbospectrum stays the LTE synthesis engine. NLTE-synth ready for the base-13 elements, LTE-synth elsewhere; Cu/S grids not re-provisioned. Unblocks RYA-525.
- **v6** (2026-07-06) — RYA-526 set-down + RYA-525 architecture capture. **Two-engine floor** ratified as governing law (RYA-525, build-pending — SCIENCE_STANDARDS.md/selection-fn/loud-fail guard not yet wired). **N wired** into production NLTE (RYA-526: `N_Amarsi2020_PySME.csv`, near-LTE at the Sun, warm-star indicator; 4 cool-metal-rich COG nodes excluded → loud-flag) + N I NLTE-grid row. **Infra:** Sirius scope extended to all downloads/extractions (Mac disk-fill); PySME venv-isolation, venv312 pinned/untouched (transient downgrade, restored) — both RYA-526 incidents. Solar 5/1/20/0 verdict flagged **STALE** pending the RYA-527 two-engine re-freeze (verdict v-next, gold v3).
- **v5** (2026-07-05) — RYA-522 set-down (Ryan-ratified checkpoint, the picture stopped moving): solar gold reference re-frozen **v2** from the verdict channel (RYA-521), **C 10.26→8.491** (RYA-520 saturated-C I-5380 fix), **tiered confidence** (gold C/O/K/Mn/Fe/Sc; gf_floor Cr/Si; upper_limit Li; owed/held incl S/Sr/N/Co — no value immortalised), v1 immutable+SUPERSEDED. Solar verdict → **5/1/20/0**. Sirius = authoritative runner, py3.12/numpy2.2, null cross-machine drift (RYA-511/517). Verdict = single authoritative channel; raw EW diagnostic-only (RYA-521). "merged ≠ wired" wiring-integrity row (RYA-463 orphaned → wired, RYA-519). +2.1-tail Sr flagged for saturation-trace.
- **v4** (2026-07-04) — VENDORED (RYA-516, Mr. Code): confirmed every row against its ticket + `constants.py`; **Targets table now script-generated** from `STAR_PARAMS` (was hand-typed). Corrections from source: Sun Teff resolved to 5772; Procyon [Fe/H] +0.03 / ξ 1.8; α Cen A/B params emitted from source (0.20) with the RYA-435 ratification caveat; 55 Cnc [Fe/H] +0.31. Merge states verified vs `origin/main` @ `3425995`: RYA-406 (#47) + RYA-407 (#48) + RYA-336 + RYA-446 (#71) confirmed **merged** → upgraded from SETTLED-PENDING-MERGE to SETTLED. Telluric [confirm] resolved to RYA-424 (+ NIRPS nuance). NARVAL source flagged **[OPEN]** (no ticket). Wired maintenance into `DEV_CYCLE.md` + `skills/codex-state-register`.
- **v3** (2026-07-04) — Fe rows corrected after verifying RYA-406/407: no Fe II regression (arbiter 7.486 balanced, on main; 406 reconciled the gate); Fe I scale-gate 336 already on main; Fe I scatter 0.138 = honest floor (446). Retracted the "329 drives scatter" guess. Added the versioning & edit protocol.
- **v2** (2026-07-04) — expanded to full personal-codex: targets & params (MIRROR), NLTE grids, 3D models, line lists, instruments; added the NATIVE/MIRROR distinction.
- **v1** (2026-07-04) — seed: solar anchor, Procyon, pipeline integrity, infra, gates, scope/drift corrections.

---

_Rows tagged `[confirm]` are reconstructed from working memory and carry citation debt — a row is not truly settled until its authoritative source is attached (per the value-provenance rule). Maintenance procedure: `skills/codex-state-register/SKILL.md`._
